# Next-Step V2 详细设计

> vibe-coding 第 2 步·详细设计。输入：`需求文档.md` + `概要设计.md`。
> 给每个模块定接口 / 数据结构 / 依赖 / 关键逻辑 / 验收要点；开头先拍定 3 条 lead 实现级取舍。
> 代码事实基于实地勘察（artifact-service.ts / pending-change-service.ts / artifact-guard.ts / profile-session-wiring.ts / agent-profile-session.ts）。

---

## A. lead 实现级取舍（3 条，已拍定 → 记 `../../docs/设计决策记录.md` ADR）

> QA 轮次 5 把这 3 条列为"拆任务时定"。本轮由 lead 按 `lead-autonomy` 拍定（标 🔧）；用户 review 本文时可推翻。

### 🔧 取舍1 · `propose_edit` 收 **id**（不收 path）
- **背景**：artifact 既有 id(uuid) 又将有真实文件 path(crd.md)，propose 用哪个定位？
- **选项**：A 收 id；B 收 path；C 都支持。
- **决策：A 收 id。** **理由**：现有 API / PendingChangeStore / ArtifactService 全按 `artifactId`，收 id 零阻抗；agent 工作流天然两步——先 `list_artifacts` 拿 id → `propose_edit(id, newContent)`。path 反查（artifact.json 已记 filePath）作便利项，**后续按需再加**。

### 🔧 取舍2 · crd.md 物化到 **`<projectRoot>/<sanitize(title)>.md`**（项目根）
- **背景**：真实文件物化在哪，才"本地可开 + file panel 可见、类 Notion 页"？
- **选项**：A 项目根；B `docs/` 子目录；C `artifacts/` 子目录。
- **决策：A 项目根 `<projectRoot>/<sanitize(title)>.md`。** **理由**：最符合"类 Notion 页、一眼可见"；MVP 单/少 artifact 够用。物化路径在 `create` 时由 title 清洗生成、**存进 artifact.json 的 `filePath` 字段**，后续 submitVersion/rollback 以存的 filePath 为准（不随 title 改而漂移）。**多 artifact 拥挤 → 后续可改子目录**（登记，非本期）。
- **🔎 review 加固**：① `sanitizeFileName` **复用既有 `lib/domain/orchestrator.ts` 的实现**（已覆盖空串/纯点/分隔符/控制字符/兜底，配 5 单测）、提到共享位置，别另写半套——须覆盖边界：清洗后为空、纯点(`..`)、超长(加 length 上限避免 ENAMETOOLONG + v1 半成品)、与磁盘已有同名 .md 避让。② **缺 delete/清理工具 = 已知 gap**（受限集无 delete/bash → create 错一篇无法删、手删真实文件后侧车仍在致列表不一致）：本期 MVP 不实现 delete，登记后续。

### 🔧 取舍3 · `create` 与 `propose` **分开两个工具**（不合一）
- **背景**：创建（无上一版、无 diff）与修改（有上一版、走 diff→确认）流程语义不同。
- **选项**：A 分开；B 合一（按 id 是否存在自动分流）。
- **决策：A 分开。** **理由**：两者流程语义不同——创建直接落 v1+物化、无确认闸门；修改必走 diff→按块确认。分开各自单一职责、schema 清晰；合一会让"要不要走确认"隐式依赖 id 是否存在，易错（违 Simplicity / 模块独立）。

---

## B. 实现前置 · 工具机制 spike（命门，必做且先做）

第一轮 P0 的教训：**接线命门先 spike、GO 再展开**（省大坑）。V2 有两个未验证命门，实现**第一步必做 spike**（任务卡 `V2-0`），GO 才进 V2-1~V2-6：

1. **`defineTool` 精确签名**：pi 包导出 `defineTool`（`dist/index.d.ts:8`），但本设计未固定其 `parameters`（zod? JSON schema?）/ `execute(args, ctx)` 签名。spike 跑通一个最小自定义工具（如回显 `list_artifacts` 雏形），确认签名。
2. **受限工具集组合行为**（关键安全命门，**review 已修正命门认知 → D-V2-04**）：确认「`tools` 白名单 = **7 项 `["read","grep","find","ls","create_artifact","propose_edit","list_artifacts"]`（含全部 customTool 名）** + `customTools` = 3 提议工具」组合下，内核**既**激活只读内置工具、**又**挂上 3 提议工具，且 **write/edit/bash 确实不可用**。
   - **命门修正（D-V2-04）**：原以为"白名单只放只读工具、customTools 自动挂上"——**错且是 blocker**。内核对 customTools **同样按白名单名过滤**（`agent-session.js:1825-1831`：customTool 名不在 `tools` 白名单则被滤掉、连注册都不到）。故白名单**必须显式含 3 个提议工具名**，否则 agent 调不到、闭环断（与 P0「同名覆盖」不同的新组合）。
   - **负对照须双向**：spike 既证 **write/edit/bash 调用失败**（无绕过），也证 **3 提议工具确能被调起并执行成功**（闭环通）。

> spike 产出：GO/NO-GO + 确认的 `defineTool` 调用范式 + 受限工具集的确切 options 形态。记 ADR。

---

## C. 模块详细设计

### V2-1 · 文档物化层（`lib/domain/artifact-service.ts` 扩展）

**数据结构变更**：`Artifact` 增 `filePath?: string` —— 物化真实文件相对 projectRoot 的路径（如 `需求规格.md`）。写进 `artifact.json`。

```ts
export type Artifact = {
  id: string; projectId: string; kind: string; title: string;
  currentVersion: number; version: number; status: "draft" | "finalized";
  filePath?: string;          // 新增：物化真实文件（相对 projectRoot）
  extra?: Record<string, unknown>;
};
```

**新增私有方法**：
- `private materializedPath(projectId, artifact): string` = `join(registry.get(projectId).root, artifact.filePath)`。
- `private materialize(projectId, artifact, content): void` —— 原子写（临时+rename，仿现有 `atomicWrite`）真实文件。

**改造现有方法**（在既有逻辑末尾追加物化，不动既有版本/侧车逻辑）：
- `createArtifact`：生成 `filePath = sanitizeFileName(title) + ".md"`（**复用 `orchestrator.ts` 既有 sanitizeFileName**；冲突加序号、与磁盘已有 .md 避让）；写进 artifact.json；建 v1 后 `materialize(content)`。
- `submitVersion`：提交新版后 `materialize(newContent)`（覆盖真实文件 = 当前版）。
- `rollback`：回退后 `materialize(回退版 content)`。
- **🔎 外部编辑保护（D-V2-06）**：materialize **覆盖真实文件前**，先读真实文件当前内容、与"即将被覆盖的 version 快照（上一当前版 content）"比对；**不一致 → 抛 `EXTERNAL_MODIFIED`（ArtifactError 新 code，API 映射 409）拒绝物化并提示"真实文件已被外部修改，请先同步"**。防外部手改被 AI 确认静默覆盖致数据丢失。（首版 create 无上一版、不比对。）

**依赖**：仅 node:fs + 既有 ProjectRegistry + `orchestrator.ts` 的 sanitizeFileName。**不依赖** 上层。
**验收要点**：建→真实文件存在且 content==v1；改提交→真实文件==新版；回退→真实文件==回退版；artifact.json 含 filePath；title 含非法字符/空/纯点/超长被安全清洗；同 title 第二次建不覆盖第一份；**外部改真实文件后再 submitVersion → 抛 EXTERNAL_MODIFIED（不静默覆盖）**。

---

### V2-2 · 提议工具（`lib/pi/doc-tools.ts` 新建，`defineTool`）

**工厂**（仿 `assembleArtifactGuardOptions` 闭包注入范式，QA Q4 先例）：

```ts
export type DocToolDeps = {
  projectId: string;          // 闭包注入（execute 的 ctx 不带 projectId）
  sourceActor: string;        // 写进 version.author / PendingChange.sourceActor
  artifactService?: ArtifactService;   // 默认其文件后端
  pendingStore?: PendingChangeStore;
};
export function buildDocTools(deps: DocToolDeps): GuardToolDef[];  // 返回 3 个 defineTool 工具
```

**三个工具**（schema 与 execute 关键逻辑）：

| 工具 | parameters | execute 关键逻辑 | 返回 |
|------|-----------|------------------|------|
| `create_artifact` | `{ kind, title, content }` | `artifactService.createArtifact(projectId, {kind,title,content,author:sourceActor})`（V2-1 已含物化） | `{ id, filePath, version: 1 }` |
| `propose_edit` | `{ id, newContent }`（🔧 收 id；description 须**硬约束**见下） | **①查未决**：`listPendingChanges(projectId,id)` 非空 → 拒绝返回"该文档已有 N 处待确认，请先处理"（D-V2-05）；**②** `old=readCurrentContent` → `computeReplaceDiffBlocks`，**空块(无变化)→不 save、返回 `{changeId:null,diffBlockCount:0,note:"内容无变化"}`**；**③** `buildReplacePendingChange` → `pendingStore.save` —— **不写盘** | `{ changeId, diffBlockCount, note }` |
| `list_artifacts` | `{}`（MVP 不过滤） | `artifactService.listArtifacts(projectId)` | `[{ id, title, kind, currentVersion }]` |

**依赖**：V2-1（物化）+ 既有 pending-change-service（`buildReplacePendingChange` / `PendingChangeStore`）+ `defineTool`（pi）。
**关键点**：`propose_edit` 复用 guard 同款 `buildReplacePendingChange`，落的 PendingChange 与现有 resolve 路由 / PendingChangeCard 完全兼容（op=replace、diffBlocks 由 computeReplaceDiffBlocks 算）。确认流水线（resolve → applyResolvedBlocks → submitVersion → 物化）走既有 API 路由 + V2-1 物化，**零新增确认逻辑**。
**🔎 description 硬约束（coreIssue 核心，模型运行时唯一真读的通道）**：`propose_edit` 的 defineTool 描述字符串必须写明「**newContent 必须是完整新全文、未改段落逐字保留、不得改写未提及内容**」——用户"只改某段"的体验由 LCS（只切变化块）+ 按块确认交付，agent 内部仍回整篇；若回残篇，LCS 会把其余正文判为删除→满屏噪声。**收 id 口语桥**：3 工具 description 引导"用户用标题/文件名指代时先 `list_artifacts` 按 title 挑 id 再 propose"（不动 schema）。
**验收要点**：faux 驱动断言——create 落 v1+真实文件；propose 落 PendingChange（磁盘有 pending JSON、无新版本、真实文件未变）；**已有未决时再 propose 被拒**；**空/无变化 propose 不 save、不产幽灵版本**；list 返回清单含 id。

---

### V2-3 · 文档会话装配（`lib/pi/doc-session.ts` 新建，替 artifact-guard 装配位）

```ts
export function assembleDocSessionOptions(deps: DocToolDeps & { cwd: string }): {
  options: { tools: string[]; customTools: GuardToolDef[] };
};
```

**装配内容**：
- `tools`（白名单）= **7 项 `["read", "grep", "find", "ls", "create_artifact", "propose_edit", "list_artifacts"]`**（含全部 customTool 名，**D-V2-04 命门**：内核对 customTools 也按白名单名过滤、漏名则 agent 调不到、闭环断）—— **无 write / edit / bash**。
- `customTools` = `buildDocTools(deps)`（3 个提议工具）。
- skill：经既有 `assembleProfileSessionOptions` 的 `skillNames`（= profile.skills）启用，docSession 不重复处理。

**与 guard 的关键区别**（更简）：guard 要"拦 write/edit"故保留工具名 + 重建 operations；**V2 不要 write/edit/bash**，故白名单直接排除它们、customTools 只加 3 个新工具——**无需重建任何内核工具的 operations**。

**安全论证**（依赖 B·spike 验证）：白名单无 write/edit/bash → 内置写盘工具不激活；customTools 只加只读的提议工具 → **无任何直接写盘 / 执行路径 → 结构性无绕过**。这是 §F5 的实现。
**lead 取舍（受限集范围）**：给 read+grep+find+ls（只读探索全套，便于 agent 读材料/定位）+ 3 提议工具；比"只 read"更实用且仍无写盘能力。
**验收要点**：单测断言 options.tools = 含且仅含上述 7 名、**不含 write/edit/bash**；customTools 含且仅含 3 提议工具；spike **双向**负对照证 write/edit/bash 调用失败 + 3 提议工具确能调起。

---

### V2-4 · 接线改造（`lib/pi/profile-session-wiring.ts` 改 1 处）

**现状**（勘察 wiring.ts:126-135）：`assembleArtifactGuardOptions({sourceActor, cwd})` → `createAgentSession({...options, ...guardOptions, ...createOptionsOverride})`。

**改造**：把 `assembleArtifactGuardOptions` 换成 `assembleDocSessionOptions`：
```ts
const { options: docOptions } = assembleDocSessionOptions({
  projectId,                  // 由 projectRoot 反查（registry.findByRoot）或入参透传
  sourceActor: profile.name,
  cwd,
});
const { session: inner } = await createAgentSession({ ...options, ...docOptions, ...createOptionsOverride });
```

**注意点**：
- **🔎 spread 顺序 = 受限集生效的唯一支点（D-V2-04 / major4）**：`createAgentSession({...options, ...docOptions, ...})` 中 `options`(=assembleProfileSessionOptions) 含 `tools: profile.tools`、`docOptions` 也含 `tools`(7 项白名单)，**两个 tools 键相撞**——`docOptions` 必须 spread 在 `options` **之后**覆盖掉 `profile.tools`（否则 profile.tools 若含 write/edit/bash 会泄漏、受限集当场失效）。`wiring.ts:125` 旧注释"顺序无关"只对 guard(noTools、无 tools 键)成立、**不适用本轮**；spread 处必须加注释 + 加单测。
- `assembleDocSessionOptions` 需要 `projectId`（提议工具闭包要）；现 `startProfileSession` 入参有 `projectRoot`，需补 projectId（registry 反查或入参加 projectId）。
- **范围同 P0**：本轮把 **profile 会话**装配从 guard 换 docSession（P0 也只接 profile 会话）。dispatch / 主对话 / idle 不在本轮（本就未接 guard）。
- **🔧 登记后续**：是否区分"文档型 profile（受限工具集）vs coding 型 profile（完整工具）"——本轮 MVP 不区分（profile 会话 = 文档会话，承接 P0 范围）；若未来有 profile agent 需要 coding，再加 profile 标志位分流。登记 `后续`。
**验收要点**：经真实 `startProfileSession` + faux 起会话，工具集为受限集（含 7 名、不含 write/edit/bash）；**profile.tools 含 write/edit/bash 的档案经装配后泄漏对照测（断言被覆盖掉）**；create/propose 闭环可跑。

---

### V2-5 · 删 guard 层（`lib/pi/` 删除 + 清理）

- 删 `artifact-guard.ts` + `artifact-guard.test.ts`、`artifact-intercept.ts` + `artifact-intercept.test.ts`。
- 清理 import：`profile-session-wiring.ts` 去掉 `assembleArtifactGuardOptions` import（V2-4 已替换）。
- `resolveManagedTarget`：guard 用它判断"受管路径写"。V2 不再拦路径（agent 不直接写受管路径、受管内容经工具按 id 操作）→ **可删**。删前 grep 确认无其它引用。
**验收要点**：`grep -rn "artifact-guard\|artifact-intercept\|assembleArtifactGuardOptions\|resolveManagedTarget"` 仅命中已删文件自身（应无活引用）；`tsc` 无未解析 import。

---

### V2-6 · 端到端复用验证（`scripts/` + 既有测试）

- **逻辑层**：驱动文档会话 → agent `create_artifact` → 断言真实文件 + v1 侧车；`propose_edit` → 断言 PendingChange；走 resolve 路由按块确认 → 断言新版本 + 真实文件同步。复用 P0 的驱动范式（`spike/p0-wire-verify` 先例）。
- **🔎 局部修改正/负用例（coreIssue）**：正向=多段文档只改一段 → 断言**仅产 1 个 mod 块**（其余进 equal 不产块）；负向=传残篇 → 断言产大量 del 块（固化"必须整篇"契约）。
- **🔎 并发 / 外部编辑用例（D-V2-05/06）**：同一 artifact 已有未决 → 再 propose 被拒；外部改真实 crd.md 后确认 → 抛 EXTERNAL_MODIFIED 不静默覆盖。
- **真浏览器**：ArtifactPanel 看 diff/版本、PendingChangeCard 按块 ✓/✗、版本下拉/rollback。复用 D4/D5 的 E2E fixture/drive。
- **受限工具集**：断言会话工具集含 7 名、无 write/edit/bash（B·spike 双向负对照）。
**验收要点**：端到端 create→propose→按块确认→新版本+真实文件更新全通；局部改/并发/外部编辑用例通过；D3-D5 既有单测 + 真浏览器无回归；三类门禁全绿。
- **凭证现状（已变）**：本地 `~/.pi/agent/` 已配 DeepSeek（default `deepseek-v4-flash`，端到端验证有效）→ **V2-6 可真起 pi agent 验证、不必全靠 faux**（faux 仍作 hermetic 单测）。注意这是 **pi 运行时**凭证；Claude Code 实现 team 用 Claude 凭证、与此无关。

---

## D. 接线全景（数据流）

```
文档会话 agent
  │  create_artifact(kind,title,content)
  ├────────────────────────────────────────> ArtifactService.createArtifact ──> 写 v1 侧车 + 物化真实 crd.md
  │  propose_edit(id, newContent)
  ├──> readCurrentContent(old) ─> buildReplacePendingChange ─> PendingChangeStore.save（待确认，不写盘）
  │                                                                    │
  │                                              ┌─ ArtifactPanel/PendingChangeCard 渲染 diff 高亮（D3/D4）
  │  用户按块 ✓/✗ ─> resolve 路由 ─> applyResolvedBlocks ─> submitVersion ─┴─> 物化真实 crd.md（新版）
  │  list_artifacts() ──> listArtifacts ──> [{id,title,kind,version}]
  └─ read/grep/find/ls（只读探索） + skill（解析/转化）
       —— 全程无 write/edit/bash，无绕过路径
```

---

## E. 风险与待定

| 项 | 状态 |
|----|------|
| `defineTool` 签名 / 受限工具集组合 | **B·spike 验证**（GO 才展开）；命门=白名单须含全部 customTool 名（D-V2-04）—— 头号风险 |
| pi 运行时凭证 | **已配 DeepSeek**（`~/.pi/agent`，default deepseek-v4-flash，已验证）→ V2-6 可真调模型、不必全靠 faux。（Claude Code 实现 team 的 Claude 凭证是另一层） |
| **外部直接改 crd.md = 数据丢失** | **已升级处理（D-V2-06）**：非"不进版本"那么轻——会被下次 AI 确认**静默覆盖丢失**；本轮加 materialize 前比对、抛 EXTERNAL_MODIFIED 拒绝 |
| 同一 artifact 并发 propose | **已处理（D-V2-05）**：拒绝并存（propose 前查未决） |
| 文档型 vs coding 型 profile 区分 | 本轮不做、登记后续（V2-4） |
| 缺 delete/清理工具 → 孤儿文件 | 本轮不做、登记后续（V2-1） |
| 长文档 token / diff 噪声（block_id 方案 C） | 后置（需求 §6） |
| 本机 OOM | 实现 agent team 串行、E2E 重启 dev（需求 §5） |
