# V1.2 第八轮 · 详细设计（evict-by-sessionId）

> 配套：[需求文档](需求文档.md) · [概要设计](概要设计.md) · [方案B评审NO-GO记录](方案B设计评审-NO-GO记录.md)。承重轮。
> 全部 file:line 经 ultracode 调查 + 设计评审门核实 + lead 亲核「worker 工具集无 spawn」「__piSessions 写入仅 rpc-manager.ts:290-291 两点」。承重命门轻（纯应用层、零内核时序依赖）。

## 〇、设计要点

保留 F16 每阶段 evict 释槽机制，**只收窄逐出粒度**：`evictAgentSessions(agentId)` → `evictSession(sessionId)`。承重命门 = 「by-sessionId 逐出粒度正确」，T1 hermetic spike 坐实后开 T2。**不动 concurrency-gate / 内核 / owner-map / 上限**。

---

## 一、逐文件设计

### 1.1 `lib/pi/evict-agent-sessions.ts`【改·新增 evictSession】

**现状（已 Read）**：`:33-49` `evictAgentSessions(projectRoot, agentId, deps)`：`:38` `lookup = deps?.sessionsForAgent ?? sessionsForAgent` → `:41 for (const sid of lookup(projectRoot, agentId))` 逐个 `:44-46`「若 `inner.isStreaming` 先 `send({type:'abort'})` → `destroy()`」。`:11-13` 红线注释「绝不碰 bySession/removeOwner」。

**改动**：新增 `evictSession(projectRoot, sessionId, deps?)`：
- 复用 `:44-46` 单会话配方：`const w = getSession(sessionId); if (!w || !w.isAlive()) return; if (w.inner.isStreaming) await w.send({type:'abort'}); w.destroy();`（getSession 经 DI 缝、生产取 `globalThis.__piSessions.get`）。
- **跳过 `sessionsForAgent` 反查**（直接对单个 sid 操作）——这正是「不误删同 agent 其他会话」的关键。
- **绝不碰** `bySession`/`removeOwner`（:11-13 红线）；`evictAgentSessions` 保留不删（别处可能引用，grep 确认）。

### 1.2 `lib/domain/pipeline-orchestrator.ts`【改·逐出收窄】

**现状（已 Read）**：`:82` `doEvict = deps.evictAgentSessions ?? evictAgentSessions`（⚠️ 评审订正：spec 旧写 :80，:80 实为 doAcquireSlot）。正常路径 `:181-182` `stage.sessionId = result.sessionId; doSetOwner(...)` → `:187` `await doEvict(projectRoot, stage.agentId)`；catch `:167` 兜底 evict。

**改动**：
- `:187`：`doEvict(projectRoot, stage.agentId)` → 按 `stage.sessionId` 逐出（此时 `:181` 已赋值 `result.sessionId`）。最小改法 = 把注入的 `doEvict` 换成 `evictSession` 签名、传 `stage.sessionId`（`deps.evictSession ?? evictSession`、`deps.evictAgentSessions` 注入口相应改名）。
- catch `:167`：worker 抛错时若已建会话则有 `stage.sessionId`（在 catch 前 `:181` 赋值）→ 按 sid 逐出；未建会话则 no-op（sid 为 null/undefined 时 `evictSession` 内 `getSession` 返 undefined→直接 return）。
- 头注 `:9-12`「按 agentId evict」叙述订正为「**按本阶段 sessionId evict**（防跨 run 误杀同 agent 用户接管会话）」。
- **保留** `setOwner`(:182)、`acquireSlot`(:127) 顺序、cancel signal 链(:102)。

### 1.3 `lib/pi/evict-agent-sessions.test.ts`【改·追加 evictSession 单测】

> ⚠️ 评审订正：是**追加**（该文件已有 evictAgentSessions 测试，保留），非新建。

新增 `evictSession` describe：faux registry 放 2 个会话 sidA/sidB **同属 agentX**（模拟「本阶段 worker sidA + 用户接管 sidB」）。断言：① `evictSession(root, sidA)` 后 sidA 被 destroy、**sidB.isAlive() 仍 true**（不误删同 agent 其他会话）；② sidA.inner.isStreaming=true 时先 `send({type:'abort'})` 再 destroy（spy 顺序）；③ evict 后 `getOwner(cwd, sidA)` 仍返 agentX（owner-map 零碰、第五轮红线）。

### 1.4 `lib/domain/pipeline-orchestrator.test.ts`【改·承重断言 agentId→sessionId】

**承重 verify 结构不变**（仍每阶段 evict、size→0、peak≤1、acq→set→evict 顺序、负对照、catch 路径）——只把 evict spy 的**第二参断言**从 `agentIds[n]` 改为 `sessionId('s'+n)`，faux harness 的 `ownerByAgent` 改为按 sid。

> ⚠️ 评审订正（测试改造范围，按 describe 块枚举、勿漏）：evict spy 断言散布在——**承重块 :98-261**（含 :211-215 四阶段 + **:253 catch 路径**，旧 spec 上界 227 漏 :253）+ **T6 cancel 块 :603-775**（:737 + **:753**，旧 spec 716-757 漏 :753）+ **失败分支 describe :386 起**也注入 evict spy。三块的 evict 断言一并改。变异检查：把 evictSession 误写成「逐出该 agent 全部会话」（退化回 by-agentId）→ 承重块/误杀断言必红（证非 vacuous）。

---

## 二、承重墙（T1 spike，lead 复跑；未绿不开 T2）

**命门**：`evictSession` 按 sessionId 逐出**只 destroy 目标 sid、不误删同 agent 其他活会话、流式先 abort 防 jsonl 双写、不碰 owner-map**。

**这是「单会话逐出粒度正确性」命门（纯应用层、复用现成 abort-then-destroy 配方、零内核时序依赖）**——比方案 B 的「全局计量时序正确性」（依赖内核 isStreaming 边界、retry 退避漏算）轻得多，**无需活进程打点、hermetic 单测即可**。

**spike 验法**（沿 T1/T3 faux Map「三处闭包同一对象 + 负对照 + 变异检查」纪律）：
- faux：`fauxMap`（唯一计数源）+ `ownerBySession` 反查。放 2 个会话 sidA/sidB 同属 agentX。
- **断言①（误杀根除直接证据）**：`evictSession(root, sidA)` 后 `fauxMap` 只剩 sidB（sidA.destroy 被调、真删 Map）、`sidB.isAlive()` 仍 true。
- **断言②（流式守卫）**：sidA.inner.isStreaming=true → 先 `send({type:'abort'})` 再 destroy（spy 顺序断言）。
- **断言③（owner-map 红线）**：evict 后 `getOwner(cwd, sidA)` 仍返 agentX（只 destroy registry、绝不碰 bySession/removeOwner）。
- **负对照**：`destroyDeletesMap=false`（destroy 只翻 alive 不删 Map）→ `fauxMap.size` 不回落，证「size 回落」断言真依赖 destroy 删 Map、非 vacuous。
- **变异检查**：把 `evictSession` 误写成「逐出该 agent 全部会话」（退化回 by-agentId）→ 断言① 必红（sidB 被误删），证测试能抓住误杀回归。

**go/no-go**：上述五条全绿 = **GO**（开 T2）。命门纯应用层、零内核时序依赖，预期高置信 GO。

---

## 三、确认的风险（评审 + lead 核）

| severity | 风险 | 缓解 |
|---|---|---|
| **minor** | 实现漂移去改 `evict-agent-sessions.ts` 既有 `evictAgentSessions` / 误碰 owner-map | 只**新增** `evictSession`、不动 `evictAgentSessions` 本体；git diff --stat 收口确认 owner-map/`bySession`/`removeOwner` 零改动。 |
| **minor** | catch 路径 sid 可能未赋值（worker 在 setOwner 前抛错） | `evictSession` 内 `getSession(sid)` 对 undefined/null 返 undefined→直接 return（no-op 安全）；orchestrator catch 传 `stage.sessionId`（可能 null）即可。 |
| **minor** | 承重测试 evict 断言散布 3 块、漏改卡死 T2 门禁 | 详设 §1.4 已按 describe 块枚举（承重块 98-261 / T6 块 603-775 / 失败分支 386 起）；T2 grep `evict.*toHaveBeenCalled` 逐一核。 |
| 说明 | 不顺带修 legacy dispatch 占槽（方案 B 才顺带） | legacy 完成态占槽是既有行为、dispatch 默认 60s 超时不死等、不在本轮 bug 范围；非回归，记非目标。 |

> **第二条 evict-by-agentId 不动**（评审揪出）：`app/api/projects/[id]/agents/[agentId]/route.ts:63` 改 mode 时也按 agentId evict，但 gated on `before.mode!==updated.mode`、不在 run 完成误杀因果链上、本轮不动。误杀根除只需收窄 orchestrator 的 :167/:187 两处。

---

## 四、红线合规核对（逐条 holds）

| 红线 | 落点 | 状态 |
|---|---|---|
| 不碰 owner-map（第五轮） | `evictSession` 只 destroy wrapper、复用 abort-then-destroy 配方、`bySession`/`removeOwner` 零碰 | 守住（AC-6 spike 验 getOwner 仍返 agentId） |
| 不改计量/上限 | `concurrency-gate` / `HARD_CAP` / `readMaxConcurrent` / `DEFAULT_MAX` 零改动 | 守住 |
| F16 不复发 | 每阶段仍 evict 释槽（按 sid）、活 worker 恒 ≤1 | AC-2 验 |
| cancel/re-attach 零回归 | 两者不经 evict（cancel 走 signal、re-attach 走 owner-map）；evict 收窄不影响 | AC-4 验 |
| 不改 pi 内核、纯文件无 DB | 只动 `evict-agent-sessions.ts`（加 evictSession）+ `pipeline-orchestrator.ts`（收窄） | 守住 |
| 计槽语义不变 | 「在册≤3」保持、不动 concurrency-gate | 守住（与方案 B 的关键区别） |

---

## 五、待 lead 核 / 待用户拍板

**已用户拍板**：evict-by-sessionId（D-V1.2-50 轮次2，方案 B 经评审 NO-GO 后改选）。

**待 lead 实现期核**：
1. `evictSession` 的 getSession DI 缝形状（对齐 `evictAgentSessions` 现有 `deps?.getSession` 缝）。
2. orchestrator `:187`/`:167` 注入口最小改法（换 `deps.evictSession` 还是复用 `deps.evictAgentSessions` 改传 sid）。
3. 承重测试三块 evict 断言的精确行（T2 grep 核 :211-215/:253/:737/:753/:386 区）。

**承重命门待 T1 坐实**：`evictSession` 只逐目标 sid、不误删同 agent 其他会话、owner-map 零碰（hermetic spike）。
