# V1.2 设计决策记录（ADR）—— lead 实现级取舍

> 与用户拍板 QA（`QA/`）区分：本文件记 **lead 自主的实现级取舍**（不触使命 / 不变量、可推翻）。第一轮起编号 `D-R1-*`。
> （继承自 V1.1 的祖先 ADR 在 `docs/V1.1/设计决策记录.md`，本文件是 V1.2 自身的。）

## 第一轮 · 多 Agent 管理（卡片视觉重构）

- **D-R1-01 · 复用既有纯函数**：头像 / 模型解析复用 `agentColor` / `agentInitial` / `splitModel`（`lib/stores/useAgentStore.ts`），不新造。model 显示用 `splitModel(p.model)?.modelId ?? p.model`（**`?? p.model` 非 `?? '默认'`**，保住无斜杠裸串值）。

- **D-R1-02 · 字段渲染条件**：卡片只读 7 个真实档案字段、零运行态。tools 计数仅 coding 且 >0（doc 模式 tools 被内核忽略、显示会误导）；thinking 仅非 off；role / 计数空则整段隐藏、卡片优雅退化为「头像 + 名 + 模式徽章」。

- **D-R1-03 · 玻璃→Swiss 转向保留 `.glass-card` 类名**（D-V1.2-9 后）：用户拍板转 Swiss 极简，玻璃装饰（极光 / 渐变边 / 品牌环 / 模态环境底）全删，`.glass-card` 基类从「半透明 blur 玻璃」改为「纯色 surface（`var(--bg)` + 细边）」。**保留类名不改**——避免动所有 className 引用；经 grep 实证 `.glass-card` 全仓**仅** AgentManager 卡片 + DispatchPanel 行用，改其样式即全覆盖、无副作用。`.glass-card.agent-card` Swiss 后仅设 `border-radius:10` + hover/focus。

- **D-R1-04 · 不修 build 的 Google Fonts 失败**：`npm run build` 卡在 `app/layout.tsx` 的 `next/font/google` 取 Noto Sans Mono、本环境 `fonts.googleapis.com` 不通（sandbox off 也失败、重试 3 次）。**确认为预存环境限制、非本轮回归**（`layout.tsx` 未碰、失败在字体抓取阶段、早于本轮文件编译）。改 layout 换本地 / 系统字体超本任务范围、**未擅自动**，登记于此。lint + test + 真浏览器三层已验。

- **D-R1-05 · 技术债收敛**：AgentManager 4 处 + DispatchPanel 1 处硬编码「选中态」`rgba(37,99,235,0.x)` → `var(--accent-soft)`（亮暗自适应）；**不动** DispatchPanel `running` 状态徽章色（状态色非选中色、语义不同，误改会让状态徽章跟 accent 漂）。

- **D-R1-06 · 「全局污染真 bug」实为 ultracode 综合夸大**（记教训）：第一轮玻璃设计探索的综合 agent 自称「揪出真 bug——`.glass-card` 全局共享于 file panel/Dispatch/Sidebar、玻璃增强裸挂会污染全站」，并据此定「作用域铁律」。**lead grep 实证该前提为事实夸大**（`.glass-card` 全仓仅 AgentManager 用、无对照物）；另综合给的硬编码 rgba 行号也全错。**教训**：ultracode 综合/reader 会虚构具体事实（路径 / 行号 / 影响面），承重事实须 lead 带 `file:line` 独立复验，勿尽信综合自述。作用域本身无害（未来防护），Swiss 删玻璃后此点 moot。

## 第二轮 · 版本 diff 与历史

> 用户拍板见 `QA/第二轮-版本diff与历史决策.md`（D-V1.2-10~14）。承重 file:line 经 lead 亲读复验（`ArtifactPanel.tsx` / `anchor.ts` / `lcs.ts` / `artifact-service.ts` / `useArtifactStore.ts` / `degrade.ts` / `pending-change-service.ts`）。

- **D-R2-01 · 新建客户端安全版本 diff 聚块函数**：版本对比要在客户端把「两版 content」算成 `DiffBlock[]`，但现成的 `computeReplaceDiffBlocks`（`pending-change-service.ts:129`）在 **server-only 模块**（顶部值导入 `node:crypto/node:fs/node:path`，:1-14）。客户端值导入它会把 `node:fs` 拖进 bundle、Turbopack 全站 500（D-R7B-07 血泪、`lcs.ts:1-11` 记载）。故**新建 `lib/artifact-view/version-diff.ts`**：`computeVersionDiffBlocks(oldContent,newContent):DiffBlock[]`，照搬 `groupOpsToBlocks`（`pending-change-service.ts:94-123`）纯算法逻辑、聚块循环与 `anchor.ts:36-49` 逐字一致，**只从 `lib/domain/lcs.ts` 值导入** `lcsDiff/splitLines`、`DiffBlock` 仅 `import type`；block.id 用**确定性序号**（如 `v{old}-{new}-{idx}`，绝不用 `node:crypto.randomUUID`，id 只供 React key 与 `data-block-id`）。

- **D-R2-02 · 去 ☑️❌ 走现成只读路径、复用形态C，不改 DiffBlockCard 内部**：`DiffBlockCard` 的 ✓/✗ 整段受 `canResolveHere = isFullscreen && !resolved && changeId!=null && resolveBlock` 门控（`ArtifactPanel.tsx:551,595-616`），「已确认/已拒绝」状态标受 `isFullscreen && resolved` 门控（:618-622）。版本对比走「**不传 changeId / resolveBlock / isFullscreen**」即恒只读、无按钮无状态标——`DiffBlocksView`（:646-657，`<DiffBlockCard block={b}/>`）已是此先例（全屏下也不冒标，因 isFullscreen 未传 = undefined）。故**零改 DiffBlockCard**；只需把 `InlineDiffView` 参数化为接受裸 `(oldContent,newContent,diffBlocks)` + 可选 resolve 三件套，或抽只读兄弟组件。视觉**复用形态C混合**（equal=Markdown / change=DiffBlockCard），与受管文档现有行内 diff 一致。

- **D-R2-03 · 降级护栏按「版本 diff 总块数」另判**：现有降级判定 `countPendingBlocks`（`degrade.ts:19-21`）基于 `state==='pending'`。版本对比块非 pending（D-R2-01 给确定性序号、state 取非 pending），该计数恒 0、永不降级。故版本 diff 须**按总块数 > effectiveLimit** 单独判降级到并排 `DiffBlocksView`，复用 `INLINE_HL_LIMIT=25`（`degrade.ts:8`）/ `FULLSCREEN_INLINE_HL_LIMIT=80`（`ArtifactPanel.tsx:21`）阈值常量。

- **D-R2-04 · 取前驱版按 versions[] 排序取前一个、非「版号-1」**：版本号当前连续（`submitVersion`/`rollback` 恒 `currentVersion+1`），但「前驱 = 选中版号-1」是「版号连续」的隐含假设、未显式保证。**改为：versions[] 按 version 升序，取选中版在序列里的前一个元素**作 base，应对未来潜在版本号空洞。v1（序列首元素）无前驱 → 只读全文 + 提示。

- **D-R2-05 · diff 历史 = listVersions 懒算、覆盖 rollback、瞬态标量态**：时间线条目 = `store.versions`（`useArtifactStore.ts:68`，listVersions 已拉回含 content/note/author/createdAt，:209-220），**零新增存储**；点条目才算单条相邻版 diff（**懒算**，`lcsDiff` 是 O(n*m) 全表 DP、勿预算全部版本对）。范围**覆盖 rollback 版本**（note=`rollback to v{n}` 作语义标注）。时间线「当前展开条目」用**标量字段**（如 `expandedHistoryVersion: number|null`），**严禁**新增返回新数组/对象的派生 selector（否则 useShallow 漏用 → 无限重渲染崩、只真浏览器暴露，D-D3-10）。

- **D-R2-06 · diff 历史入口 = 独立 toggle 按钮 + 覆盖正文（甲）**：`[Diff历史]` 是 ArtifactPanel 工具栏一个 toggle 按钮（放版本下拉右侧、常驻可见），**不**塞进 `[行内│查看Diff]` 段（该段在历史/无 pending 时隐藏，会一并藏掉入口）。激活时主体区切成时间线、**覆盖正文**（非浮层，避开 A1 全屏浮层层级/手势冲突）；时间线模式下隐藏 `[行内│查看Diff]` 段；再点按钮或选版本下拉即回正文/版本 diff。

- **D-R2-07 · 推翻 D-D5-4（历史版纯只读、无 Diff）**：D-D5-4（祖先 `docs/V1.1/设计决策记录.md`）原定「看历史版 = 纯只读全文」。本轮（用户拍板 D-V1.2-11）改为「选历史版 = 该版 vs 上一版只读行内 diff」，**保留「回到只读全文」逃生开关**。这是有意识地扩展/推翻既有决策、非回归。

## 第三轮 · TOC diff 与文档性能

> 用户拍板见 `QA/第三轮-TOC-diff与文档性能决策.md`（D-V1.2-15~18）。承重 file:line + 性能 baseline 经 lead 亲读/亲测复核（chrome-devtools 实测）。调研 verdict GO·需修正 4 处，已全部纳入下列 ADR。

- **D-R3-01 · 新建客户端安全 `lib/artifact-view/toc-diff.ts`**：导出 `computeTocDiff(oldContent,newContent): TocDiffItem[]`（`TocDiffItem = TocItem + diffKind:'add'|'del'|'mod'|null + side:'base'|'target'`）。**只值导入 `lib/domain/lcs.ts` 与 `lib/artifact-view/toc.ts`**，绝不值导入 `pending-change-service.ts`/`artifact-service.ts`（含 node:fs，守 D-R7B-07）。纯函数、O(行数) 线性。

- **D-R3-02 · `parseTocWithLines` 不破 `parseToc` 契约**：归属需标题行号，但给原 `parseToc` 加 `line` 字段会打破 `toc.test.ts:28-32/:52` 的 `toEqual` 三字段严格断言。故**新建 `parseTocWithLines`**（或在 toc-diff.ts 内复刻 toc.ts 的 ATX_HEADING/FENCE 扫描），**原 `parseToc` 一字不改**。`parseToc` 行序与 `splitLines` 行序同源（都按 `\n` split），标题行号与 lcsDiff 的 ops 行号互通。

- **D-R3-03 · 章节归属算法（三步线性）**：① base/target 各 `parseTocWithLines` 拿标题行号；② 一趟 `lcsDiff(splitLines(base),splitLines(target))` 遍历累加 i(base行)/j(target行)，标 `baseChangedLines[i]`（del）/`targetChangedLines[j]`（add），equal 两侧未变；③ 每标题章节区间 = 从本标题到**下一个同级或更高级标题**之前；key 在 target 有 base 无→`add`、base 有 target 无→`del`、两侧都有且区间内有改动行→`mod`、否则 `null`。**子章节改动冒泡标父**（核实修正①：这是「按下一同级/更高级标题切区间」的**必然结果**——父区间天然含子章节行，非可选项）。

- **D-R3-04 · 对齐 key + 真同名兜底（核实修正②）**：对齐 key 用「**剥离去重后缀的原文 text + level**」（规避 `toc.ts:52-56` 的 `-1/-2` 后缀因增删错位误判）；但「**真·同名同级**章节」（合法）会撞 key，故对撞 key 的标题用**标题序列顺序配对**兜底（按出现顺序一一对应），不靠纯 key 集合。标题改名（`## 背景`→`## 项目背景`）**接受拆成 del+add 两条**，不做模糊匹配（成本高收益低）。

- **D-R3-05 · 删除章节占位 + 视觉=下方色线（D-V1.2-15/17）**：`computeTocDiff` 产出 **base/target 合并有序序列**（按位置交织），del 章节按 base 位置插入。TocSidebar 渲染：改动章节目录条加 `border-bottom: 2px solid <KIND_STYLE 色>`（add 绿 #4ade80 / mod 黄 #eab308 / del 红 #f87171）+ `marginBottom`/`paddingBottom` 留间距；**无圆点/图标**。del 条目额外：暗色文本（opacity 降低）+ **不可点击**（无 data-slug 落点，onClick no-op / cursor default）。未变章节渲染与现状完全一致（零回归）。

- **D-R3-06 · 性能=仅 memo 化（D-V1.2-18 + lead chrome-devtools baseline）**：**实测 baseline**（大文档 ~1200 行 / 16KB）——滚动流畅（avg 16.5ms、p95 16.7ms、**0 掉帧、0 long task**）；卡顿是**渲染瞬间 ~600ms long task**（只读 1 实例 593ms ≈ 版本 diff 11 实例 596ms，**实例数非放大器、总内容量相同**——调研「N 实例数十倍」热点被实测**推翻**，核实修正④）。故优化=**仅 memo 化**：`Markdown` 组件的 6 个 heading 工厂 + components 对象**提模块级常量**（不依赖 props）、`Markdown` 包 `React.memo`（children 字符串可浅比较）、InlineDiffView 段做 `React.memo` + key 从 index 改**段内容稳定 key**（保 D-R7B-04 对齐不漂移）、DiffBlockCard `React.memo`。目标=消除「内容不变时的冗余重解析」（store refresh / 父重渲染触发的 600ms）。**不虚拟化**（滚动已流畅、收益存疑、撞 TOC/A3 querySelector 跳转）、**不按长度降级**（损形态C体验）。

- **D-R3-07 · 不共享 ops、不碰 D-R7B-04（核实修正③）**：TOC 归属**独立跑一次 lcsDiff** + `useMemo` 缓存即可，**不拆 version-diff 的聚块纯函数、不动 buildLineDiffSegments/computeVersionDiffBlocks/groupOpsToBlocks 三处对齐**。理由：性能瓶颈实测是 react-markdown 渲染、**非 lcsDiff**（O(n*m) 在 1200 行约几十 ms、远小于 600ms 渲染），无需为省一次 lcsDiff 去碰对齐红线。

- **D-R3-08 · T0 baseline profiling 作承重墙先做、性能验收走真浏览器 Performance API**：性能优化前先实测 baseline（已由 lead chrome-devtools 完成）；优化后须真浏览器对比 render long task 下降（量化）。`build`/单测/lint 证明不了掉帧（D-R1-04 build 非 oracle）。**工具取舍（2026-06-22 用户拍板）**：验收默认 **browser-e2e**（脚本断言+截图+Performance API via page.evaluate，省上下文）；chrome-devtools 的 `take_snapshot` 太耗上下文，仅留作交互式调试备用。见 [[next-step-browser-e2e]]。

- **D-R3-09 · 「子改动冒泡父」精确化为「仅已存在子章节的内容改冒泡、整段子章节增删不冒泡」（T1 实现细化）**：D-R3-03 字面「父区间内有改动行即 mod」会让「在 # A 下新增/删除整段 ## B」误把 A 标 mod（A 自身没改），使 per-section 指示失去意义（任意后代被增删→所有祖先 mod）。故 T1 精确化：① **配对存活的子章节内容改** → 冒泡标父 mod（符合 D-R3-03 设计意图，扫父节即知该大节有内容变）；② **整段子章节的增/删**（未配对标题的区间）→ 给其区间打掩码、`sectionHasChange` 跳过这些行 → **不冒泡**（由该子章节自己的 add/del 条目表达）。副带正确：标题改名（del+add 两端都不配对）也不再误标父 mod。实现见 `toc-diff.ts:buildExcludedMask`/`sectionHasChange`（lead 复核批准、记此）。**⚠️ 本条的「冒泡」部分已被 D-R3-10 推翻（见下）。**

- **D-R3-10 · 真浏览器实测后用户拍板：①归属彻底「不冒泡」②视觉改「标记条+类型符号」（取代 D-R3-05 下方色线 + D-R3-09 冒泡）**：T2 真浏览器（browser-e2e）实测大文档（1202 行）后，lead 跑 `computeTocDiff` 核对发现「过度标记」：文档根标题 h1 被标 mod（D-R3-09 仍把存活子章节内容改冒泡到根→根成全文改动总开关）、第 60 章被标 mod（新增的第 61 章前的空行分隔符落进 ch60 区间）。**用户拍板「都不要」（决策 C）**：
  - **归属改为「最内层精确归属、完全不冒泡」**（取代 D-R3-09 的冒泡）：每个改动行只归属到其前**最近的标题**（任意级别）；标题归属区间 = `[本标题行, 紧邻下一标题行)`（到下一个**任意级别**标题，非「同级或更高级」），父区间不再含子章节行。→ 文档根标题不因后代改动而 mod、per-section 最精确（贴合用户「看清改动位置」）。
  - **掩码扩展修「邻章牵连」**：给未配对（纯 add/del 整段章节）区间打掩码时，**额外把该标题行之前紧邻的连续空行/分隔行并入掩码**（从标题往上、连续空白到上一个非空行）→ 新增/删除整章不牵连前一章（修 ch60）。
  - **视觉「乙·标记条+类型符号」**（取代 D-R3-05 的 `border-bottom` 全宽细线，用户经 ui-ux-pro-max 设计建议 + 多轮拍板）：改动章节标题下方一条「**3px + 圆角(radius 2) + 内缩到标题宽度**」的子元素色标记条（非全宽 border，读作「该条目标记」而非分隔线）+ 行首一个**极小单色类型符号**（`+`新/`~`改/`−`删）——符号靠**形状**让色盲也能区分类型（满足 CRITICAL 规则 `color-not-only`，纯色线不达无障碍）。del 仍暗色 opacity 0.55 + 不可点。
  - 实测结果：11 处标记（+×1/~×9/−×1）全为真改动，h1 根 + ch60 不再误标，pageErrors=0。实现见 `toc-diff.ts`（归属/掩码）+ `ArtifactPanel.tsx:TocSidebar`（标记条+符号），commit `42d111e`。

## 第四轮 · 让文档型派发也能产受管文档

> 用户拍板见 `QA/第四轮-派发产受管文档决策.md`（D-V1.2-19/20）。调研 = 工作流 `wf_778dac19`（5 路精读→Option B→3 视角对抗 holds=True）。承重 file:line / 内核类型 / 端到端均经 lead 亲验（T1 spike 复跑 + T6 真浏览器）。commits：T2 `3d1077b` / T4 `dc0b4a6` / T5 `3c3d4c8` / 收尾 chore `209d1d3`。

- **D-R4-01 · 接入点镜像交互式 wiring 的 mode 分流（合并原 T2/T3）**：`dispatch-runner.ts:runWorker` 增 `projectId` 入参，在 `assembleProfileSessionOptions`(:108) 与 `createAgentSession`(:130) 之间插 `docOptions = profile.mode==='coding' ? undefined : assembleDispatchDocSessionOptions({projectId,sourceActor:profile.name,cwd}).options` + `codingToolsFallback`（coding 且 tools 空→`CODING_TOOL_NAMES`）。**spread 顺序 `options → docOptions → codingToolsFallback → createOptionsOverride` 不可调**（D-V2-04，与 `profile-session-wiring.ts:165-170` 同序；docOptions 的受限白名单须排在 `options`(含 profile.tools) 之后覆盖之，否则 write/edit/bash 泄漏）。`orchestrator.ts` 调用点传 `projectId: task.projectId`。机制零改、内核零 fork。

- **D-R4-02 · dispatch 专用受限集禁 propose_edit（headless 无确认者）+ buildDocTools 本体零改**：新增 `DISPATCH_DOC_SESSION_TOOLS`（6 项，去 propose_edit）+ `assembleDispatchDocSessionOptions`（doc-session.ts，不动既有 `DOC_SESSION_TOOLS`/`assembleDocSessionOptions`）。新增 `buildDispatchDocTools`（doc-tools.ts）走「新函数内联复用」——把 create/propose/list 三个 `defineTool` 抽成私有工厂 `makeCreateArtifactTool`/`makeProposeEditTool`/`makeListArtifactsTool` + `resolveBackends`，**`buildDocTools` 签名/返回/顺序 `[create,propose,list]` 零改**（守 `doc-tools.test.ts`/`doc-session.test.ts` 既有 16 断言）。理由：fire-and-forget 无人按块确认，propose_edit 落死悬 PendingChange、且经「已有待确认则拒绝」闸门阻塞该文档此后所有提议（含交互式）。

- **D-R4-03 · artifactId 抽取走 `agent_end.messages`（T1 spike 实证、lead 复跑）**：新增纯函数 `extractCreatedArtifactIds(messages)`——`m.role==='toolResult' && m.toolName==='create_artifact'` → `m.content` 里 `type==='text'` 的 text → `JSON.parse` → `.id`，按序收集、parse 失败/无 id 跳过。**字段路径经 lead 复核内核类型 `@earendil-works/pi-ai/dist/types.d.ts:211-218`（`ToolResultMessage{role:"toolResult";toolName;content:(TextContent|ImageContent)[]}`）+ T1 spike 真跑实证**（messages 抽出 id 与 `listArtifacts`/`turn_end.toolResults` 三者一致）。**默认 messages 抽取**（精确、零额外 I/O）、`list_artifacts` 侧效作兜底（备案）。⚠️ 调研设计原稿曾猜错字段（`role:'tool'`/`.name`）、被对抗核实的集成视角读内核源码纠正——再次印证 ultracode 综合的具体字段/行号须 lead 复验（同 D-R1-06）。

- **D-R4-04 · createdContent 由 runWorker 回读、保 orchestrator 领域纯度**：喂下游需上游受管文档「正文」（assistant 末条文本此时可能只是「已创建文档」回执）。lead 定 = **runWorker 回读** `new ArtifactService(new ProjectRegistry()).readCurrentContent(projectId, lastArtifactId)`（默认后端与提议工具同源 ~/.pi）填 `WorkerResult.createdContent`，**try/catch 兜底**（回读失败静默退回 undefined、下游用 output，不因回读异常炸 worker——队员加固、lead 认可）。**不让 orchestrator 直依赖 ArtifactService**（保领域编排层纯度、测试注入面不变）。

- **D-R4-05 · 产物对账（WorkerResult / orchestrator / Assignment）**：`WorkerResult` 增 `artifactIds:string[]`（**必填**、恒返回 [])+`createdContent?:string`。`orchestrator.ts`：判空放宽为 `!output.trim() && artifactIds.length===0` 才 failTask（doc worker 可能只 create_artifact、文本为空）；回填 `assignment.artifactId = artifactIds.at(-1)`；喂下游 `upstreamOutput = lastArtifactId ? (createdContent ?? output) : output`；**`writeArtifact` 文本保底仍写**（兼容 coding/纯文本 worker，受管文档才是权威产物）。`dispatch-store.ts:Assignment` 增可选 `artifactId?`（无迁移）。`orchestrator.test` 既有 3 处 fakeRun 桩补 `artifactIds:[]`（必填后）。

- **D-R4-06 · 展示层 by-id + 终态刷新受管分组（守客户端 bundle 红线）**：`DispatchPanel` 产物按钮 `isArtifact ? onOpenArtifact(artifactId) : openOutput(output)`、渲染条件放宽 `output||artifactId`、有 artifactId 时只露「受管文档」入口；新增**终态 useEffect**（`notifiedTaskRef` 防重、每任务触发一次）在产出过 artifactId 时调 `onArtifactsChanged`。`AppShell` 挂载传 `onOpenArtifact={handleOpenArtifact}`（现成、by-id 开 ArtifactPanel）+ `onArtifactsChanged={bump explorerRefreshKey}`（FileExplorer 受管分组重取）。**`"use client"` 组件零新增 value-import**（artifactId 只是轮询 JSON 字符串，不引含 node:fs 的服务端模块，守 D-R7B-07）——经 T6 真浏览器 console pageErrors=0 验证。

- **D-R4-07 · 收尾 chore 与功能解耦**（`209d1d3`）：顺手清 2 个**既有** tsc 错误（`session-grouping.test.ts`/`useAgentStore.test.ts` 的 AgentProfile fixture 漏 round-6 起 required 的 `mode` → 补 `mode:"doc"`）+ `doctor-checks.test.ts`「已安装的内核包加载成功」加 `{timeout:15000}`（动态 import 内核包，本环境冷载+全量并发 collect 偶发 >5s 超时 flake）。**单独 commit、不混功能卡**（surgical），与本轮功能无关、用户授权 T6 收尾顺手补。修后 tsc 0 错误、vitest 417 全绿（无 flake）。

- **D-R4-08 · 验收纪律与「idle 不可信」教训**：承重墙 T1 spike **lead 亲自复跑探针**（不认队员自报告）；每卡 lead 读真实 diff + 复跑 lint/test/tsc 再 commit；T6 端到端 **lead 亲自走真浏览器**（chrome-devtools-mcp）+ 亲读截图，**不认队员自跑 E2E**。**教训**：ns-impl 多次发「idle/已完成」但 `git status` 显示只改了部分文件——**队员 idle 通知不可靠、messages 有滞后错位，进度一律以 git 实盘为准**（见 [[next-step-agent-team-recovery]]）；后查实那些是「写到一半的快照」、最终整卡都完成、git+门禁为唯一真值。T6 真浏览器实测：CS2 项目派发 2 doc worker → 项目根 2 份真实 .md + `.pi/artifacts/managed/<id>/` 侧车 + 各 assignment 回填 artifactId + 进度弹窗「受管文档」按钮 by-id 开 ArtifactPanel + file panel 受管分组自动刷新出 2 份 + console pageErrors=0。

## 第五轮 · 会话分组与主会话

> 用户拍板见 `QA/第五轮-会话分组与主会话决策.md`（D-V1.2-21~23）。调研 = ultracode 已调查 + 三视角对抗 holds=True + lead 复核根因。承重 file:line / 内核类型 / 端到端均经 lead 亲验（真浏览器统一验收）。commits：T1 `bae6a95` / T2 `c44e282` / T3 `b06e447` / 收尾 `5bb4641`（T4 无代码，分支 v1.2）。

- **D-R5-01 · T1 setOwner 经 OrchestratorDeps 可注入钩子**：`OrchestratorDeps` 增可选 `setOwner?`，`runDispatch` 内 `doSetOwner = deps.setOwner ?? setOwner`（生产默认真 `setOwner` / 测试注入 spy）。保持 orchestrator faux 可单测、不引内核依赖、不真写盘。归属落盘点 `orchestrator.ts:152`（取到 `assignment.sessionId` 之后、早于 timeout/aborted 判断）无条件 `doSetOwner(projectRoot, result.sessionId, assignment.agentId)`，completed / timeout / aborted 三类都写（D-V1.2-23），三类前置失败（档案不存在 / acquireSlot 抛错 / runWorker 抛错）在 :152 前 return 不写。对照范式 `app/api/projects/[id]/agents/[agentId]/session/route.ts:45`。+6 spy 用例。

- **D-R5-02 · T2 必须新增独立 onSessionsChanged、绝不复用 onArtifactsChanged**：既有 `onArtifactsChanged` 被 `if(!producedArtifact)return` 闸门挡，**仅产受管文档才 fire**；coding / 纯文本 worker（artifactId 恒空、占绝大多数 dispatch）跑完不触发它 → 分组当场不显（M7·5.3 类 FAIL 复发）。故 DispatchPanel 新增**独立** useEffect（独立 ref `notifiedSessionsTaskRef` 按 taskId 去重、仅终态触发一次、**不被 producedArtifact 门控**），调独立回调 `onSessionsChanged`，与 `onArtifactsChanged` 并存、各刷各的。AppShell `handleDispatchSessionsChanged` 复用有界重试 `tick`（≤8×700ms、`before` = bySession 键数为基准、键数 `<= before` 继续 tick、每次并 `setRefreshKey` 重拉 `/api/sessions`）。

- **D-R5-03 · T3 wait-gate（map 异步就绪前不过早设默认 cwd）**：`mainSessionId` 恢复要读 map；map 由 AppShell 进项目时异步 refresh，未到位时 `selectMapForProject` 回退空 map（mainSessionId=null）。若不等，会先错过主会话恢复、误落默认 cwd 设了 `selectedCwd`、再被恢复 effect 阶段②顶部 `selectedCwd!==null` 永久挡住 → 主会话再也恢复不了。修法 = 无 URL 会话且 `loadedProjectId!==currentProjectId` 时先 `return` 等待（`map` 在 deps、refresh 本地文件读 ms 级后 effect 重跑放行）；`currentProjectId` 为空（无项目 / 纯深链）不涉主会话、不等；URL 深链恢复完全不依赖 map。残留风险（session-map GET 持续失败则 gate 一直挡默认 cwd）评估为可接受：本地文件读几乎不失败、且该失败下分组 useMemo 本就降级为空（已有行为），未引入新失败模式。

- **D-R5-04 · T3 下沉 SessionSidebar 现有恢复 effect、不在 AppShell 另开 restore 路径**：恢复逻辑重写为两阶段（① restoredRef 守护的会话恢复：`target = pickSessionToRestoreOnEnter(...)`，非空且列表就绪→find→恢复 / 找不到=主会话已删→降级默认 cwd（仅 URL 深链调 onInitialRestoreDone）；② 默认 cwd），全部落在 SessionSidebar 既有恢复 effect 内。**不在 AppShell 另起 restore 分支**，避免双路径竞态。`currentProjectId`+`currentRoot`+`map/loadedProjectId` 三行订阅上移到 effect 前（下方 grouped useMemo / JSX 仍引用、无重复声明）。纯函数 `pickSessionToRestoreOnEnter(map, urlSessionId)`（lib/main-session.ts，URL 优先 / 否则 mainSessionId / 皆空 null）签名取 `urlSessionId: string | null | undefined`——Props `initialSessionId?` 含 undefined、调用点直传不 coerce，须 undefined 入参才过 tsc。

- **D-R5-05 · T4 天然成立、不写代码**：首次进新项目 = 主对话新建态，由既有 `effectiveNewSessionCwd` 链处理（T3 无主会话→`setSelectedCwd(currentRoot)`→`onCwdChange`→AppShell `setActiveCwd`→`effectiveNewSessionCwd=activeCwd`（因 `selectedSession===null`）→ChatWindow newSession 模式）。守 D-V1.1-09（临时 id 懒落盘、不调 startProfileSession / 后端预建、不占并发槽）。**不写代码**、避免过度设计；真浏览器确认天然成立。

- **D-R5-06 · re-attach 6→7 工具集非对称、文档化接受（机制零改）**：doc-mode dispatch worker headless 跑时用 `DISPATCH_DOC_SESSION_TOOLS`（6 项、无 propose_edit，第四轮成果）；但 T1 后该 worker 会话进 `bySession`，idle re-attach 续聊时经 `resolveOrReattachSession`→`getOwner` 命中→`reattachProfileSession` 按 `profile.mode` 重建得 `DOC_SESSION_TOOLS`（7 项、含 propose_edit）。属**续聊语义**（人类接手该会话继续编辑、需 propose_edit）、与 headless 一次性 fire-and-forget（无人按块确认、禁 propose_edit）语境不同，故非对称**可接受**、本轮不改（机制零改）。红线核对（lead file:line 已核）：`doc-session.ts` DOC_SESSION_TOOLS(7,含 propose_edit) vs DISPATCH_DOC_SESSION_TOOLS(6,无)；`profile-session-wiring.ts:reattachProfileSession` 按 profile.mode 分流（coding→codingToolsFallback 含 bash / doc→DOC_SESSION_TOOLS）；`session-reattach.ts:resolveOrReattachSession` 用 getOwner 反查（T1 让 dispatch worker 进 bySession 后 getOwner 不再返 null→走 profile re-attach，之前 generic）。

- **D-R5-07 · 分组验收 testid（不影响渲染 / 逻辑）**：收尾 commit `5bb4641` 给 SessionSidebar `AgentSessionSection` 加 `data-testid="agent-session-group"`（+ `data-agent-name` / `data-count`），供真浏览器精确断言「按 agent 分组 + 计数」用，不影响渲染 / 逻辑。同 commit 订正 `session-agent-map.ts:sessionsForAgent` 注释。真浏览器统一验收 PASS（lead 亲看截图）：Bug1 coding 派发 done 不手动刷新 ~7s 后自动出 2 agent 分组（编码甲 / 编码乙各 count=1）、bySession 2 条、不堆「其它会话」；Bug2 发首条成主会话→再进自动恢复（主对话区高亮 + 完整对话、url 无 ?session=）；删主会话→再进新建态不崩；空项目进入=新建态可输入；console pageErrors=0（仅 403 临时项目 /api/files + 会话切换竞态 fetch 失败，已知无关）。

## 第六轮 · 版本治理与上游对齐

> 用户拍板见 `QA/第六轮-版本治理与上游对齐决策.md`（D-V1.2-24~27）。本轮由 ultracode 调研（版本机制 + pi-web/内核上游 delta + skeptic 复核）+ **M4 承重 spike（7-agent 对抗式）** 驱动；承重事实（上游版本归属、零扩展实况、首页显示）均经 lead `npm pack` / file:line / 真浏览器复验。commits：M1 `e204c9f` / M2 `cf82425` / M3 `7fb6fdb` / M4-M5 TODO `d4be136` / docs `5365e05` / 版本订正→1.2.6（+ tag v1.2.1~v1.2.6）（分支 v1.2）。属轻量 chore 轮（M4 承重已 NO-GO），不出三件套 spec。

- **D-R6-01 · 版本号方案 B 落地：追溯打 tag、不改 release 脚本、不碰 next.config**：D-V1.2-24 选方案 B（`1.2.<已收官轮次>`）。lead 定实现：① package.json version `0.1.0→1.2.6`（**本轮 = V1.2 第六轮**，故 1.2.6；单一来源，next.config 构建期读、首页两处显示零改）；② 历史 5 轮收官点**追溯打标注 tag** `v1.2.1@149322e`/`v1.2.2@a23fa6b`/`v1.2.3@8e99306`/`v1.2.4@979c8e6`/`v1.2.5@31d7b73`（第一~五轮收官，package.json 当时仍 0.1.0、message 注明「追溯标记」、tag 仅作轮次溯源锚不伪装其当时版本），`v1.2.6@<本轮最终收官 commit>`（= package.json 实为 1.2.6 处、第六轮收官；含版本订正 `ae80f7b` + code-review 修订 `fd49865`）；③ **不改 release 脚本**（`npm version patch --no-git-tag-version` 几乎不真跑、会顺带 publish），改为约定「每轮收官手动 bump + 打 tag」；④ 不引 CHANGELOG（轮次 docs 已是变更记录）。**⚠️ 订正记录（用户复核）**：初次实现误把 version 设 `1.2.5`、并把 `v1.2.5` 打在本轮 bump commit `e204c9f`（= 把编号当成了上一轮·第五轮）；用户指出「本轮应是第六轮」→ fix-forward 订正：version→`1.2.6`、`v1.2.5` 改指第五轮真收官 `31d7b73`、`v1.2.6` 指本轮订正 commit（不重写 e204c9f 等已被 docs 引用的 hash）。

- **D-R6-02 · 基线记录 = package.json.upstream（机器锚）+ app 根 UPSTREAM.md（叙事）**：D-V1.2-25 组合方案。`package.json` 顶层加 `upstream`（forkedFrom/forkBaseVersion 0.6.16/forkCommit f923751/kernel{declared,trackingTarget}/lastComparedUpstream）——npm 忽略未知顶层字段、零副作用、不删改现有键；是将来比对 pi-web 的前提（node_modules 无 pi-web 包）。`UPSTREAM.md` 落 **`next-step-V1.2/` app 根**（非仓库总根）——V1.2 是自带 git 的自包含 app、接手者在此找；放 fork 三层关系 + 公开仓库地址 + 「如何对照上游」操作手册（npm view/gh compare/**npm pack**）+ 锁 minor 语义 + 本次对照结论。顶部注明「权威版本号以 package.json.upstream 为准」。

- **D-R6-03 · M4（rpc 扩展工具过滤修复）裁决 NO-GO（provable no-op），仅留 TODO + 红线约束**：M4 spike（测绘×3 + 红线泄漏/no-op/内核API 怀疑者×3 + 裁决）+ lead 亲验 → **本环境零扩展安装**（`node_modules/@earendil-works/` 仅 2 包、无 `~/.pi/agent/packages`、源码零 `additionalExtensionPaths`）→ 上游修法 `setActiveToolsByName([...toolNames, ...扩展工具名])` 中扩展名集恒空、套用等价现状，是 **provable no-op**，违「不做无效改动」→ NO-GO。**红线安全**（doc 会话物理不流经 `startRpcSessionInner`：初次/​re-attach 走 `profile-session-wiring.ts` 的 `createAgentSession`；退化分支 `session-reattach.ts:129` `startGeneric(filePath,cwd)` 只传 2 参→toolNames=undefined→修复行 `if (toolNames && toolNames.length > 0)` 守卫不可达）。`rpc-manager.ts:351` 上方留 TODO（指上游 0.6.18、no-op 原因、红线「改动只能加非空分支、绝不给 doc 会话加扩展工具」）。**纠正调研报告硬错**：修复实为上游 **0.6.18**（非报告所称 0.6.17）——lead `npm pack` 0.6.16/17/18 实测构建产物 `setActiveToolsByName(g)`（pre-fix）→`setActiveToolsByName(h(o,g…))`（fix）首现于 0.6.18，0.6.17 与 0.6.16 字节相同。

- **D-R6-04 · M5（markdown 安全渲染）跳过、留 TODO**：lead 读真实上游 `lib/markdown.ts@v0.6.18`（仓库 contents API），确认其本质 = `rehype-raw`+`rehype-sanitize`「**渲染原始 HTML**（净化）」，是**可选功能非 bug 修复**：我方 `MarkdownBody` 用 remark-gfm 已正常渲染 markdown 表格、默认不渲染原始 HTML（react-markdown 安全默认）；引入 = 加 2 依赖 + 新攻击面 + `rehype-sanitize` 跑在 KaTeX/mermaid 链上的回归风险。用户拍「先记 TODO 待议」（D-V1.2-27 轮次2）。`MarkdownBody.tsx` 加 JSX TODO（指上游 0.6.17、性质、回归风险、将来真有「文档渲原始 HTML」需求再立项 + 须真浏览器验 KaTeX/mermaid/表格零回归）。

- **D-R6-05 · M3 内核升 0.79.10 = 仅 npm update（不改 spec）+ 门禁实测兜底**：`^0.79.0` 0.x 锁 minor 已允许 0.79.10（仅 0.80.x 超范围）→ `npm update` 两内核包到 0.79.10、**package.json spec 不变**（D-V1.2-27），只移 package-lock。安全性不靠调研报告的「20 符号兼容」声明、靠**门禁实测兜底**：`tsc --noEmit` 0 错误（封装层 lib/pi/** 用到的内核 API 在 0.79.10 全兼容）+ vitest 426 passed + eslint clean。0.80.x 暂不升（pi-ai 入口重组破坏性变更、连上游 pi-web 0.6.18 都仍钉 ^0.79.10）。

- **D-R6-06 · 验收纪律：ultracode 对抗式 + lead 复验戳破单 agent over-claim；首页走真浏览器**：本轮最大教训——首个调研报告（单 agent fan-out）把**一个 no-op（rpc）+ 一个可选功能（markdown）** 都包装成「已验证有用的修复」、还把 rpc 修复版本误归 0.6.17。是用户坚持「用 ultracode 调研」→ 改 M4 spike 为 7-agent 对抗式（怀疑者默认 NO-GO + npm pack 负对照 + 零扩展实况核实）+ lead `npm pack`/file:line 亲验才戳破。**承重事实必须对抗式核实 + lead 亲验、不尽信单 agent 报告**。首页版本显示属 UI、走真浏览器验收：chrome-devtools-mcp 实读 DOM `web v1.2.6`/`pi v0.79.10` + 标题点击 scramble `1.2.6p0.79.10`，lead 亲看截图（存仓库根 `第六轮验收-首页版本号.png`、未入 git）。dev server 用 run_in_background 持久化（detached `(&)` 子壳会被回收）、验完按 PID/task 杀（旧僵尸 30141 用 next「already running」提示拿到 PID 23649 杀掉）。

## 第七轮 · 流水线与阶段看板

> 用户拍板见 `QA/第七轮-流水线与阶段看板决策.md`（D-V1.2-28~47）。承重墙 T1 由 ultracode 10-agent 对抗式去风险（GO_WITH_MITIGATION）驱动、agent team 实现、lead 独立复跑 + 变异 sanity 验收。commits：T1 spike `f430d95`（分支 v1.2）。

- **D-R7-01 · T1 承重墙双命门 spike 确定性 GO（hermetic 探针、T8 已删）**：开工第一步 T1 用两个 hermetic spike 单测各证一条承重命门，lead 独立复跑（不认 impl 自跑）。**T8 已删两探针**（`pipeline-evict-release.spike.test.ts` + `pipeline-reattach.spike.test.ts`），命门事实保留在本 ADR + T3/T6 承重契约（runPipeline 每阶段 evict 释槽 + reattach 装回 doc 工具，已由 `pipeline-orchestrator.test.ts` 承重 verify 覆盖）。
  - **裁决 GO**：`pipeline-evict-release.spike.test.ts`（T1-A·4 测）+ `pipeline-reattach.spike.test.ts`（T1-B·5 测）全绿；全量 vitest 435/435、tsc 0、eslint 0、纯新增不改任何承重源码。
  - **去风险揪出的两个「假 GO 制造机」（写 spike 前内化进断言）**：① faux 计数 Map 与 acquireSlot 读的 Map、evict 的 getSession、faux.destroy 删除三处闭包**必须同一词法对象**；② faux worker 的 `destroy()` **必须真 `fauxMap.delete(sid)`**——因生产 acquireSlot 数 `Map.size`（`concurrency-gate.ts:20`）非 isAlive()，真 `wrapper.destroy()`（`rpc-manager.ts:230-236`）也不删 registry、删只来自 `onDestroy→registry.delete`（`:290`）；直接复用 `evict-agent-sessions.test.ts:16-19` 现成 fakeWrapper（只翻 alive）会致 size 永不降 = 假绿。**变异检查坐实**：把 destroy 改成不删 Map → A1/A4 的 `size.toBe(0)` FAIL。
  - **负对照（确定性区分判据）**：T1-A·N1 不 evict→`acquireSlot({timeoutMs:30})` 抛 /上限/（vs 正向 `{timeoutMs:Infinity}` 立即放行）；T1-B·B4 `removeOwner` 后→走 generic（vs B3 owner 在→走 reattach）。
  - **T1-B 两半勿混**：路由分流（哪个分支 spy 被调）与工具集装配（真 `reattachProfileSession`→`getActiveToolNames()` 恰 = 7 项含 propose_edit）分属两文件、各写一组——spy 不进 `createAgentSession` 读不到工具名。
  - **T3/T6 承重契约（实现须照此）**：runPipeline 每阶段 `acquireSlot({timeoutMs:Infinity})`→`runWorker`→`setOwner`（在 evict 前）→**每阶段含 completed 调 `evictAgentSessions`（只 destroy、不碰 owner map，红线 D-V1.2-41）**；`{timeoutMs:Infinity}` 零改 acquireSlot 本体（`deadline=now+Infinity`、while 天然不进等待循环）。T6「进入完整对话」对 evict 销毁过 + setOwner 写过归属的 doc worker 会话经 `resolveOrReattachSession` 走 reattach、装回 7 项含 propose_edit，**仅复用、勿改** `session-reattach.ts`/`reattachProfileSession`/`doc-session.ts`。
  - **AC-12 无须降级**：T1-B 绿 → 「进入完整对话」解冻续聊保留为 MVP（D-V1.2-42），不退化只读快览。
  - **探针去留**：两 spike 是开工门禁、不进 T3/T6 长期测试集，**T8 已删**（命门事实已迁入 progress.md「T3/T6 承重契约」+ 本 ADR；删后全量 vitest 505→496）。路径勘误：`evictAgentSessions` 在 `lib/pi/evict-agent-sessions.ts`（详细设计部分处误写 `lib/domain/`）。

- **D-R7-02 · T2 数据层落地取舍（ultracode 8-agent 调查 READY，揪 5 真坑）**：pipeline-store + pipeline-run-store + 蓝图 2 路由 + 校验，骨架逐字仿 dispatch-store。lead 亲读两 store diff + 独立复跑 vitest 40/40 + 全量 475/475 + tsc/eslint 0（commit `23f2460`）。调查揪出的 5 个易翻车点已内化：
  - **① listRuns/list/findX 仿 `listArtifacts`（per-file try/catch 跳坏文件）绝不仿 `listVersions`**：后者 `readVersion`（artifact-service.ts:396-402）对坏 JSON 抛 ArtifactError、无 per-file try/catch，照搬会让一个半截写入的 .json 崩掉整列表（违 AC「容错跳坏文件」）。任务卡原写「仿 listVersions」是错引。单测专设「跳坏文件」用例守此（写 `{ not json` + 正常项 → list 返 length 1 不抛）。
  - **② atomicWrite 内置 mkdir**：dispatch 的 atomicWrite（dispatch-store.ts:134-138）裸 tmp/rename、mkdir 只在 create；但 run-store.create 入参已是完整 run、无「create 必先 write」保证，全新项目首跑 `.pi/factory/runs` 不存在会 ENOENT。改仿 session-agent-map.writeMap（:50-57）在 atomicWrite 内 `if(!existsSync(dir))mkdirSync(recursive)`。
  - **③ reconcileOrphan 是有写盘副作用的实例方法**（命中孤儿 `this.write`），与纯函数 pruneMissing 本质不同；任务卡「纯函数式」措辞误导。`liveSessionIds:Set<string>` 由参数注入，领域层**不出现真 `globalThis.__piSessions`**（仅注释提及，守 D-R7B-07）；`cur` 用 `run.stages[idx]` 取后必 `cur?.sessionId` 兜越界。单测五分支（done 早返 / alive 不动 / dead→failed+文案+磁盘回写复读 / sessionId=null 不判 / index 越界 not.toThrow）。
  - **④ order 校验**（dispatch 无此概念）：`Number.isInteger` 守卫 + 排序后逐位 `sorted[i].order===i+1`（一次覆盖重复/缺号/含0/负/小数/乱序）+ `N>=1` 拒 0 阶段（区别 dispatch 2-3）+ **补 name 非空**（数据模型有、任务卡校验清单漏）；通过后 sort 归一升序落盘。create/update **共用同一私有 validateAndNormalize**。
  - **⑤ findRun 返回 `{projectId, run}`**（与 dispatch findTask 返回裸 task 不同）；findBlueprint 返回 `{projectId, blueprint}`。
  - **ADR 定值**：listRuns `RECENT_N=20` / 磁盘保留 `KEEP_M=50`（N<=M）；pruneOld **只在 create 触发**（listRuns 纯读）、**只删终态(done/failed) run**（绝不删 running/pending checkpoint）、per-project 全局（不按 pipelineId 维度，避免误删其它蓝图历史）。
  - **领域错误新建 `PipelineError`**（code NOT_FOUND|INVALID）两 store 共用（pipeline-store export、run-store import）；不复用 DispatchError——`lib/api/errors.ts` 的 domainErrorResponse 按 `code` 字段**鸭子类型**映射（NOT_FOUND→404 / INVALID→422）、不看类名，故新建与复用映射逐字节相同，但「每域一错误类」是既有约定。API 路由 catch 里 `return domainErrorResponse(error)`、不手写状态码。
  - **类型 export 落点**：PipelineBlueprint/StageSpec 在 pipeline-store.ts、PipelineRun/RunStage 在 pipeline-run-store.ts，各文件内 export；client（T4 usePipelineStore）用 `import type` 跨含 node:fs 的领域文件**安全**（tsconfig isolatedModules:true 编译期擦除 type-only import，useDispatchStore.ts:6/8 既有约定），不单开 types 文件。`DispatchStatus` 用 `import type` 从 dispatch-store 复用、不重定义。
  - **不引乐观锁**：PipelineBlueprint 无 version 字段、单进程单用户，update 用 PUT 整体替换语义、容忍 read-modify-write TOCTOU（同 dispatch）。
  - **T2 边界**：run 的 API（runs POST/GET、pipeline-runs/[runId] 读时对账 GET、cancel）属 T3/T6；T2 只交付 run-store 类（含 reconcileOrphan/listRuns 纯方法+单测供 T3 调）+ 蓝图 2 路由。reconcileOrphan 的实际调用（注入 `new Set(globalThis.__piSessions?.keys()??[])`）在 T3 GET 路由。API 路由真实 HTTP 行为按项目惯例（API 不在 vitest include）留 T8 真浏览器 e2e 验。

- **D-R7-03 · T3 承重·编排器 runPipeline 落地取舍（ultracode 8-agent 承重调查 READY，揪头号 gap）**：runPipeline（仿 runDispatch）+ 起 run/列 runs/读时对账 3 路由 + runControllers。lead **独立重跑承重 verify**（真实 runPipeline + faux Map）+ file:line 复核冻结释槽。commit T3 `636133e`。
  - **★头号修正（承重）·runWorker 漏 try/catch**：详细设计 §3.3 伪代码的 `doRunWorker` 没包 try/catch（模板 `orchestrator.ts:131-154` 有）。若 runWorker throw → fire-and-forget 的 `.catch(()=>{})` 静默吞 + run 永卡 running + 会话可能已建未 evict 漏槽（破 AC-2）。**修法**：`try{ result=await doRunWorker(...) }catch(e){ try{await doEvict(projectRoot,stage.agentId)}catch{} return failRun(...,'worker 执行失败') }`——catch 内 **best-effort doEvict 兜底释槽**（runPipeline 独有；evict 对无会话天然 no-op、对已建会话是唯一释槽手段；吞 evict 错不盖原始 reason）。残窗（runWorker 内已 register 但未 setOwner → bySession 无该 sid → evict-by-agentId 反查不到）靠 GET reconcileOrphan + rpc 10min idle 兜底，低概率可接受、不引额外机制（守红线·勿过度设计）。
  - **evict 时机（承重契约）**：`doEvict(projectRoot, stage.agentId)` 在 **setOwner 之后、result.reason 判定之前**——completed/timeout/aborted/判空**四类 return 前都已 evict**（单测 4 条失败分支各断言 evictCalls===1）。承重 verify ④用顺序 log `["acq0","set0","evict0",...]` 钉死 setOwner 在 evict 前（T1-A 契约）。
  - **累积喂下游 cache 按 stage.order**（非下标 i）：进程内 `Map<number,string>`、`cache.set(stage.order, createdContent??output)`、下游拼**所有** order<本阶段 且 done 的上游（AC-6，非只一跳）。dev 重启 cache 丢→MVP 不续跑（读时对账只判 failed）、可接受。
  - **runControllers 落点 = `lib/pi/run-controllers.ts` 挂 globalThis**（declare global var `__piRunControllers` + 惰性 getter，仿 rpc-manager.ts:243-257）：dev Turbopack 热重载重求值模块顶层、模块级 `new Map()` 会让 POST set 的 controller 丢→T6 cancel 取不到。T3 只 setRunController；get/delete 留 T6。
  - **acquireSlot 不接 signal**（留 T6/T5）：T3 只传 `{timeoutMs:Infinity}`、acquireSlot 本体不动（T3.md:14 定）。已知：排队中（acquireSlot 轮询、未起 worker）的阶段在 T3 阶段 cancel 不能中断（signal 翻转后 acquireSlot 不看 signal），须等槽空放行到循环顶 signal 检测才 failRun；冻结模型下活会话恒≤1、槽极少长期满，窗口小、可接受。`current.cancelRequested` 顶检测在活进程内恒 false（内存副本不回读盘）、真 cancel 靠 signal、T6 接通——保留写法 + 注释钉死语义。
  - **预检失败 422**：POST 起 run 时 `profileStore.get` 抛 NOT_FOUND→catch 重抛 `PipelineError("INVALID",`Agent 不存在: ${id}`)`→422（蓝图引用已删 agent = 当前不可执行的实体，非 URL 资源不存在；T3.md:26 + §3.9 定）。预检在 `runStore.create` **之前**（任一缺即 422、盘上零 run、controller 不注册、runPipeline 不启动，不留半个 run）。
  - **不加外层兜底总壳**（匹配 runDispatch；runStore.write/doSetOwner 等同步副作用抛错由读时对账 + 路由 `.catch` 兜底，避免过度设计）。failRun 只落 `run.failedReason`+`run.finishedAt`（PipelineRunStage 无 output 字段）。
  - **红线守住**：orchestrator 服务端领域层、**不 import node:fs、不写 .pi/artifacts**（worker create_artifact 已物化、artifactId 即权威产物），不被 client value-import；headless worker 复用既有 `DISPATCH_DOC_SESSION_TOOLS`（6 项无 propose_edit）装配、编排器白嫖、不新增放行；evict 只 destroy 不碰 owner map；读时对账 `globalThis.__piSessions` 只在路由层读、领域层不碰；不改内核。
  - **承重 verify（lead 独立重跑，非 impl 自证）**：`pipeline-orchestrator.test.ts` 用真实 runPipeline + faux 三件读写同一 fauxMap（继承 spike「同一 Map + destroy 真删」纪律），6 断言全绿：①evict 每阶段一次(agentId 顺序) ②结束 size===0 ③peak≤1 ④顺序 log ⑤负对照(destroy 不删 Map→第2阶段 acquireSlot 超时) ⑥catch 路径释槽(runWorker throw→evict+size 回落+后续不跑)。vitest 15/15 + 全量 490/490、tsc/eslint 0、零回归。AC-4 多 run 并发为文档论证（冻结后活会话≤1、acquireSlot 限流轮转、不饿死）、单测不强证。

- **D-R7-04 · T4 看板族 UI 落地 + 补「一键起 run」触发疏漏（ultracode 7-agent 调查 GO，对抗式把关揪 1 blocker + 5 修正）**：T4 11 文件（dot-matrix / avatar / status-meta / usePipelineStore + 6 组件 + AppShell 接线 + dicebear@9.4.2），视觉严格照 `看板视觉定稿-kimi-v11.html` 的 Kimi(A) 块、亮/暗双主题。lead 亲读全 11 文件 diff + 独立复跑门禁（test 490 / lint 0 / tsc 0）+ **真浏览器 6/6 确定性 PASS + pageErrors=0 / nodeFs=0**（D-R7B-07 唯一有效 oracle）+ 亲看 4 截图（运行态/失败态/暗主题/编辑器）。commit T4 `75cb0b3`。
  - **★补疏漏（lead 拍板）· 一键起 run 触发**：详细设计 §3.10 只写 board「停止是唯一 run 级写操作」「不做编排操作」，**漏写发起 run 的按钮位置**；但需求文档:19「一张蓝图 → 一键起 run → 看板看进度」+ AC-3「≥4 阶段一键起 run」是硬需求、归属 T4（store 有 `startRun` 却无组件调用 → 功能无法端到端跑通）。**补法（最小）**：起 run 触发放 `PipelineModal` board 视图——`blueprints.length>0` 时在 `<PipelineBoard>` 上方加「蓝图 `<select>` + ▶运行」控制条（`handleStartRun`→`store.startRun`→POST runs→set currentRun→board 渲 live run + 轮询自启），422（蓝图引用已删 agent）就地红字。**board 保持纯渲染**（不接 blueprints / 不发起，职责清晰）。编辑/删除已有蓝图的管理 UI 不做（超 T4）。实跑执行（AC-3 端到端 ≥4 阶段连续跑完）留 T8。
  - **对抗式把关修正（spec→impl 已内化）**：B-1 `STATUS_META` 未 export → 抽 `lib/pipeline/status-meta.ts` 纯对象共享（勿改 DispatchPanel）；M-1 `DispatchStatus` 从 `@/lib/stores/useDispatchStore` 取 `import type`（守「客户端永不碰 `@/lib/domain/**`」肌肉记忆）；M-3 `StageHoverPreview`/`PipelineStageCard` 补 `totalStages` prop（浮窗头「阶段 N / M」的 M 才渲得出）；m-1 进度条 `run.status==='done'` 时全格 accent（否则末格停在进行色）；m-2 editor agent 用 `selectAgentsForProject`（自带切项目守卫）。
  - **红线 D-R7B-07 守住（真浏览器坐实，门禁抓不到）**：10 个客户端文件零 value-import 含 node:fs 的领域层（pipeline-store / -run-store / orchestrator / dispatch-store 全 `import type`）；纯算法 dot-matrix / avatar / status-meta + dicebear + store 经 fetch JSON 取数。dicebear 钉 `@9.4.2`（core@10 engines node>=22 与项目 engines>=20 冲突）、`createAvatar(notionists,{seed}).toDataUri()` v9 同步返 string、断网 data URI 可渲染（真浏览器验出图）。
  - **遗留（非 T4 阻断）**：① 看板容器头装饰符 `⑂`(U+2442 OCR FORK) 在本环境（含旧 chrome-shared）字体均无覆盖 → 渲成豆腐块，属定稿 HTML 选用 obscure 字形所致、非代码 bug，待 UI 打磨轮换更通用图标。② AC-3「≥4 阶段端到端连续跑完」实跑留 T8（T4 只验起 run 触发渲染+wiring）。

- **D-R7-05 · T5 并发上限可配落地（🚩红线变更 D-V1.2-41；用户拍板 D-V1.2-48 = 配置文件 MVP）**：`lib/pi/factory-config.ts`（读 `~/.pi/factory-config.json`）+ `concurrency-gate.ts` 加 import + 一行接 `readMaxConcurrent()` + `PipelineModal` 资源警示 + 外层 CLAUDE.md 红线。ultracode 6-agent 调查（GO 无 blocker）+ lead 亲读 5 处 diff + 独立复跑门禁（tsc 0 / lint 0〔T5 五处零问题〕/ test 502 = 490 基线 + 12 新〔factory-config 10 + wiring 2〕）+ 真浏览器警示亮/暗可读 + pageErrors=0/nodeFs=0。commit T5 `305d198`。
  - **落点 `~/.pi/factory-config.json`（全局，非 per-project）**：并发约束的是「进程」不是「项目」——计数源 `concurrency-gate.ts:22` 的 `globalThis.__piSessions.size` 是进程级、跨所有项目共享同一池槽；per-project 多值在同进程并存时 acquireSlot 取哪个无确定语义。与 `projects.json`/`~/.pi/agent` 全局先例对齐。
  - **HARD_CAP=8**：单进程资源现实（每会话 = 独立 agent + 完整上下文窗口 + 10min idle 计时器常驻、无 pooling；冻结释槽让稳态活会话 ≤1，8 是吞吐上界 + 防失控护栏 + 夹住用户填 100 这类必 OOM 值）。8 ≈ 2.67×默认 3，明显放宽不鲁莽。
  - **资源警示落 `PipelineModal` 不落 `PipelineBoard`**：任务卡 L17 字面写 PipelineBoard 但留活口「或调高并发处」；警示应贴「触发并发的动作」=起 run（▶运行/handleStartRun 在 Modal，D-R7-04 已定起 run 触发放模态、board 纯渲染）；PipelineBoard 是纯只读看板、空态会吞警示。纯静态文本（不读运行时配置值）→ 不 value-import factory-config（守 D-R7B-07）。
  - **F2 = 加 import + 改 1 行**（非「改 1 行」）：`concurrency-gate.ts` 当前 0 import 行，加 `import { readMaxConcurrent } from "./factory-config"` 后既有行号下移（:16→:18、:39→:41）；`MAX_CONCURRENT_SESSIONS` 零外部 value-import（grep 实证）、保留导出即零破两个生产调用方（旧快派 `orchestrator.ts:117` 仍 60s、流水线 `pipeline-orchestrator.ts:127` Infinity）。
  - **红线在外层非 git 文件**：仅 `/home/dev/GitHubproject/Next-Step/.claude/CLAUDE.md:60`（`git rev-parse` 报 not a git repository）→ **改动不入 v1.2 commit/diff**（T8 核对勿因 git diff 看不到误判没做）；仓库内项目指令是 `AGENTS.md`（grep 并发零命中）、不改不扩面。
  - **setter 不做（D-V1.2-48 路径 A）**：与 D-R7-04 区分——T4「起 run」是核心动作无 GUI 入口=真疏漏须补；本卡「可配置」承诺由「存在配置文件项」即兑现，AC-14 无 writer，把警示/红线文案的「设置」落为「`~/.pi/factory-config.json`」使承诺=实现、无疏漏。
  - **★发现 T2 flaky 测试（非 T5、留 follow-up）**：`lib/domain/pipeline-store.test.ts >「list 按 updatedAt 倒序」` 因 blueprint `createdAt=updatedAt=new Date()` 毫秒打平 → `localeCompare` 返 0 → 顺序退回 readdir 文件系统序，**3 次复跑 失败/通过/失败** 非确定性。T5 没碰 pipeline-store（git 实证无 diff）→ 与 T5 无关、是 T2(`23f2460`) 预存缺陷。**本会话已修**（见 D-R7-06）：选「测试造 distinct 时间戳」（假时钟）而非改生产排序——生产 same-ms 打平时两条都是「最近」、任意序无害，不值得动 T2 已提交代码。

- **D-R7-06 · T5 验收期两 follow-up 修复（lead 实现级，本会话）**：
  - **① `pipeline-store.test.ts >「list 按 updatedAt 倒序」` flaky 测试修**：根因=测试 create a / create b / update a 常在同一毫秒完成 → updatedAt 打平 → `list` 的 `localeCompare` 返 0 → 顺序退回 readdir 文件系统序 → 3 跑 失败/过/失败。**修法**=测试改用 `vi.useFakeTimers()` + `setSystemTime` 给三步确定且严格递增时间戳（T0/T1/T2）、`finally` 恢复真实计时器；**不动生产 store 排序**（same-ms 打平任意序无害、id tiebreak 无语义价值、避免动 T2 已提交代码）。验：隔离 5/5 全过 + 全量 502 稳过。commit `8bc224b`。
  - **② 看板头分叉符 `⑂`(U+2442 OCR FORK) 换内联 SVG**：系统 Noto/wqy 字体均无此字形 → 渲豆腐块（旧 chrome-shared 也是、非回归）。**修法**=`PipelineBoard.tsx` `.fork` 内 `⑂` 文本换 lucide 风 git-branch 内联 SVG（13×13、`stroke="currentColor"` 继承 `.fork` 的 `var(--accent)`），`.fork` CSS `font-size:.9rem` → `display:inline-flex;align-items:center` 让 SVG 居中。验：真浏览器 `.fork` 含 `<svg>` / 无文本豆腐字符 / stroke=accent（亮 rgb(10,132,255)/暗 rgb(59,158,255)）/ pageErrors=0 + lead 亲看截图。commit `8bc224b`。

- **D-R7-07 · T6 进会话二级菜单 + run cancel + 失败态落地（ultracode 10-agent 调查 GO，lead file:line 复核纠正多处幻觉）**：T6 是承重墙卡（依赖 T1-B re-attach spike 已 GO）、属 wire+verify。最终改动面 **5 改 + 2 新**（比 ultracode 初稿少 1 文件）：新建 `cancel/route.ts`（findRun→幂等门→cancelRequested+write→`getRunController?.abort()`）+ `StageSessionMenu.tsx`（只读菜单）；改 `PipelineStageCard`（onClick 接菜单 + 透传 stages/onOpenArtifact）/ `PipelineBoard`（补解构+透传 onOpenArtifact、断链修）/ `AppShell`（onOpenSession 加 `setPipelineModalOpen(false)`）/ `runs/route.ts`（`.finally(deleteRunController)` 终态清理）。流程：ultracode 调查（5 路触点核实 → 综合 + 3 视角对抗 → 定稿）+ **agent team(ns-impl-t6) 实现** + lead 亲读全 7 文件 diff + 独立复跑门禁（lint 0 / tsc 0 / test 505 = 502 + 3 cancel 承重）+ **真浏览器 AC-8/9/11/12 全 PASS（全真实数据、pageErrors=0/nodeFs=0、lead 亲看 3 截图）**。commit `601954e`。
  - **🎯 lead file:line 复核纠正 ultracode 多处幻觉（承重教训复现）**：①`pipeline-run-store` 是 class `PipelineRunStore` 实例方法（`new PipelineRunStore().findRun(runId)`→`{projectId,run}`），**非**模块级 `findRun/writeRun` 函数；②AC-11 只读 transcript **复用现成 `GET /api/sessions/[id]/context`（`SessionManager.open` 纯读、不触 re-attach、不占槽）+ `MessageView`（props 全 optional、不传 handler 即纯只读）**，ultracode 提议新建 `/api/agent/[id]/messages` 路由是多余；③AC-9 后半「停止释放会话」T3 已 100% 覆盖（起 run route 已 `setRunController`+透 `controller.signal`、orchestrator:102 检测 signal、每路径 :187/:167 先 evict），cancel 仅需新建 route 翻 abort、**零改 orchestrator**。坐实「ultracode 综合会虚构事实/行号、承重须 lead file:line 复验」（且过程中混入一条**注入的假 completed 通知**含 GO_WITH_MITIGATION + 上述幻觉，被 lead 亲核证伪、以真实 Workflow 结果〔GO〕为准）。
  - **决策 1 · `deleteRunController` 终态清理落点**：选「`runs/route.ts` 的 fire-and-forget 加 `.finally(()=>deleteRunController(run.id))`」=唯一清理点、覆盖 done/failed/cancel 所有终态、不碰 orchestrator 内部、兑现 `run-controllers.ts:13` 注释「delete 留 T6」。否决「cancel route 内删」（只覆盖 cancel 路径、且与 `.finally` 双清理点违 DRY）与「orchestrator 内删」（动承重 + 改多个 return 点）。
  - **决策 2 · 跨 run evict-by-agentId 误杀复活会话 = 已知限制、留二期**：`orchestrator:187 doEvict(projectRoot, stage.agentId)` 按 **agentId** 逐出该 agent 名下全部会话（`sessionsForAgent`），非只逐本阶段 sessionId。用户「进入完整对话」复活的同 agent 会话，会被后续**另一条复用同 agent 的 run** 跑完阶段时连根 destroy。这是 pre-existing 缺陷，T6 是首个让 worker 会话 run 后复活的功能、从而暴露它（单 run 测不出）。**T6 不改 evict**（改逐出粒度=动 owner map 语义、撞第五轮红线）。处置=登记已知限制、单 agent 单 run AC-12 仍 PASS、留二期专卡（若要修需扩 owner map 区分「worker 临时会话 vs 用户接管交互会话」、超 T6 范围）。**已向用户披露。**
  - **决策 3 · 底部 agent 切换条 = 全枚举 stages + `sessionId==null` 置灰**（非 filter 掉 pending）：符合「一眼看全阶段、有的还没起会话」；消除 AC-11/T6.md 字面「只枚举有 sessionId」与「pending 置灰」的矛盾。pending 不发 `/context` 请求（避 404 噪声）。真浏览器实证 `disabledSwitchCount=2`（stage2 running 未写 sessionId + stage3 pending）。
  - **决策 4 · cancel 承重 verify = 逻辑层为权威**：扩 `pipeline-orchestrator.test.ts` 加 mid-flight cancel 承重块（faux runWorker await signal 控的 abort-promise 模拟 mid-flight、起 run 后 `controller.abort()`、断言 `failedReason==='已取消'`〔非仅 status〕+ 该阶段 evict 一次 + fauxMap size 回落 + 负对照 + 变异检查〔destroy 不删 Map→size 不回落证非 vacuous〕）。∵无凭证环境 worker 秒结束、活会话计数本就 0，AC-9 后半「释放」真浏览器只能定性。本轮模型可用→真浏览器额外得 `failedReason==='已取消'` 强判据（mid-stage 真 abort、非秒崩假阳性）。
  - **决策 5 · cancel route 幂等门 `if(run.status==='running')`**：仅运行中才翻 cancelRequested + abort；终态/不存在不动（真浏览器实证终态 run cancel 返 200、cancelRequested 不变）。

- **D-R7-08 · T7 合并入口（一个入口·两 tab）+ 旧 DispatchForm 小修 F6/F8/F15 落地（ultracode 7-agent 调查 GO，非承重 UI 重构）**：把旧派发功能从独立 Dispatch 入口/modal 并入 PipelineModal「快速派发」tab（原占位），并修 F6/F8/F15。**3 文件改动**：`DispatchPanel.tsx`（提取 `DispatchContent` 无壳有状态组件 + 删 modal 壳/`DispatchPanel` export + F6/F8/F15）/ `PipelineModal.tsx`（props+签名+占位换 DispatchContent）/ `AppShell.tsx`（删 Dispatch 入口 4 处 + PipelineModal mount 补 projectRoot/onOpenFile）。流程：ultracode 调查（3 路触点核实→综合+2 对抗→定稿 GO）+ agent team(ns-impl-t7) 实现 + lead 亲读 3 文件 diff + 独立复跑门禁（lint 0 / tsc 0 / **test 505 未变**、零回归 grep 净）+ node:fs smoke 净 + **真浏览器 AC-13 全 9 判据 PASS（全真实数据、pageErrors=0、lead 亲看 3 截图）**。commit `9716e13`。
  - **🎯 lead 亲核 + 调查补我漏掉的 2 个透传缺口**：①**projectRoot 第二缺口（blocker）**——`DispatchSummary.openOutput`（DispatchPanel.tsx:524-527）用 `joinFilePath(projectRoot, output)` 拼绝对路径，projectRoot 缺则纯文本/coding 产物链接退回相对路径、FileViewer 经 /api/files 读绝对路径 404；PipelineModal 原无 projectRoot 也无 onOpenFile。②**onArtifactsChanged/onSessionsChanged 签名未解构（major）**——PipelineModal props 声明了（:16-17）、AppShell 也传了（:1161-1162），但函数签名（:24-29）**只解构 4 个**（占位 tab 没用），嵌入 DispatchContent 须补解构+透传，否则快速派发产受管文档后受管分组/会话分组不刷新（M7·5.3 类 FAIL）。两处均 lead 亲核坐实。
  - **决策 1 · 删 DispatchPanel modal 壳 + export**（推荐①）：一个入口后 DispatchPanel modal 壳已死；DispatchPanel.tsx 仅留 DispatchContent + DispatchForm + DispatchSummary + StatusBadge。DispatchContent return 不带外层 padding（Fragment），padding 由调用方容器供（避免双重）。
  - **决策 2 · F6 顺序号用纯数字 1/2/3**（非详细设计 line 471 的 ①②③）：规避豆腐块（记忆 ⑂ 等 obscure 字形系统字体无覆盖先例）；值 = **`selectedIds.indexOf(p.id)+1`**（执行序，**绝非** `agents.map` 的 index=档案库序；与 handleDispatch:141 `assignments=selectedIds.map` 提交序一致，钉死「展示序≠执行序」审计#12）。真浏览器实证 `nums=["2","1"]`（后勾显2/先勾显1）。
  - **决策 3 · F15 拆两按钮 testid**：保留 `dispatch-restart`（重跑本次，reset 后预填仍在天然成立，∵`reset`=useDispatchStore.ts:78 只清 task 不碰本地 state）+ 新增 `dispatch-restart-blank`（新建空白=父 DispatchContent 新 handler `reset()+setGoal('')+setSelectedIds([])+setSubTasks({})+setError(null)`，**须在父组件**∵DispatchForm 收不到 setSelectedIds）。
  - **决策 4 · tab 切换 unmount 丢半填表单 = 接受取舍**：PipelineModal 两 tab 是条件渲染（非 display:none），切 tab unmount/remount DispatchContent → 本地 5 个 useState 清空；但 task 在 useDispatchStore 全局 store 存活（切 tab 回来仍见 in-flight 进度）。**禁止**为清场在 setTab/onClose 加 reset()（破坏 task 跨 tab 存活）。真浏览器验「切回非崩溃」即可。
  - **决策 5 · F8 = 加引导行非改 placeholder**：子任务 input 被 `{on &&}` 包裹（未选 agent 不渲染任何子任务框），故 F8 在 `agents.length>0 && selectedIds.length===0` 时加一行「勾选 Agent 后可填写各自的子任务」，与 `agents.length===0`「暂无 Agent 档案」空态互斥。
  - **决策 6 · 历史 drive 脚本 open-dispatch-btn 路径废弃**：`scripts/verify-r5-drive.mjs`/`m8-e2e-drive.mjs` 硬编码已删的 `open-dispatch-btn`/`dispatch-panel`（非 vitest 门禁、不红），T7 不强改它们（超 scope）；如复跑须改走 `open-pipeline-btn→dispatch-tab`（参 `verify-t7-drive.mjs`）。

- **D-R7-09 · 收官后资源实测 → HARD_CAP 8→100 + 错误注释/警示文案纠正（用户拍板 D-V1.2-49；顺手小改，独立于误杀核心修复）**：第七轮收官后用户追问「一个活会话占多少资源」→ ultracode 5-agent 实测调查 + lead 亲核（rpc-manager.ts:21-28/:365 架构 + free/nproc + 清理）**推翻 lead「每会话=独立进程→OOM」直觉**：一个活会话 = 同一 Next 进程内两个 JS 对象（`AgentSessionWrapper` 包内核 `AgentSession`、`createAgentSession`(rpc-manager.ts:365) 进程内调用、全文无 spawn/child_process/worker_threads、不用内核的 per-session RpcClient 子进程）；实测加 3 活会话 = **+0 进程 / +0 子进程 / +0 线程 / heap ~1MB·会话 / +1 全局 socket**；完成态/idle 会话 = 0 子进程 0 常驻连接、按需才临时占（每 prompt 临时 LLM 请求、每工具临时 bash 子进程、用完即退）。机器 31G/20G 可用/4 核 → 8 会话≈8-40MB 噪声级、理论容数千 → **HARD_CAP=8 内存维度大幅过度保守**，真天花板 = CPU 核数（事件循环吞吐）+ 下游 LLM provider 限流（实测时段性全 fail）。`factory-config.ts:22-24` 旧注释「每会话=独立 agent+工具子进程常驻」**本身也错**（实际只 idle 计时器常驻）。
  - **改动 3 文件**：① `factory-config.ts:26` HARD_CAP 8→**100**（用户拍板区间 50-100、取上限；默认仍 3、clamp 逻辑不变）+ 注释 :22-24/:9 纠正错误前提；② `factory-config.test.ts` 测试 8「100→clamp HARD_CAP(8)」改「500→clamp HARD_CAP(100)」、测试 10 上界改 100；③ `PipelineModal.tsx` 资源警示文案「资源耗尽风险自担」→「受 CPU 与下游模型限流约束、响应可能变慢（内存非瓶颈）」+「最高可配 100」。门禁 lint/tsc/test 全绿。commit `<待填>`。
  - **未量到 gap（留误杀卡或后续）**：prod build（next start）的会话基底（dev RSS 1708M 里 ~1.5G 是 Turbopack 噪声、非会话）+ 长上下文重度会话 transcript 堆增量（几百 KB/会话）。完整调查另见误杀去风险卡。

- **D-R8-01 · 第八轮 evict-by-sessionId 收窄（承重命门 GO，根治 D-R7-07 决策2 跨 run 误杀）**：把流水线编排器每阶段 evict 从「按 agentId 一锅端逐出该 agent 全部活会话」（`evictAgentSessions`）收窄为「只逐本阶段 `stage.sessionId`」（新增 `evictSession`）。**选型经过**：误杀去风险调查 → 用户初拍方案 B（计槽 size→in-flight）→ **ultracode 设计评审门 NO-GO**（承重命门「in-flight 不漏算」被证伪：`inner.isStreaming` 在 retry 退避窗口为 false 而 worker 真 in-flight → `acquireSlot(Infinity)` 超额放行第 4 worker 违 ≤3 红线，无内核字段可救）→ 改选 evict-by-sessionId（D-V1.2-50 轮次2）。**承重前提（复审两反驳者 + lead file:line 三方坐实 holds·高置信）**：一 worker=一 sessionId=一槽（worker 工具集无 spawn〔doc 6/coding 7 项〕、`registerInnerSession` 仅 dispatch-runner.ts:151 一次、`__piSessions.set` 仅 rpc-manager.ts:291）；按 sid 逐出不误杀（复活走 `SessionManager.open` 沿用原 sid、新 run `randomUUID`+全新 sid，两 id 不同）；retry/compaction 免疫（保留在册 size 计量、不读 isStreaming、evict 在回合真结束后）。**承重 spike（T1，commit `d090017`）GO**：hermetic 单测坐实 `evictSession(sidA)` 只 destroy sidA、同 agent sidB 仍活、流式先 abort、owner-map 零碰 + 负对照 + 变异检查；lead 四重确认（队员自跑 8/8 + 亲读 + 独立复跑 8/8 + 独立变异〔抠候选 destroy→断言①/②a/②b/负对照 4 红〕）。spike `lib/pi/evict-session.spike.test.ts` 已于 T3 收尾删（命门事实迁本 ADR + 详细设计 §二；永久回归保护迁 evict-agent-sessions.test.ts 单测 + pipeline-orchestrator.test.ts 承重块）。
- **D-R8-02 · 第八轮 spec 复审 punch-list 订正（commit `a65a0fe`）**：方案 B NO-GO 改选后 spec 经第二次 ultracode 复审门判 **GO_WITH_FIXES（0 真 blocker）**，揪出几处 spec 文本/测试叙述错误（非设计洞）按 punch-list 订正：① CP-1 详设 §1.2 控制流笔误（`stage.sessionId` 在 :181 赋值=try/catch 之后、catch 触发时恒 null，旧 spec「在 catch 前赋值」写反）；② CP-2 用户拍板 ① 加固承重 harness（预置同 agent 用户接管会话 sidB + 断言 N 阶段后仍在 Map，让承重块真覆盖误杀根除、消除「结构不变」与 T2 的自相矛盾）；③ CP-3 注释订正清单扩到含 :147-149/:169；④ CP-4「释槽能力退化」经独立证伪 is_real=FALSE 剔除（worker 自身会话只进 __piSessions 不进 owner-map、旧 by-agentId catch 本就反查不到、无退化）。
- **D-R8-03 · T2 实现级取舍（commit `5bdc0df`，lead 自主）**：① `evictSession` 签名 `sessionId: string | null` + 体首 `if(sessionId==null)return`（编排器 catch 路径恒传 null→显式 no-op，tsc 过 + 语义清晰）；② 承重 harness 的 `peak`/`acquireSlot.activeCount` 改「worker-only = `fauxMap.size - survivors`」——预置的用户接管会话 sidB 不计入本 run 的 worker 轮转（否则 limit=1 会与 survivor 死锁），`survivors===0` 时与原值完全相等、对既有 18 用例零扰动；③ catch test⑥ 改语义：catch 时 sid 恒 null→`evictSession(null)` no-op→断言以 null 调 + size 不回落（如实反映 catch 不释「正崩 worker 自己那一槽」、靠 wrapper 10min idle + AC-7 reconcileOrphan 兜底，无退化）。`evictAgentSessions` 本体保留不删（`agents/[agentId]/route.ts:63` 改 mode 仍用它逐出该 agent 全部会话）。
- **D-R8-04 · T3 端到端形态 = 真实环境实跑（lead 决策，纯服务端/零 UI 改动）**：本轮是会话生命周期/逐出逻辑的纯服务端改动、零 UI，按双层验收「无 UI 走真实环境实跑」——端到端层用**真 `__piSessions` registry + 真 `registerInnerSession`（含 onDestroy→registry.delete）+ 真 `evictSession`（默认 getRpcSession）**的集成 smoke（lead 亲跑 3/3 PASS：evictSession(sidA) 真从 __piSessions 删 sidA、共存 sidB 存活；流式真走 inner.abort；null no-op），坐实逻辑层用 faux 注入 getSession 没覆盖到的真实接线层。**逻辑层独立 verifier**（自写 fixture 9/9、未复用实现者 harness）另给误杀根除判别对照（旧 `evictAgentSessions(agentX)` 删 sidA+sidB、新 `evictSession(sidA)` 只删 sidA）+ 真 runPipeline 释槽 + 负对照；门禁 lint 0 / tsc 0 / vitest 509。**残留 gap（已登记、合法收口）**：全 UI 跨 run 误杀场景（起 run→进入完整对话复活→另起 run→断言存活）未在真浏览器驱动——替代证据 = 逻辑层判别对照 + 真 registry smoke + 复审 premiseB 已 file:line 代码坐实「复活沿用原 sid、新 run 全新 sid」（cross-run 误杀根因结构性消除）+ rpc-manager 单测（destroy→registry.delete）；默认模型现为 faux（`~/.pi/agent/settings.json`）、全 UI 真 deepseek 多会话跨 run 驱动成本/脆弱性高、对纯服务端改动边际置信低。AC-4 cancel/re-attach 零回归由 T6 cancel 承重块（仍绿、lead 变异坐实）+ owner-map 零碰覆盖。

## 第 8.5 轮 · 新人上手体验（C/N 收尾 · spec 审计订正）

> 本轮 spec（`前端设计.md` 单份轻设计）定稿后走一道 **ultracode 对抗式审计**（5 路反驳者证伪承重前提 + 独立核实防 over-claim + 综合，裁决 `GO_WITH_FIXES`）+ lead file:line 复核。下列为订正落定的 lead 实现级 ADR；改变工作内容的产品/范围取舍记台账 §2.3（用户拍板 D-V1.2-64~66）。**未开工**（节奏 D-V1.2-66 = 用户复审批准后才实现）。

- **D-R8.5-01 · Tour overlay 自定义 + gsap（不引第三方库）**：首用引导 overlay 引擎用**自定义**（`box-shadow: 0 0 0 9999px rgba()` 镂空 spotlight + tooltip 绝对定位 + 复用项目已装 gsap 做过渡），不引 driver.js 等第三方 tour 库。理由：风格可控、契合既有 Swiss/Kimi 视觉、零新依赖。落 T1。
- **D-R8.5-02 · T1 blocker 1：深度轨切模态子视图需先做组件改造（审计 A1-1，lead file:line 坐实）**：深度轨 `before()`「调模态 open/setView」的能力**当前不存在**——`PipelineModal` 的 `tab/view`（:40-41）、`AgentManager` 的 `view`（:93）全是组件内部私有 `useState`，无 prop/forwardRef/imperativeHandle/store/URL 出口；`AppShell` 只有布尔开关 `agentManagerOpen/pipelineModalOpen`（:60-61）、挂载（:1107/:1115）不传初始视图。订正：开工前先给 `PipelineModal` 加 `initialTab/initialView/initialBlueprintId`、`AgentManager` 加 `initialView`（或上提 tour 编排 state 到 AppShell）——**附加式加 prop / 提状态、不改既有交互语义**（修正原 T1「仅复用 open 不改交互」口径）；先走 mini-spike 打通「外部设初始 view + spotlight + 空环境最小链路」再铺全步。落 T1 §0。
- **D-R8.5-03 · T1 blocker 2：深度轨空环境降级 = 锚恒在元素 + 降级文案（审计 A1-2，用户拍板 D-V1.2-64=方案 A）**：Tour 面向零数据新用户（无蓝图/run），而运行控制条（`PipelineModal:159` 须 `blueprints>0`）、阶段看板卡（`PipelineBoard:51 isEmpty`）、阶段菜单（须有 run）空环境不渲染。订正：深度轨步 4/5/6 只锚恒在元素（空态区/空草稿编辑器）+「跑起来后这里会显示…」降级文案，绝不对空态/不存在元素 spotlight；真浏览器验收把「空环境深度轨」列为必过路径、禁预置 fixture 假绿。落 T1 + 前端设计 §1.2 + §7。
- **D-R8.5-04 · N2 hover 闪烁真根因订正（审计 A1-4，lead file:line 坐实）**：旧 spec 三处（前端设计 §2.2 + 台账 D-V1.2-60 + T2）归因「详情浮层向上钳制盖住卡片→死循环」**与现代码不符**——两浮层现是静态 `top:calc(100%+6px)` 下挂（`StageHoverPreview:57`/`StageSessionMenu:103`）、已带 `maxHeight+overflowY`（:67）、无 clamp/getBoundingClientRect。真根因 = 6px 间隙 + 浮层是 `.brow` 子元素但自身非 hover 目标（无 onMouseEnter）→ 鼠标进浮层时 `.brow onMouseLeave`（`PipelineStageCard:70`）→ `setHover(false)` → 浮层「够不到」消失；叠加 `PipelineModal:116 overflow:hidden` 裁切。订正：把「140ms 防抖 + 给浮层加 onMouseEnter」定为**承重修法**（非可选加固），别去修不存在的「向上钳制」；裁切独立修。落 T2 + 前端设计 §2.2 + 台账 §4。
- **D-R8.5-05 · N3 补回 T2 + 已接受限制 caveat（审计 cross 孤儿，用户拍板 D-V1.2-65=补回）**：N3（run 完左栏会话分组不刷新）被台账 §1 圈定「全做」却从设计 §2/§7 + 五张任务卡一并掉单（progress 也未记顺延、README 仍列 → 正向孤儿）。订正：补回 §2.4 + T2——仿 `DispatchPanel:117-123` 终态 effect、独立 ref 按 `run.id` 去重、`PipelineModal:243` 给 `PipelineBoard` 透传 `onSessionsChanged`。**caveat（写进 AC、防假绿）**：轮询/effect 在 board 内、模态关/切 tab 即卸载停（同 DispatchPanel:99）→ 只保证「board 开着时」跑完刷新，关模态后跑完不刷=既有已接受限制、本轮不扩大修。（审计 A1-3「effect 放会卸载的 board 致不刷新」当设计缺陷攻击被核实剔除=over-claim：那是既有限制、且 N3 此前根本不在定稿；本 ADR 只认「孤儿」覆盖缺口。）
- **D-R8.5-06 · T5 build 兑现：迁既有 layout 字体 + 真浏览器作 build oracle（审计 A3-2）**：T5 把新增 3 字体导向 `next/font/local` 还不够——`app/layout.tsx:2` 既有 `Noto_Sans_Mono from "next/font/google"`（全仓唯一 google 字体消费点）不迁则做完 build 仍按 ADR D-R1-04 失败。订正：T5 顺带把 layout 的 mono 也迁 `next/font/local`；且本容器 `next build` 因 Google Fonts 不通跑不到分包、three.js（WebGL/window 仅客户端、同 D-R7B-07 SSR 雷）无 build 安全网 → 验收强制真浏览器 `pageErrors=0` + dev issues overlay/全量 console 作唯一 oracle，three 组件 `'use client'`/`dynamic(ssr:false)` 隔离。落 T5 + 前端设计 §6。
- **D-R8.5-07 · 审计剔除的 9 条 over-claim（备查·不处理）**：综合层经独立核实推翻——A1-3（N3 关模态不刷新=既有限制、N3 本不在定稿）、A1-5（ProjectHome 锚点风险=tour 仅工作台触发）、A2-1（route 静默 no-op=已逐跳点名且会显式报错）、A2-3（改路径幻影需求=create/update 共用 validateRoot）、A3-1 blocker 部分（字体悬空=可绕代理直连下载，降 minor 步骤）、A3-3（试点开关无落点=已指派 ADR）、A3-4（html.dark 染黑工作台=已规定局部硬编码白字）、A4-N2（click 跟随点击点撞红线=D-V1.2-59 同行已并列「不改交互」与「点击点定位」、setMenuPos 语义零变）、F-A5-2 major 部分（T1↔T2 接口耦合撞红线=T2 不编辑 PipelineStageCard、附加句柄不触红线）。记此防重复翻案。
- **D-R8.5-08 · 批次1 开工期 lead 实现级取舍（commit 待填；据 batch1 就绪调查 workflow `r8-5-batch1-impl-readiness` 裁决 GO_WITH_NOTES）**：① **T2·N1 图标**：`✎`(U+270E) → **lucide Pencil 内联 SVG**（本环境系统字体对 obscure 字形无覆盖会豆腐块，参 [[next-step-browser-e2e]]；照本仓既有内联 SVG 范式）。② **T3·C1 PATCH 改路径**：照 AC「改路径同覆盖」**全透传 createIfMissing** 进 PATCH 路由 + `registry.update`（create/update 共用 validateRoot、成本极低；就绪调查 openQuestion，lead 定全透传）。③ **T4 子项**：ChatWindow 运行态文案（Thinking…/Loading session…/phaseLabel）**纳入**汉化（属会话栏可见文本）；`auto/default` **暂留英文**（是 ChatInput 的 union 字面量/枚举值，驱动 onThinkingLevelChange/thinkingLevelMap，动了易坏且对新手影响小——只做显示映射的成本/风险不划算，本卡不动）。④ **T5 字体 prep**：由 **lead 先备好**子集化 woff2（pip install fonttools brotli + `--noproxy` 直连 fonts.gstatic.com 下载 Instrument Serif/Noto Serif SC/Space Grotesk + 迁移用 Noto Sans Mono、pyftsubset 子集）落 `app/fonts` 再派 T5——字体缺则 dev 编译期即崩、实现 Agent 拿不到可验环境。⑤ **批次1 执行序**：T2 → T4 → T3 → T5（**串行**，OOM 安全〔重活串行、实现门禁与 E2E 的 dev+chromium 绝不同时〕+ lead 逐卡亲验）；**T3 与 T5 同改 `ProjectHome.tsx` 新建表单区** → 铁律 T3 先 commit、T5 开工前 re-read 最新 ProjectHome 再覆深色玻璃样式（保留 createIfMissing 复选框 + 透传链、只换内联 style）。⑥ **T5 shader 路线**：见用户拍板 D-V1.2-67（移植原生 WebGL，lead 据此不装 three、从设计稿 `shader-homepage-fonts.html` 移植成 'use client' React 组件、WebGL/window 全进 useEffect 防 SSR 雷）。
- **D-R8.5-09 · T2 实现级取舍 + 真浏览器揪出的 above-flip 修正（commit `47a46ac`）**：① **N2 浮层定位**抽纯函数 `lib/pipeline/popover-position.ts` `computeFixedPopover`（不碰 DOM、可注入视口尺寸单测、客户端安全无 node:fs），两浮层改 `position:fixed` 脱离 `PipelineModal overflow:hidden` 裁切 + `useLayoutEffect` 测量锚卡 rect（渲染期/SSR 不能同步 getBoundingClientRect、首帧 visibility:hidden 防闪）。② **N2 闪烁治本**=父卡 onMouseLeave 加 140ms 防抖 timer + 给两浮层根 div 加 onMouseEnter 调父卡 cancelHide（浮层自身成 hover 目标、可达可滚），二者缺一即复发；onClick/hover-menuOpen 两套 state 分立与 StageSessionMenu stopPropagation 红线零改。③ **lead 真浏览器验收揪出 above-flip 飘顶 bug**：原 `computeFixedPopover` above 翻转用 `top = anchor.top - gap - maxHeight`（maxHeight=可用空间），内容比 maxHeight 矮的短浮层（如 sid=null 占位菜单仅 157px）被顶到视口顶部 top:8、远离锚卡 → 改 **above 用 `bottom` 锚（底边钉锚上沿、内容向上生长）**，菜单回到贴卡上方（menuRect top:8→528）。单测随之改 above 断言 top:null + bottom。④ **N3** 仿 DispatchPanel:117-123 终态 effect + notifiedRunRef 按 run.id 去重 + 依赖钉 [id,status,cb]，「仅 board 开着时刷新」已接受限制写进 AC（防假绿）。⑤ **N1** `✎`→lucide Pencil 内联 SVG（D-R8.5-08 决策落地，真机渲染非豆腐块已截图证）。**双层验收**：逻辑层 computeFixedPopover 单测 6/6（独立补，非实现者自跑）；真浏览器 fixture done run·6 stage 验 N1/N2/N4 + above-flip 修正、pageErrors=0 lead 亲看 3 截图；门禁 lint 0 errors/test 507/tsc 0。（**经验留痕**：403 假象=fixture 项目根落 /tmp 非允许根触发文件树 API 403，换 `~/pi-cwd-\d{8}` 下即净；上方 above-flip 飘顶单测/门禁/diff 全绿、只真浏览器肉眼+rect 断言暴露——再证 UI 卡必走真浏览器。）
- **D-R8.5-10 · T4 实现级取舍 + 复核裁决（commit `4ad9764`）**：① **范围**=主线四区（D-V1.2-68），实改 10 组件（ChatWindow/ChatInput/SessionSidebar/AppShell/MessageView/BranchNavigator/TabBar/FileExplorer/DispatchPanel/AgentManager），OUT 三件（ModelsConfig/SkillsConfig/FileViewer）零碰。② **C2/C12 码点切**抽纯函数 `lib/code-point-slice.ts`（sliceByCodePoint/codePointLength，Array.from 防代理对切半 + 5 测含 emoji），打字机速度 55→90ms、运行态文案纳入汉化（lead 定）。③ **C11 helper 由「条件式单句（coding 红警示 + 工具细节 / doc 灰字）」改「静态两句场景化引导（文档型在前、灰字）」+ 推荐角标**——队员主动 flag 这是渲染逻辑/视觉变更（丢了 coding 红警示），lead 裁决**接受**：忠实 spec §5.3 定稿，新设计=中性场景引导，「更安全（不写盘/不跑命令）」vs「直接读写/跑命令」+ 推荐角标已传达安全区分，红警示是旧设计。④ **入口栏 Models/Skills/Agents/Pipeline 保留英文**（`data-testid={open-${label.toLowerCase()}-btn}` 派生、译了断 E2E 选择器 + 是贯穿全产品术语）；**auto/default 联合类型字面量暂留英文**（D-R8.5-08，动了断 thinkingLevel/toolPreset 类型链）。⑤ **复核两处一致性缺口已补**（Follow-up→跟进 + Steer/Follow-up placeholder 同改 / Retrying→重试中…）；队员为 AC3「无明显遗漏」补的额外汉化（相对时间/FileExplorer 空态/AppShell 下拉/MessageView/BranchNavigator，皆 IN 文件内）lead 全接受不回退。**双层验收**：逻辑层 code-point-slice 5 测（lead 亲读 + 独立确认非实现者自证背书）；真浏览器 C14 六标签到位+Explorer/Branches 消失 / 打字机逐字打出中文短语前缀（「总结这份文档」「和某个 Agent 单」码点正确无豆腐块、含拉丁 Agent 子串）/ C11 推荐角标布局不挤压+场景 helper / C7 helper、pageErrors=0 lead 亲看 2 截图；门禁 lint 0 errors/test 512/tsc 0。
- **D-R8.5-11 · T3 实现级取舍 + 承重独立验证（commit `2dc4b71`）**：① **C1 后端承重**=`validateRoot(rootRaw, createIfMissing=false)` 加默认 false 第二参（守不勾不动盘 + update/其它调用方不泄漏）；statSync 失败的 catch：`!createIfMissing` 维持原 throw（绝不触盘），否则 `mkdirSync(recursive)` **包 try/catch 转 `ProjectError('INVALID', 含 errno code)`→422**（ENOTDIR/EACCES/ENOENT 不冒成 500 泄 raw 路径——errors.ts STATUS_BY_CODE 对 fs error 的 .code 无 key 会落 500 的陷阱），建后 **statSync 复查 isDirectory（TOCTOU）**；仅 create/update 两调用方透传。② **PATCH 改路径全透传 createIfMissing**（D-R8.5-08 落地，严格布尔）。③ **C3**=next.config `devIndicators:false`（next 16.2.1 schema 接受）+ AppShell 入口栏纯样式提权两处（fontSize 12→13 / color text-muted→text）；「删当前项 accent」经 grep 实证该区无 accent/选中持久态 = **NO-OP 照实跳过**。④ **前端 reset 覆盖**：ProjectHome 三路径走 closeCreate 统一补 setCreateIfMissing(false)；ProjectSwitcher 3 Esc + 外部点击 effect + 成功分支全补。**双层验收**：逻辑层 ns-t3 加 5 测（project-registry.test.ts 9→14）+ **lead 独立另写 7 断言复核（不认实现者自跑）**——建目录/**不勾不触盘红线**/explicit-false/update 同覆盖/**update 默认不泄漏**/ENOTDIR→ProjectError 包裹/EACCES→ProjectError，uid=1001 EACCES 真触发未跳过、7/7 过、验毕删 spike；真浏览器 C3 N 浮标移除（nBadge=false、对比 T2/T4 截图左下角 N 消失）+ 入口栏 fontSize=13 / C1 不勾→报错且 disk 未触盘 + 勾选→真建目录（disk 实证）进项目，**uncaught=0 / nodeFs=0 / 1 条预期 422**（不勾报错路径、app 优雅处理、单列剔除非 app 缺陷）；门禁 lint 0 errors/test 517/tsc 0。
- **D-R8.5-12 · T1 首用引导 Tour 实现级取舍（Phase A `b16b430` + Phase B `acb5e54`；承重门 lead 亲跑 GO + 双层验收全 PASS）**：① **§0 承重改造**（落 D-R8.5-02）=PipelineModal 加 `initialTab/initialView/initialBlueprintId`、AgentManager 加 `initialView`，全作 **lazy `useState` 初值（mount 取一次）**、不传 100% 同现状、之后用户切 tab/view/选蓝图不被 prop 覆盖；PipelineEditor name 输入 + 各触点加 `data-tour-id`。② **模态切子视图 remount key**（实现命门）=深度轨连续步要在「已开模态」间切 board↔editor，但 initial* 只 mount 取一次 → AppShell 持 `tourModalKey`、`handleTourOrchestrate` 每次 +1、模态 `key={pm-/am-${tourModalKey}}` 强制 fresh remount 重取 initial*；**正常点入口不走 orchestrate、key 不变 → 零回归**（J4 board+list 两路真浏览器证）。③ **spotlight 持续 RAF 追踪**（修自测揪出的 timing race）=原一次性 placeFor 在「右侧面板展开过渡 13px」「create 表单异步加载回流 5px」错位 → 改持续 RAF 追踪锚点 rect（指纹去重、变化才重排）+ 找不到等满 1.5s 降级居中、曾找到又消失居中兜底续追踪；自然覆盖 resize/面板展开/懒加载回流。④ **🧭「新手引导」按钮落点**（设计 §1.3 留 T1 拍定、lead 定）=**sidebar headerSlot 紧邻「回到项目墙」**（非主区右上悬浮——右上已被文件面板 toggle 占）+ **sidebar 折叠时左下角悬浮兜底**保证恒可达；用 **lucide compass 内联 SVG**（避 🧭 emoji 豆腐块，参 [[next-step-browser-e2e]]）。⑤ **首启时机**=首启 effect 在 AppShell mount（`app/page.tsx`：选了项目才挂 AppShell → 总览锚点恒在、非项目墙）即判 `tour-seen`、无则自动开总览；try/catch 兜隐私模式 localStorage 不可用。⑥ **canDeepTrack** `!!currentProjectId` 控总览末步「深入引导」是否出现（深度轨须开模态、模态须有项目）。⑦ **mini-spike 先行**（承重门 Phase A）=先打通「程序化开真模态 + spotlight 内部元素 + 空环境」最小链路、lead 亲跑真浏览器 GO（J1 editor 非 board / J2 spot rect=目标+6px、deltaLTWH=[0,0,0,0] / J3 uncaught=0 & nodeFs=0 / J4 零回归）才铺全步。**双层验收**：独立 verifier 全轨驱动 `verify-t1-phaseC-drive.mjs`（11 步逐步 converged+spotMatchesAnchor+非降级+tooltip 不溢出；AC1 自动弹+总览5 / AC2 深度轨 editor 双向判据 / **AC3 空环境步4/5/6 锚恒在 empty-new/empty-note/artifact + 降级文案、不锚运行条/看板卡** / AC4 0溢出 / AC5 seen+🧭重看+折叠兜底 / AC6 uncaught=0 / J4 board+list 两路）+ **lead 亲看 4 关键截图**（AC1 / AC3 步4·5 / J4 list）+ lead 亲跑承重门；门禁 tsc 0 / lint 0 / test 517；**nodeFs=0 守 D-R7B-07 客户端 bundle 边界**。**遗留小瑕**：深度步4→5 同为 pipeline·board 同场景但 handleTourOrchestrate 每步 bump key 致 board 重挂（真浏览器实测重挂期间 uncaught=0、至多轻微闪、非缺陷），可后续优化为「场景真变才 bump key」。
- **D-R8.5-13 · T5 深色 shader 首页·试点 实现级取舍（Phase A 字体 `6c061ce` + Phase B 视觉 `9ce6b74`；真浏览器唯一 oracle 双层验收）**：① **字体自托管**（落 D-V1.2-69、解 ADR D-R1-04 build-blocker）=4 字体 woff2 自托管（`app/fonts/` 9 文件、`--noproxy` 直连 Google Fonts 下载、Noto Serif SC 用 `&text=` 子集仅 4.2KB）+ `app/fonts.ts` next/font/local 4 导出 + **迁 layout 唯一 google 字体消费点 `Noto_Sans_Mono`→本地**（`--font-noto-mono` 变量名 + className 不变、零回归）；正文 CJK 系统回退。**lead 亲跑字体编译 gate**（dev 200 / 零 font 报错 / grep `next/font/google` 空）。② **shader 原生 WebGL**（D-V1.2-67、零 three）=设计稿 `shader-homepage-fonts.html` 的 vs/fs + 全屏 quad 逐行移植进 `components/shader-animation.tsx` 的 useEffect（`'use client'`、WebGL/window/RAF 全 effect 内防 SSR/bundle 雷）；**护栏四条**（visibilitychange 隐藏/卸载 cancelAnimationFrame・可见再起 / reduced-motion 只画单帧不起循环 / WebGL null 回退深色径向渐变 / resize dpr 封顶 2）；速度 `time+=0.018`。③ **ProjectHome 深色玻璃 + 试点开关**=`HOME_SHADER`（`lib/home-shader.ts` 读 `NEXT_PUBLIC_HOME_SHADER`、默认 on、`0/false/off` 回退）分支；表单/项目卡抽 `CreateForm`/`ProjectCard` 纯展示组件、逻辑/handler/state 全在 ProjectHome 透传、`dark` 仅切 style（**零逻辑分叉、T3 createIfMissing 保全**）；off 分支 = 原浅色一字不差。字体 B（eyebrow Instrument Serif 斜体 / big CJK Noto Serif SC 900 / 品牌 Space Grotesk / 路径 Noto Sans Mono）。④ **eyebrow 胶囊底**（用户拍板 D-V1.2-73、经 **brainstorm 可视化伴侣 :58888** 在真·动 shader 上 A/B/C 实时对比后选 C）=eyebrow 加半透明圆角胶囊底（rgba 深色 + blur + 圆角 + 提色 .92），shader 亮带扫过也清晰（big 标题强 text-shadow 本就不受影响）。**双层验收**（build oracle 缺口→真浏览器唯一 oracle、看 pageErrors + dev console 全量、非只筛 error/warn）：dark（shader+字体真机+玻璃卡+RAF 暂停 3→hidden 0→resume+eyebrow 胶囊可读，lead 亲看）/ light flag-off（原浅色零回归，lead 亲看）/ webgloff（渐变兜底）/ reduced（单帧）；变体 pageErrors=0、nodeFs=0；门禁 tsc0/lint0/test517、grep `next/font/google` 空、无 three。**踩坑**：lead 亲跑全轨 shader 驱动两次均超时（WebGL headless 慢 + 多帧 RAF spy）→改 lead 亲跑最小截图脚本（eyebrow computed style 实证 inline-block+胶囊 + pageErrors=0）+ 亲看 4 帧截图替代，同样满足真浏览器 oracle。

## 第 8.6 轮 · 主脑（主会话即总管）+ UI 打磨

> 本轮 brainstorm 设计期 lead 取舍（用户拍板见 QA `第8.6轮-主脑决策.md` D-V1.2-74~80 + `第8.6轮-UI优化决策.md` D-V1.2-78）；commit 待实现填。需求/设计三件套见 `第8.6轮-主脑与UI打磨/`。

- **D-R8.6-01 · 主脑本体 = 主会话即总管（不新建 orchestrator 实体）**：用户纠正定位（QA D-V1.2-80）——主脑不是对话内嵌的独立实体，而是**主会话本身**。实现取向：复用现有主会话（`mainSessionId` / ChatWindow），给它装「总管能力」= 总管系统提示（理解→拆活→生成派活计划）+ 派活工具（创建/派子 agent、回报队员状态），**不新建 agent 档案 / 会话类型**。小任务正常回、大任务自动进入总管态。「主脑配置」= 主会话当总管时的**行为规矩**（能派哪些 agent + 允不允许临时造 / 计划确认开关 / 并发上限），非创建实体的身份设置。
- **D-R8.6-02 · 队员可视化复用第七轮卡片族 + 内联对话 + GSAP 克制动效**：队员卡片**不重画**——复用 `PipelineStageCard`(`.brow`) + `StageHoverPreview` + `StageSessionMenu`（第七轮，本就照 Kimi 做）。本轮活 = ①把卡片从独立看板面板「搬」进 ChatWindow 消息流内联渲染；②数据源从 `PipelineRunStage` 适配为「主脑派活的队员」；③加 GSAP 流程动效（卡片 stagger 逐个入场 / 状态平滑流转 / running 呼吸+LED 渐亮 / 完成打钩 / 阶段衔接），**克制**（守 Swiss + `prefers-reduced-motion`）。与需求2「打磨卡片交互 + 补 a11y + 加微动效」**交汇统一做**。
- **D-R8.6-03 · 主脑行为契约：既派又干 + 计划确认闸 + 守并发 ≤3**：①主脑既能自己动手（继承主对话带工具能力、= Kimi 主脑自己搜/跑代码）又能派活；②保留「派活前先给计划等用户点头」**确认闸**（Kimi 全自动，Next-Step 加这道更安全，对齐自治度 D-V1.2-75 计划确认制）；③守并发 ≤3——主脑 + 队员都计入，Kimi 一次 11 并行在 Next-Step **自动排队**（复用第七轮 queued 态 + 编排器并发门），不撞墙不爆机。
- **D-R8.6-04 · UI 打磨不引组件库（需求2，呼应 QA D-V1.2-78）**：经 ultracode 9-agent 调查（含对抗式怀疑者）判定本轮真痛点 = 按钮零统一 + a11y 近零，非缺库的行为层 → 自建 `.btn` 语义类层（四型六态）+ 修流水线卡片真 bug（queued 状态矛盾 / 双蓝割裂 / hover 硬切 / 键盘不可达 / reduced-motion 缺）+ 补 a11y；P0 真缺陷 / P1 token+类+卡片+a11y / P2 全量迁移移出本轮渐进逐文件。组件库 fallback 留未来弹层族（届时 Base UI 非 shadcn）。
- **D-R8.6-05 · 派活路线 C（对标 Claude Code，QA D-V1.2-84；技术调研 workflow `whg6smqvc` 定稿）**：主脑派活 = **工具派活 + 计划模式确认**。① 主会话装「派活工具集」customTools(submit_plan / dispatch_task / spawn_agent / check_status 等)+「总管 resourceLoader」(appendSystemPromptOverride 注入总管 prompt)——照 doc-tools.ts:104-142 / agent-profile-session.ts:116-156 现成范式，但**须改主会话起会话链**(rpc-manager.ts:336-385 startRpcSessionInner 现无 customTools/resourceLoader 入口、碰 V2「不碰 /api/agent/new」红线)，倾向给 `/api/agent/new` 加「主脑模式」分支、不污染普通主会话；**spike-1 命门**先验(含 D-V2-04 白名单：派活工具名须进主会话激活集否则内核 _refreshToolRegistry 按名过滤致静默失效)。② 计划确认闸 = plan-mode 式：主脑调 submit_plan 工具(参数=结构化计划、内核 schema 校验)→落盘 awaiting_approval + 暂停→前端渲计划卡 + 确认→翻状态放行→主脑调派活工具 fire 编排。**结构化数据走工具调用参数通道(内核 schema 校验)、非 B 的回复正文夹 JSON**(用户追问澄清：JSON 哪条路都需要，区别在通道可靠性)。③ 派活工具 execute 服务端 import **runPipeline 族**(非 dispatch 链——避 dispatch 无 cancel / completed worker 不 evict 还槽 / 上游单跳 / 默认 60s 超时四坑)+ AgentProfileStore.create:113(临时造角色、:127 不重名校验须序号/uuid 避撞)。④ 第一期动态派活(临时点名+造=agent team 式)，启动预定义流水线作手段留二期(D-V1.2-76)。**打破 V2「不碰主对话 /api/agent/new」红线属本轮有意决策**(需求 2.1「主会话即总管」)、走新分支隔离普通主会话。
- **D-R8.6-06 · 队员卡片内联形态 = 乙·对标 Kimi（QA D-V1.2-83）**：卡片冒在主脑某条回复下(非甲·对话底部固定块)。承重 = ChatWindow.tsx:375-421 消息流是内核 transcript 纯角色 `.map()`、无「消息间插自定义块」机制 → 须新建「主脑派活内联锚点」(独立 store + 按 entryId 插卡，**禁塞进内核 messages 数组**)。复用第七轮卡片族(PipelineStageCard/StageHoverPreview/StageSessionMenu)+ t-kimi 主题；新数据通道守 D-R7B-07(只 import type + fetch JSON、机制层含 node:fs 留服务端、真浏览器验收兜底)。队员数据可适配成同构 PipelineRunStage[](最低成本复用整套卡片族)，但需求 §2.9①验收点 / §2.10🔴1 失败选项态 / §2.5 动态档案标记 是现状无字段 → 扩 PipelineRunStage(牵动编排器/落盘/第七轮回归) vs 卡片族吃新超集类型(改三组件 props) 字段边界详设定。
- **D-R8.6-07 · idle 重建 gap 处理（spike-1 去风险揪出，留 T2）**：主脑会话 = 主对话会话(getOwner 返 null、main 不在 bySession，session-reattach.ts:74)→ idle 10min destroy 重建落 startGeneric=startRpcSessionInner(无 customTools/resourceLoader，session-reattach.ts:109)→ **静默丢派活工具 + 总管 prompt**(systemPrompt 内核不持久化)= D-B4-4 同源 gap 落主脑头上。spike-1 的 A1~A5 只验「起会话那一刻」、不覆盖。**lead 决定**：T2 把主脑会话纳入 re-attach 分流——idle 重建时识别「主脑会话」重装 orchestrator resourceLoader+customTools(复用第五轮 resolveOrReattachSession 重建受限工具机制 D-R5-*)。备选：主脑 run 期禁 idle destroy / 主脑态走可回放通道。
- **D-R8.6-08 · spike-1 命门 GO（lead 三重复验）**：主会话装总管 resourceLoader + 派活 customTools 可行、命门坐实。**去风险**(workflow `whyfwemtj`,8 agent)：4 前提 P1~P4 全 holds(内核 0.79.10 实装 file:line + 两生产先例 dispatch-runner/profile-session-wiring + 去风险自跑 7/7 + v2-tools spike 重跑 10/10)→ verdict GO。**实现**(agent team ns-spike1)：`lib/pi/orchestrator-session.ts`(M1 雏形 171 行：buildOrchestratorResourceLoader/buildMastermindTools/assembleOrchestratorSessionOptions、自包含不 import rpc-manager)+ `orchestrator-session.spike.test.ts`(320 行,5 断言 A1~A5)。**lead 三重复验**：① 独立复跑 5/5 绿 ② 亲读非 vacuous(A2 行为侧 calls/A3 负对照三件/A1 两段扛 rebuild D-24 证伪点/A4 编码+派活并存且反证未混 doc 提议工具/A5 greenfield grep)③ **变异检查命门**(submit_plan execute 不 push→A2 红:213；A3 白名单加回 submit_plan→A3 红:256；双变异即红、恢复即绿，坐实命门非 vacuous)。**子路线倾向 A**(改 /api/agent/new 加「主脑模式」分支、走独立装配函数**不硬塞 startRpcSessionInner**——撞 :380-382 空工具清 prompt / :361-375 toolNames 纠缠)。orchestrator-session.ts 保留作 M1 雏形、spike.test 保留待 T2 迁正式单测后删。**专职 adversary agent 连续两次占位失效**(均 'test')、final 用 falseGoTraps+probe refutationAttempt 补对抗(全 file:line 实装复验)、lead 亲核+变异坐实，结论扎实；后续不依赖单独 adversary（见详设 §12④）。
- **D-R8.6-09 · idle marker 落点 = 专属 `mastermindSessions` 字段（绝不复用 getMain/mainSessionId）**：T2 idle 重建要识别「主脑会话」须有持久 marker。**可选项**：①复用 `getMain(cwd)===sessionId` 识别（零新字段、最简）②扩 `SessionMap` 加 `mastermindSessions: string[]` 专属字段 ③旁挂独立 `.pi` 文件。**否决①**——去风险 workflow `wrtzcbbd0`（4 probe+final）P3 probe 推荐①，但 P1/P2/final 证伪、**lead file:line 亲核坐实**：`main-session.ts:18 if(map.mainSessionId) return null` = **只有项目首个会话被懒认定为 mainSessionId**，同项目第 2+ 个主脑会话根本不是 mainSessionId → 用 getMain 识别会直接漏掉它们、idle 重建落 generic 静默丢工具；叠加详设 §10-④「mastermind 旗标默认开 + 用户可覆盖」→ 关掉总管的主对话也落 mainSessionId、会被 getMain 误判为主脑而凭空多出派活工具。两条都证 getMain 复用语义错。**选定②**（同文件、原子写 writeMap 复用、pruneMissing 一处联动；marker 是「某 sid 是主脑」的可持久化事实、必须落盘——不学 run-controllers globalThis）。**承重接线约束（命门·必做）**：`readMap`(session-agent-map.ts:40-46)/`emptyMap`(:22-24)/`pruneMissing`(:64-72) 三处都只重建 `{mainSessionId,bySession}`、静默丢未知顶层字段 → **必须同步改三处保留 `mastermindSessions`**，漏改任一则 marker 写了读不回、idle 主脑哑火无报错（probe 探针确定性坐实，最易偷工）。新增 `markMastermind/isMastermind`（可选 `unmarkMastermind`）**绝不碰** bySession/setOwner/removeOwner/setMain 既有语义（owner-map 红线，单测断言 bySession 不变兜底）。**影响**：marker 与 mainSessionId 第一期对首个会话重合但语义独立；二期「关闭总管 UI」时 unmark 与 setMain 解耦（关总管≠取消主对话）。
- **D-R8.6-10 · T2 wire 落点：独立 wiring 文件 + 服务端写 marker + resolver 主脑分支 + reattach 不 applyRuntime**：**①起会话函数落点 = 新建 `lib/pi/orchestrator-session-wiring.ts`**（仿 profile-session-wiring.ts），放 `startOrchestratorSession`（新建）+`reattachOrchestratorSession`（idle 重建），惰性 `await import("../rpc-manager")` 拿 registerInnerSession 避静态环（profile-session-wiring.ts:289-294 先例）。**不放进 orchestrator-session.ts**——破 A5（spike `/import\(.*rpc-manager.*\)/` 正则连惰性 import 都命中），后者保持纯装配。**②/api/agent/new 分流**：route 内 `if (mastermind === true)`（禁 `!==false`，undefined 会误入）；普通分支 `startRpcSession(tempKey,"",cwd,toolNames)` 一字不动（字节级零回归）；主脑分支调 startOrchestratorSession，**绝不调 setActiveToolsByName**（绕 startRpcSessionInner :362/:374/:380-382 三坑）。**③marker 写时机 = 服务端**（route 拿 realSessionId 后同步 markMastermind，零窗口；否决前端 PATCH 的 ms 级窗口）。**④resolver 主脑分支**（session-reattach.ts）：在 if(agentId) profile 分支之后、`return startGeneric` 之前插 `if (isMastermind(cwd,sessionId)) return reattachOrchestrator(...)`，在 withStartLock 回调内、reattach 走不带锁内层（不双锁）；新增 DI 缝 `isMastermind?`+`reattachOrchestrator?` 供 hermetic 单测。**⑤reattachOrchestratorSession 不调 applyProfileRuntime**——主脑无 profile.model、让内核默认 = 与现状 generic 主会话 re-attach 字节等价零回归（rpc-manager.ts:365-370 generic 本就不传 model）；总管 prompt 抽**模块级常量**，初建与 re-attach 共用同源防现算漂移。**⑥T2 工具桩边界**：submit_plan/dispatch_task 仅返回占位 planId/runId，**禁**落 MastermindRun(T3)/起编排(T4)——T2 是机制地基，可见闭环待 T3+T5。**⑦端到端层形态**：T2 无新 UI，工具激活权威判据放**逻辑层**（route handler + get_tools 代理读 getActiveToolNames）、真浏览器仅作 D-R7B-07「起会话不崩+无 node:fs 报错」兜底 smoke（deepseek 真起会话后 LLM 是否调 submit_plan 不可控、浏览器无确定性工具激活信号）。**依据**：去风险 workflow `wrtzcbbd0` final wirePlan + lead file:line 亲核全部属实。
- **D-R8.6-11 · T3+T4 编排核心批次设计（去风险 workflow `wpjrwmijy` 3 probe + final GO，lead file:line 亲核 spike-2/spike-3 两承重 HOLDS）**：用户拍板 T3+T4 同批次（编排核心·机制层）。lead 六决策（均推荐保守项、承重面积最小化）：**Q1 移除 dispatch_task 工具**——用户点计划卡「确认」= approve 路由（HTTP+结构化+幂等门）是唯一 fire 入口，不给 LLM 越权放行的枪（连带 assembleOrchestratorSessionOptions:187 白名单去名 + 改 ORCHESTRATOR_SYSTEM_PROMPT 去掉「调 dispatch_task」改「等用户在计划卡确认」）。**Q2 teammate→agentId = T4 最小临时造**——submit_plan schema **不加 agentId**（保持主脑「描述队员」自然语义），runMastermind fire 前对每 teammate `AgentProfileStore.create(projectId,{name:role+"-"+uuid8,role,mode})`（:113，uuid 绕 :127 重名 throw）拿 agentId；动态 run 语义**天然依赖临时造**（详设 §5/§10-⑤），M6/T5 补的是 UI+去重非「造」本身。**Q3 临时造 mode = 主脑声明**——submit_plan teammate schema **加 mode?:"doc"|"coding"（默认 doc）**（唯一 schema 扩展），主脑按子任务性质声明（coding 队员真写代码 dispatch-runner.ts:131-136 给 bash、doc 队员受限省安全）。**Q4 reassign = 换池内 agent**——resume(reassign) 收 newAgentId（前端从 AgentProfileStore.list 选）改 stage.agentId+status→pending+retryCount=0 重 fire，**不留桩**（留桩则「选项各分支」AC 不过）；真临时造替补留 T5。**Q5 reject/revise 翻 failed 可回收**——旧 run failedReason="用户否决/打回"、pruneOld 可回收（母版:151 只删终态），主脑据反馈重 submit_plan 产新 randomUUID run。**Q6 三合一保守**：①计划卡 UI **留 T5**（本批次只机制层、approve/reject 走 vitest+curl 验；计划卡与 M5 队员卡片同锚点同文件、分两次改必冲突）②队员 **MVP 串行**（照抄 runPipeline for 串行+累积喂下游 pipeline-orchestrator.ts:141；并行的失败/pause 语义爆炸留二期；「同时 2 队员」靠 acquireSlot 排队物理并发自然发生）③store **新建 `mastermind-run-store.ts`**（守详设不动 PipelineRunStage、独立目录 `.pi/factory/mastermind-runs/`；复用 PipelineRunStore+扩 DispatchStatus 会牵动 dispatch/pipeline 全族类型+第七轮落盘兼容，爆炸半径大、否决）。**承重接线四处非接必塌**（final falseGoTraps + lead 亲核）：①**idle 重建闭包重注入**——orchestrator-session-wiring.ts **两处** buildMastermindTools()（:106 start + :175 reattach）都须改传 {projectId,runStore}，漏 reattach 则 idle 后 submit_plan 又变桩、静默落不了盘（T2 idle gap 在 T3 真逻辑下的延伸）②**approve 幂等门**——非 awaiting_plan_approval 态 approve 返 409/no-op，防重复点覆盖 controller+双 fire 超额占槽 ③**resume 重建 controller**——pause return 时 .finally(deleteRunController) 已删，resume 须 new AbortController+setRunController（对称）+ cancel 加 paused 分支 ④**临时造 agentId**——teammate 无 agentId 直套母版 profileStore.get 必崩（NOT_FOUND→failed）。**projectId 缺口**——ctx 只暴露 cwd 不暴露 projectId（doc-tools.ts:10-11 坐实、闭包注入先例），抽 `resolveProjectIdByCwd(cwd)` helper（复用 lookupProfile session-reattach.ts:63-65 的 registry.list().find normalizeRoot 匹配）在 route/reattach 反查。**spike-3 结构性成立**：orchestrator-session.ts import 仅 typebox/内核/coding-tools、物理够不到 acquireSlot/runMastermind（守此隔离同 A5 命门）。落地顺序（每步一 commit）：store→runMastermind→resolveProjectIdByCwd→submit_plan 真 execute→wiring 注入→approve/reject/revise/resume/cancel/GET 路由。
- **D-R8.6-12 · T5 队员卡片内联·乙 + 计划卡 + 临时角色 UI（去风险 workflow `w0kbl3j1m` 7 agent GO_WITH_MITIGATION，9 falseGoTraps；lead 三重复验 + 独立 verifier 双层验收全 PASS，commit `0536aa6`）**：把主脑派活块内联进主对话消息流（对标 Kimi），复用第七轮卡片族。**两大待设计点终裁**：**①entryId→runId 映射 = 否决独立映射 store，按 runId 从 transcript 就地派生**——runId 由 submit_plan 的 toolResult JSON `{planId,runId,status}` 携带（orchestrator-session.ts:170，randomUUID 运行期生成、不在 toolCall.input），ChatWindow.tsx:375 map 内扫 assistant.content 的 submit_plan toolCall→toolCallId→现成 toolResultsMap→JSON.parse 取 runId（新建纯函数 `lib/mastermind/derive-run-ids.ts`，**filter 非 find**·一条消息可多个 submit_plan）。runId 每次可从 transcript 重算 → 天然满足多 run 自证不串（reject/revise 只置旧 run failed 不删、主脑重 submit_plan 产新 runId）+ 刷新/re-attach 免持久化；**卡片主键用 runId、绝不用 entryIds[idx]**（live turn 期 useAgentSession.ts:295 只 append messages 不 append entryIds、entryIds 更短会 undefined）。**②卡片族吃超集**——详设 §6/§10-① 写的「MastermindStage extends PipelineRunStage」**与代码不符**（mastermind-run-store.ts:58 是独立 interface、status=DispatchStatus|"skipped" 比 PipelineRunStage.status 宽、直传 PipelineStageCard 硬报 TS2322），故新建具名超集 `lib/pipeline/stage-card-stage.ts` StageCardStage（全 import type，node:fs 不进 bundle），三组件 props + PipelineStageCard 两 helper 同步放宽 + 各补 case skipped、点阵前 skipped→done 归一化（不改共享 dot-matrix 签名）；`status-meta.ts` 扩 Record<DispatchStatus|"skipped"> 加 skipped 键（否则 StageHoverPreview:63 索引 skipped→undefined→白屏）。协变保证第七轮传窄 PipelineRunStage 零回归（变异实证：收窄 StageCardStage→tsc 红 3 处 / 删 skipped 键→TS2741）。**其余 5 决策**：③**脱壳防秃卡**——`.brow` CSS + t-kimi token 只在 PipelineBoard 本地未导出的内联 `<style>` 内、作用域 `.pipeline-board.t-kimi-{light|dark}` → surgical 抽出为 exported `components/PipelineBoardStyles.tsx`，内联容器 `MastermindTeammateCards` 包 t-kimi 壳 + 渲它（否则卡片挂载但无样式）；④**轮询 = 单宿主批量**——`MastermindPollDriver` 一个 setInterval 遍历 running 子集（依赖钉 hasRunning 布尔防定时器重建、一处 clearInterval），`selectMastermindNeedsPolling` 仅 running（6 态里只它服务端自改），**首拉 ensureRun 无条件一次 GET 与轮询分离**（否则 awaiting/paused 首帧拿不到 plan/failureOptions）；⑤**错误处置统一** postWithRefetch——任意非 2xx → re-fetch GET 覆盖 + toast（吸收 approve 裸 409{CONFLICT} 与 reject/revise/resume 的 422 不对称）；⑥**成本信号 N = plan.teammates.length**（非 stages.length，awaiting 态 stages 空数组）；⑦**临时角色克制**——`lib/mastermind/friendly-name.ts` 剥 `-[0-9a-f]{8}$` 后缀作显示名，不给 AgentManager 加过滤/删档/徽章（清理留二期，D-R8.6-11 Q2）；GSAP 只做一次性 stagger 入场（守 prefers-reduced-motion，AppShell:204 范式），running 呼吸保留既有 CSS。**AppShell 必要透传**：onOpenSession/onOpenArtifact 从 AppShell 经 ChatWindow 喂内联卡片（进入完整对话/看产物）。**D-R7B-07 守全**：客户端零 node:fs value-import（useMastermindStore 只 import type MastermindRun + fetch）、零 as 强转、未碰 PipelineRunStage 领域类型/内核/T7 按钮 token；真浏览器亮暗双主题 6 态全渲 + skipped hover 不崩 + pageErrors=0（唯一 oracle，hybrid fixture=真主脑 run 拿 transcript+覆写 run JSON 验各态）。
- **D-R8.6-13 · T6 主脑汇总能力 + 长上下文策略（去风险 workflow P1/P2/P4 GO + 专项补做 P3；lead file:line 亲核 + spike-4 真模型端到端坐实，commit `80177eb`）**：给主脑（总管·主会话）加「汇总一次派活运行成果」能力。**S1a 主脑汇总工具集**：wiring.ts 两处（start+reattach）经 `buildDispatchDocTools` 追加 create_artifact + list_artifacts（**无 propose_edit**——主脑只新建汇总、不改既有；四类工具 coding∪submit_plan∪汇总在内核扁平 Set 按名过滤、10 名全不同无冲突）；**🔴D-R7B-07 命门**：buildDispatchDocTools 只在服务端 wiring.ts import，orchestrator-session.ts 保持纯装配不引 node:fs 链（否则 "use client" 值导入即全站 500）；白名单靠 `toolNames=customTools.map(name)` 自动覆盖（规避 D-V2-04 漏名）；**reattach 也须注入**——漏则 idle 重建后主脑静默丢汇总能力（idle gap 与 submit_plan 同源命门，变异对照坐实：删 reattach 注入则 idle gap 用例即红）。**S1b list_artifacts 加相对 filePath**（补 P3-Q4 真缺口：原返回 `{id,title,kind,currentVersion}` 无 filePath → 主脑拿 artifactId 却 read 不到正文、轻读落不了地；主脑 cwd=项目 root、read 吃相对路径 → 给 `a.filePath` 相对路径即可 read；向后兼容）。**S2 汇总纪律 prompt**：ORCHESTRATOR_SYSTEM_PROMPT 追加——list_artifacts 拿元数据+filePath → 按需 read 读要综合的产物正文（**禁全文 N 篇一次性拼接**、防上下文爆炸）→ create_artifact 产汇总受管文档 + 对话文字摘要。**汇总触发 = MVP A**（主脑主会话 LLM 自己汇总·用户触发/主脑本轮；run 是 fire-and-forget headless 队员、主脑独立于 run→只能主脑自己调 list+read+create；自动 nudge 留二期）。**长上下文关键裁决（P3 补做·Q3 终裁 (A)）**：**队员上游 accumulate 不改轻读**——`mastermind-orchestrator.ts:186` formatUpstream 拼所有 done 阶段**产物全文**（注释写「摘要」实为全文、名不副实），末阶段首条 message=前 N-1 阶段全文之和；但 sf-mini 13 阶段在 DeepSeek 100 万 window 下**离撑爆两个数量级余量**（每阶段 2 万字×13≈6.5 万 token≈window 6.5%），改轻读破坏「累积喂下游」核心语义 + 撞 filePath blocker（list_artifacts 曾无 filePath）+ 失败态优雅（阶段 pause 非崩）→ **不改、不加 teammates 上限**（简单优先、避免推测性护栏）、轻读改造留二期。**compaction 内核自带**（P3-Q5 + 记忆 next-step-kernel-compaction-reactive）：auto-compaction 纯反应式（响应返回后才触发、绝不预检 request 大小、**救不了过大的首条 message**）、「一次 compact-and-retry」= 内核 `_overflowRecoveryAttempted` 既有行为逐字对上 §8、默认 enabled reserveTokens=16384 起会话即生效 → **T6 不实现任何 compaction 代码**；spike-4 待验 DeepSeek overflow 错误串能否命中内核 `OVERFLOW_PATTERNS`（命不中则 Case1 兜底空）。**两场景辨析**（P3-Q6）：队员上游=单条 prompt 一次过大·compaction 救不了（靠有界规模）；主脑汇总=多轮历史累积·compaction 正常兜底。**spike-4 真模型坐实**（真 DeepSeek 跑 2 队员「极简计数器」协作）：主脑汇总回合 list_artifacts（返回含 filePath）→ **read「README.md」+ read「计数按钮页面.md」isError=false 真读到产物正文**（我最担心的「主脑 read 相对 filePath 能否读到」假设=真模型坐实为真）→ create_artifact 产「计数器项目汇总」受管文档物化 = **filePath→read→汇总链通、sf-mini 标杆端到端达成**（transcript+截图+受管文档三重铁证）；**🎯教训 again**：spike-4 驱动脚本自动 verdict「a/b/c=FAIL 链不通」是解析错会话结构的 bug、与产物事实矛盾，lead 亲核 transcript 推翻（不信自动检测）。**P3-Q5 补记（spike-4 回报）**：compaction/overflow 真触发**受限未验**——逼 compaction 试了没触发（改 reserveTokens 没被内核 settings 快照吃进 + deepseek 1M 窗口默认阈值~983K、小任务 3K token 够不着）、DeepSeek overflow 串未试（需~1M token 成本过高）；非阻塞（内核机制已源码坐实、正常 sf-mini 规模本就触发不到）。附内核副作用：`setModel` 会 `settingsManager.setDefaultModelAndProvider()` 回写 settings.json（agent-session.js:1113），与 T6 代码无关。**教训**：本项目 transcript 断言须按内核格式（`toolCall` 驼峰 + 独立 `role=toolResult` 消息），别套 Anthropic messages 原生 schema（`tool_use`/`tool_result` 块）——spike-4 驱动脚本正因套错格式致自动 verdict 全 FAIL。
- **D-R8.6-14 · T7 UI 打磨（.btn 四型六态 + 卡片真 bug + a11y；lead 三重复验 + 独立 verifier 真浏览器 A/B/C 全 PASS，commit `1fcd55b`）**：自建 `.btn` 语义类层 + 修流水线卡片真 bug + 补键盘 a11y（方向 D-R8.6-04 不引组件库）。**P0 真缺陷**：①新增 `--color-on-accent`（暗色 primary 文字 #08111f on #60a5fa=**7.44:1** AAA，修 2.54:1 FAIL）②`--color-danger` #ef4444(3.76)→#dc2626(4.83)③status-meta 补 queued 键 + StageHoverPreview 识别 `statusDetail==="queued"`（消卡片显「排队中」/浮窗显「待执行」矛盾）④StageHoverPreview 头徽章 running/done/failed 改 t-kimi token（消双蓝割裂）——**P0④ 安全命门（lead file:line 亲核）**：改共享 STATUS_META 的 .color/.bg 唯一消费方是 StageHoverPreview（恒在 `.pipeline-board.t-kimi` 壳内 token 有值），PipelineBoard:90 只读 .label 不受影响，DispatchPanel:37 用**本地** STATUS_META 不涉及 → 零回归。**P1 系统化**：globals.css 落 `--btn-*` + `.btn` 四型六态（focus-visible 从全项目 1 处→模板级、active inset shadow 不位移邻居、loading 绝对定位 spinner 防宽度跳、**纯建模板无消费者·P2 存量渐进迁**）；PipelineBoardStyles `.brow` 交互升级（hover 硬 outline→平滑 box-shadow 过渡、emoji→chip 徽章、selected 态，**t-kimi token 命名空间一字未改**）；三卡片键盘 a11y（PipelineStageCard role=button+Enter/Space、StageSessionMenu role=dialog+focus 管理+Esc+外点关〔pointerdown 捕获阶段早于父卡 onClick〕、StageHoverPreview role=tooltip+Esc onDismiss）；reduced-motion 守卫覆盖 `.btn`+`.brow`+**T5 遗留未守卫的 pb-breathe/pb-pulse-led**（负对照证守卫非 vacuous）。**不动**（P2/红线）：900 处内联 style 全量迁移、22 处 #ef4444 存量按钮（只改 token+新 .btn）、.brow t-kimi 命名空间、shader 首页、.glass-card/.agent-card、内核。**T5/T7 在 PipelineStageCard/StageHoverPreview/status-meta 交汇**：T5 先 commit（数据/类型/skipped 层）、T7 后 re-read 改（交互/a11y/配色层），两者叠加零冲突。**验收教训**：验 CSS 改动 dev 重启≠重编译——须 `rm -rf .next` 清 Turbopack 陈旧缓存或 curl 核实服务的 CSS（verifier 首验 .btn 全不生效=复用旧 .next 缓存造的假 FAIL、非产品 bug）。
- **D-R8.6-15 · 第二期设计评审终裁（GO_WITH_MITIGATIONS；证伪型读码评审·非 spike；workflow `wf_14c177a5-30d` 7 probe+final 9 traps + lead 亲核 9 条全过）**：两命门不倒（M1-P1 会话活性 / M3-P1 Portal token 白屏均 HOLDS_WITH_CONDITIONS），但揪出**详设两处 REFUTED + 一处遗漏命门**，实现前必修：**（1）M1-P2 REFUTED·nudge 口子方向写反（最高优先）**——详设 §1.2 的 `handleFollowUp` 在 idle 态是黑洞：内核 `followUp()` 纯入队（`pi-agent-core/dist/agent.js:171-173`）、AgentSession 无 idle 守卫也无人 drain（Next-Step 走 `createAgentSession`=AgentSession 非 AgentHarness）、`rpc-manager.ts:194-196` 直通 `inner.followUp`；T6 解耦下 run 期主脑恒 idle → 照详设实现 nudge 全丢、一句小结不冒。**lead 亲核加强发现**：`agent-session.js:697 _handlePostAgentRun` 返回 `hasQueuedMessages()` → idle 入队的 nudge **潜伏到用户下一次任意真回合结束时被连环 drain**（过期小结延迟轰炸、比静默丢更糟）。修正 = nudge 统一走 `handleSend`（`useAgentSession.ts:346-348` 自带 `if(agentRunning)return` guard、idle 起真回合），忙时本轮跳过、由去重集合+下次轮询补发；**绝不用 followUp 做 nudge 排队**（潜伏炸弹）。**（2）M1-P3 REFUTED·去重判据必须 baseline-first**——present-state 判据（`done && !set.has(key)`）在「ChatWindow 切换必 remount（`AppShell.tsx:1043 key={sessionKey}` + :280/298/317/442/461 五处 bump，useRef Set 归空）× module store 跨 remount 满载历史 done（`useMastermindStore.ts:59`）」不对称下，F5/切回会把历史 done 整串重放成真回合。修正 = prev 快照 Map + 首挂只建基线不发 + 只对本地观察到的 pending/running→done 翻转 nudge；`__final__` 用 prevRunStatus 边沿判据 + firedFinal 双保险（防 resume paused→running→done 重发汇总）；覆盖 transcript 派生的**所有** runId（derive 用 filter 禁 find）。终态分支：done/partial nudge 汇总、paused nudge 一句「有队员失败等你处理」、failed（用户主动 cancel/reject）不 nudge。措辞反诱导（阶段小结「只用文字简要汇报、不要调用工具、不要提交新计划」；终态「这是汇总收尾、不要再 submit_plan」+ 携带 runId）；effect 依赖钉 stages[].status 翻转摘要非 messages 长度。**（3）M3-P1 遗漏命门·z-index 倒挂**——`PipelineModal.tsx:108 zIndex:1000` > 浮框（`StageHoverPreview.tsx:112` z:50 / `StageSessionMenu.tsx:172` z:60），Portal 到 body 后看板内浮框成 modal 同胞必被盖住（详设 §3.2/§10-② 全漏）→ Portal wrapper/浮框 z 取 1001~1099（稳过 modal、不压 OnboardingTour z:2000/ModelsConfig z:1100/Toaster z:9999）。方案 c 可行性坐实：`PipelineBoardStyles.tsx:16` 纯 `<style>` 非 styled-jsx scoped → body 级 wrapper 命中选择器 token 有值；wrapper=`pipeline-board t-kimi-{theme}`（**绝不含 board**：`:20 .pipeline-board.board` 才有 background/padding）、position static 无 transform/filter/contain、每浮框实例 useEffect 自建自毁（theme 变更新 className）、mounted 守卫防 SSR；`popover-position.ts` 不动。hover 维持（140ms scheduleHide + 浮框 onMouseEnter 桥接 = 第 8.5 轮 N2 修法）Portal 原样保留即无退化；外点关 `menuRef.contains` + React 树冒泡 stopPropagation Portal 后仍有效（M3-P2 HOLDS）。**（4）§10 其余 lead 拍定**：① M2 纯 UI 承载不改 prompt（等待条独立 data-testid、走 t-kimi token）；③ M4 专属修饰类 `mastermind-board`（PipelineBoardStyles 补 `.pipeline-board.mastermind-board`，background 用 var(--container) 或透明、**绝不铺 var(--bg)**=暗色 #0c0c0e 方向反根因）；④ 集群头「主脑派活」+ `.cnt`「已完成 {done}/{total}」（total=stages.length、done=filter done；queued 靠卡片本体、不进头计数；fork SVG 直接复制 `PipelineBoard.tsx:137-153` 勿抽共用组件）；⑤ role 落 stage 3 处纯增量（`MastermindStage`/`StageCardStage` 加 `role?:string` + `approve/route.ts:38-50` 映射补 `role:t.role`——亲核坐实 11 字段独漏 role、schema role 必填早就在、readRun 无校验旧数据零迁移）+ friendlyAgentName 漏用补 4 位置（`StageHoverPreview.tsx:156` + `StageSessionMenu.tsx:190/280/293`）；⑥⑦ 中文人名池/走马灯/创建角色卡/全局进度头条本期全不做；⑨ M7 排最后一步、倾向留三期。**（5）M6 档 2 具体形态（用户拍 D-V1.2-87）**：plan 加 `execution?: "parallel"|"serial"`（主脑声明、默认 serial 保留累积喂下游）；parallel 批内 Promise 并行 + acquireSlot 排队兜底（超 limit 自然 queued）+ 全 settle 统一判定（任一失败→整批 settle 后 pauseRun、部分成功→partial 语义沿用）；连带修 `mastermind-run-store.ts:244` tmp 竞态（`tmp-${pid}` 加唯一后缀）；clamp 显式复用 `factory-config.ts` 导出（抽 `clampConcurrent` 两处共用、禁裸传 0/负防 `timeoutMs:Infinity` 永久挂起）；limit 语义 = 进程级活会话总上限（含主脑、计划卡注明）、不做动态基线补偿。**评审方法论**：7/7 probe 全产出无占位失效（对抗融进 probe + final falseGoTraps + lead 亲核三层，第一期 D-R8.6-06 教训的落地闭环）；本期全程读码评审未搭 spike（守 [[评审 vs spike]] 边界、M7 才 spike）。
- **D-R8.6-16 · nudge 快照「吸收新 key」修复（真浏览器端到端揪出的设计缺陷；`computeNudges` 基线语义补全）**：T1 收官后端到端验收发现 `stageNudges:0`——verifier 逻辑层 repro（4 步序列：空基线→run 出现→stage done→run done 全程零发）+ lead 推演坐实**真根因**：旧实现「无翻转时 `snapshot: prev` 原样返回」×「翻转判据首见略过（`before !== undefined` 才算）」组合下，**基线建立之后才诞生的 run 永远进不了快照**、其每次状态变化都是「首见」→ nudge 全哑。而「会话中途 submit_plan 新建 run」（组件挂载在先、run 诞生在后）正是**主流程必踩**，非边缘 case；21 个单测全绿漏网原因=所有用例的 run 都在 `prev===null` 基线轮已存在（评审 M1-P3 的场景矩阵集中在 F5/切回、也没抓到「run 中途诞生」）。**修法**：`absorbed = { ...full, ...prev }`——prev 已有 key 保旧值（未发的挂起翻转留待补发、忙时不丢）、full 独有新 key 以**现值**吸收为「它自己的基线」（吸收本身不发：run 首见即带的 done 历史态不重放、与 baseline-first 语义统一；代价=run 出现同 tick 已 done 的 stage 漏一条小结，2s 轮询窗口概率低 + 终态汇总兜底、可接受）；无翻转/忙返回 absorbed、fire 后基于 absorbed 推进单 key。+4 回归测试（verifier repro 完整序列/首见含 done 不重放/忙时吸收/双 run 中途加入挂起不丢）：test 633→637；变异（absorbed 退回 `{...prev}`）→ 恰 4 新测试红、恢复绿。**教训**：①「真浏览器端到端是权威 oracle」再次应验——逻辑层 21 测试 + 设计评审都没拦住这个主流程缺陷，端到端第一跑就暴露；②纯函数快照语义里「返回旧快照」与「首见略过」各自正确、组合致命——状态机测试矩阵必须含「实体在观察者就位之后才诞生」的时序。
