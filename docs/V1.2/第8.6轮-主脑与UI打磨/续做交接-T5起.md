# 第 8.6 轮 · 续做交接（T5 / T6 / T7）

> **新窗口读这一页即可接 T5**。机制层地基已全就位，剩下是 UI（T5/T7）+ 真模型验（T6）。

## 一、现状（到哪了）

主脑「大脑」**机制层全部完成**，本地 v1.2 领先 `origin/v1.2`(`2312031`) **7 commit、未 push**：

| 卡 | 内容 | commit |
|---|---|---|
| T1 | spike-1 主会话装派活工具可行 | `cce1448` |
| T2 | 主脑会话装配 + 起会话链分支 + idle gap | `c834cfb` |
| T3+T4 | 计划确认闸 + 主脑编排器失败处理 | `63e06bc` |

**已就位的机制层**（T5 UI 直接消费、不用再动）：
- `lib/domain/mastermind-run-store.ts`：`MastermindRun`（6 态 awaiting_plan_approval/running/done/failed/paused/partial）+ store。
- `lib/domain/mastermind-orchestrator.ts`：`runMastermind`（串行起队员 + retry1→paused→选项 + resume 从 checkpoint + 临时造 agentId + partial）。
- 路由：`POST /api/projects/[id]/mastermind/runs/[runId]/{approve,reject,revise,resume}`、`GET/DELETE(cancel) /api/mastermind-runs/[runId]`。
- `submit_plan` 工具真落 `awaiting_plan_approval`（主脑调它就产一个 run）。
- 决策：**ADR D-R8.6-09/10/11**（`docs/V1.2/设计决策记录.md`，先读这三条）。

## 二、剩 3 卡（都已在三件套 speced，从「实现」切入 + 双层验收，不用重走设计）

### T5 · 队员卡片内联·乙 + 计划卡 UI + 临时造角色 UI（🔴UI承重）
详设 **§6 M5**。本轮最大一张。三件事一起做（同锚点、避免两次改同文件）：
1. **队员卡片内联**（对标 Kimi、冒主脑回复下）：`ChatWindow.tsx:375-421` 的 `messages.map` 旁挂；**复用第七轮卡片族** `PipelineStageCard`/`StageHoverPreview`/`StageSessionMenu`；数据契约 `MastermindStage extends PipelineRunStage`（卡片族吃超集）。
2. **计划卡 UI**（Q6 并入 T5）：渲 `plan.teammates` + 验收点 + 成本感知「派 N 个队员」+ **确认/否决/打回**按钮（fetch approve/reject/revise）；paused 时渲 `failureOptions` → **retry/reassign/skip/abort**（fetch resume，reassign 前端从 `AgentProfileStore.list` 选 newAgentId）。
3. **临时造角色 UI**：造本身 T4 已做（runMastermind fire 时），T5 补「看得到临时队员 + 去重/清理」。
- **母版**：`lib/stores/usePipelineStore.ts`（`import type` + fetch JSON，**D-R7B-07 铁律**）→ 新建 `useMastermindStore`。
- **⚠️待设计（T5 第一件事）**：**entryId→runId 映射**。UI 怎么知道对话里哪条消息该挂哪个 run 的卡？submit_plan 的工具调用结果里有 runId → useMastermindStore 按「对话 entry(submit_plan tool call) → runId」插卡，再 `GET /api/mastermind-runs/[runId]` 轮询 run 渲卡。这块 §6 只给了方向、要 T5 落。
- 红线 **D-R7B-07**：`mastermind-orchestrator`/`mastermind-run-store` 含 node:fs，UI **只 import type + fetch**，绝不 value-import（第七轮 anchor 前科：一 value-import 全站 500）。**真浏览器验收兜底**。
- GSAP 克制动效（守 `prefers-reduced-motion`，D-R8.6-02）。

### T6 · 长上下文 + spike-4 真模型（🔴承重·spike-4）
详设 **§8 M7**。依赖 T5（编排 + UI 就位）。
- 长上下文策略大半 T4 已实现（队员短命会话 + 跑完即 evict + 上游 accumulate-into-prompt）；剩「主脑汇总走轻读」。
- **⚠️待决**：**主脑汇总要产受管文档（create_artifact）**，但主脑会话工具集现是 `CODING_TOOL_NAMES + submit_plan`、**无 create_artifact/list_artifacts**（`orchestrator-session.ts:199`）。T6 需定：给主脑加提议工具做汇总，还是主脑用 write 直写（非受管）。
- spike-4 真模型：本机 `~/.pi/agent` 有 **deepseek 凭证**，端到端跑主脑长流程（对标 sf-mini）验 compaction 真触发不崩 + context% + heap。**sf-mini 标杆 = 产「好看好用可运行 UI 原型」**。

### T7 · UI 打磨（M8）
详设 **§9 M8** + 需求 §三。依赖 **T5 先 commit**（同改 `PipelineStageCard`、T7 再 re-read 改、避冲突）。
- `.btn` 四型六态 token/类（P0 真缺陷：暗色主按钮对比度 2.54:1 FAIL）+ 修流水线卡片真 bug（queued 矛盾/双蓝割裂/hover 硬切/键盘不可达/Esc/reduced-motion）+ 补 a11y。

## 三、怎么接上（读这些）

1. `tasks/V1.2/第8.6轮-主脑与UI打磨/progress.md`（T2/T3/T4 收官详情 + 剩卡）。
2. memory `next-step-v1.2-r86-mastermind`（状态 + T5 起点 + 踩坑）。
3. 三件套 `docs/V1.2/第8.6轮-主脑与UI打磨/详细设计.md` **§6 M5 / §8 M7 / §9 M8** + ADR **D-R8.6-01~11**。
4. Kimi 参考截图：仓库外层 `一些8.6轮来自kimi的截图/`（队员卡片形态）。

## 四、流程纪律（沿用本轮）

- **UI 卡（T5/T7）验收必走真浏览器**（`browser-e2e` skill）——SSR/hydration/node:fs 泄漏只在真浏览器暴露。
- 承重卡走 ultracode 去风险（自带证伪 probe + final falseGoTraps，**不靠独立 adversary**，§12④）→ agent team 实现 → **lead 三重复验**（独立门禁 + 亲读非 vacuous + 变异命门）→ 独立 verifier → 端到端。
- 决策记 QA/ADR；**commit / push 等用户授权**（本地已攒 7 commit 待 push）。
- **⚠️坑**：① task 系统跑 agent team 会被清空（靠 git/progress 判进度，非 idle 通知）② 亲验每卡 diff 要**全量看** git status、别凭符号 grep 漏文件 ③ 未跟踪新文件不能 `git checkout` 还原、变异测试用备份法 ④ 杀 dev 用 `fuser -k 30141/tcp`、别 `pkill -f`。
- **工作区隔离**：`docs/V1.2/第九轮-查缺补漏/`、`tasks/V1.2/第九轮-查缺补漏/`、`scripts/verify-*.mjs/.mts/.sh` 是第九轮草稿/驱动脚本，**与本轮无关、别纳入 commit**。
