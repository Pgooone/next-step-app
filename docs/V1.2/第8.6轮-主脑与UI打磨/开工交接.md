# 第 8.6 轮 · 主脑（主会话即总管）+ UI 打磨 · 开工交接

> **新窗口读这一页即可接 T2**。配合 memory `next-step-v1.2-r86-mastermind` + 本目录三件套。

## 一、现状（到哪了）

vibe-coding 全走完到 **T1**：立项 → 技术调研 → 概要设计 → 详细设计 → 设计评审 → 划分任务 → **T1 spike-1 命门 GO**。

**commit 链**（本地 v1.2，**未 push**，基线 `2312031`）：
- `57589e7` 立项 / `0d8b74a` 技术调研+概要 / `07a53e8` 详设 / `cce1448` **T1 spike-1 GO**

**spike-1 GO**：主会话装「总管 resourceLoader + 派活 customTools」可行、命门坐实（lead 三重复验：复跑 5/5 + 亲读非 vacuous + 变异 A2/A3 即红即绿）。产物 `lib/pi/orchestrator-session.ts`（M1 雏形）+ `orchestrator-session.spike.test.ts`（5 断言）。

## 二、T2 起点（从这接）

**T2 = 起会话链接线 + idle gap 处理**（详设 §2 M2 + §12②）：
1. `/api/agent/new` 加「主脑模式」分支，装 `assembleOrchestratorSessionOptions`（M1 雏形已有）的 resourceLoader + customTools。**走独立装配函数、不硬塞 `startRpcSessionInner`**——撞 `rpc-manager.ts:380-382`（空工具清 prompt）/ `:361-375`（toolNames 纠缠）；子路线倾向 **A**。普通主会话**零回归**（强变异对照）。
2. **idle 重建 gap**（D-R8.6-07，承重）：主脑会话 idle 重建落 startGeneric 丢派活工具+总管 prompt → 纳入 re-attach 分流（复用第五轮 `resolveOrReattachSession` 机制 D-R5-*）。
3. A3 加固：迁正式单测时加「stopReason 非 timeout」断言。

后续 T3~T7 见 progress.md（计划确认闸 / 编排器失败处理 / 队员卡片内联·乙 / 长上下文 / UI 打磨）。

## 三、关键决策 / 红线

- **路线 C**（工具派活 + 计划模式确认，对标 Claude Code）；**UI 乙**（卡片冒主脑回复下、对标 Kimi）。
- 不改内核 / 守 **D-R7B-07**（新数据通道只 `import type` + fetch、机制层留服务端、真浏览器验收兜底）/ 并发 ≤3 复用 `acquireSlot` 传 **Infinity** / **只走 pipeline 族否决 dispatch** / owner-map 红线。
- 打破 V2「不碰 /api/agent/new」红线属本轮**有意**（走新分支隔离普通主会话，ADR D-R8.6-05）。

## 四、怎么接上（读这些）

- memory `next-step-v1.2-r86-mastermind`（状态 + T2 起点）。
- 三件套 `docs/V1.2/第8.6轮-主脑与UI打磨/`（需求 / 概要 / 详细设计；详设 **§1** spike-1 + **§12** 设计评审）。
- 任务卡 `tasks/V1.2/第8.6轮-主脑与UI打磨/progress.md`（T1~T7 + 双层验收）。
- 决策 QA `docs/V1.2/QA/第8.6轮-{主脑,UI优化}决策.md`（D-V1.2-74~84）+ ADR `docs/V1.2/设计决策记录.md`（D-R8.6-01~08）。
- Kimi 参考截图：**仓库外层** `一些8.6轮来自kimi的截图/`（4 张，含 1664×11400 完整案例）。

## 五、流程纪律

- **agent team 实现 + lead 三重复验**（独立复跑 + 亲读非 vacuous + 变异命门）；**UI 卡走真浏览器**；主脑端到端走 **sf-mini 标杆**（产「好看好用可运行的 UI 原型」）。
- ⚠️ **workflow 对抗用扁平单字符串 schema**（嵌套数组 + 长文本会触发 StructuredOutput 编码 bug → 占位 'test'；见 memory `workflow-schema-ascii-keys`②）；对抗优先内建进 probe（refutationAttempt）+ final（falseGoTraps），lead 变异最权威。
- 决策记 QA/ADR；**commit / push 等用户授权**。
- **未 push**（4 commit 在本地 v1.2）；README 总索引第 8.6 轮条目因第九轮草稿占用 `README.md` 未更新（待第九轮一起补）。
