# 第七轮 三件套（需求/概要/详细设计）· 对抗式审查报告

> 2026-06-27，三件套起草后用 ultracode（7 agent：并行抽依据 → 顺序起草 → 对抗式覆盖审查）产出。
> **处置批注（lead）**：
> - **R1（合并入口 A/B/三文档口径不一）→ 已定**：用户拍板 **B 一个入口·两 tab（流水线 / 快速派发）**，三文档已统一（spec-fixer 修订）。
> - **R2（doc-session 白名单行号/项数事实错误）→ 已 lead 读码核准**：真值 = `DISPATCH_DOC_SESSION_TOOLS` **6 项**（无 propose_edit）@`doc-session.ts:44`；装配函数 `assembleDispatchDocSessionOptions`@`:85`；dispatch-runner 装配点 `:131-134`；对照 `DOC_SESSION_TOOLS`（7 项含 propose_edit）@`:28` 是交互式会话用、勿混。三文档已按真值修订。
> - **C1/C2 → 已修订**（progress 离散三档映射 / currentStageIndex 排队态时机）。

---

**总结论：可开工（走 T1 承重墙）。** 三件套覆盖度高、跨文档基本自洽、红线守得住、引用绝大多数经 Read 实证。

## ① 覆盖矩阵
- **决策 D-V1.2-28~47：20/20 全覆盖**（需求表 + 概要选型表 + 详细文件总表三层可对上）。
- **审计 15 缺口：15/15 全覆盖**（进会话只读守卫 / 互饿·TOCTOU 由冻结模型化解 / acquireSlot 超时 / cancel / 累积上游 / F10 产物保障 / 孤儿对账 / agent 删降级 / 蓝图校验 / 失败态 / 不回灌 / runs 清理 …）。

## ② 跨文档一致性（除 R1 外基本自洽）
- C1 · progress→点阵：需求 AC-10 像连续进度、详细 §3.6 实为离散三档 → 已对齐为离散三档。
- C2 · `currentStageIndex` 排队态时机：概要(acquireSlot 后) vs 详细(acquireSlot 前)打架 → 统一为详细版（排队即指向该阶段）。
- C3 · `statusDetail` 表述差异（可选字段），语义一致，忽略。

## ③ 红线合规：8/8 守住
- 并发≤3 改可配 + 同步 CLAUDE.md（acquireSlot `limit ?? readMaxConcurrent()`、Infinity 走现逻辑，`concurrency-gate.ts:39/40/43-49` 天然成立、零改本体）✅
- headless worker 不放 propose_edit（复用 6 项白名单，见 R2）✅
- 不改 pi 内核 ✅ ｜ 纯文件无 DB（atomicWrite 仿 `dispatch-store.ts:134-138`）✅
- 不拖 node:fs 进客户端 bundle（D-R7B-07：纯算法抽离、客户端只 import type + fetch JSON）✅
- evict 不碰 owner map（`evict-agent-sessions.ts:11-13` 红线注释）✅ ｜ re-attach 装回 propose_edit 合法（D-R5-06）✅ ｜ 看板只读、cancel 是唯一写口 ✅

## ④ 臆造核查
- 承重 file:line **绝大多数经 Read 实证为真**（concurrency-gate / dispatch-store / evict-agent-sessions / rpc-manager / dispatch-runner / orchestrator / AppShell / DispatchPanel / 两 dispatch API 路由 / D-R5-06 / D-R7B-07 ADR 均实证存在）。**未发现凭空臆造。**
- 小偏差（实现期 lead 核，非阻塞）：`session-reattach.ts` 分支行号轻微漂移（lookupProfile 实在 `:104`/定义 `:52`、reattach 分支 `:108`）；`profile-session-wiring.test.ts` reattach 工具集断言用 `:426`（含 create_artifact/propose_edit/list_artifacts）而非 `:576`(describe 块开头)。
- 草稿自标「待 lead 核」8 项（dispatch API handler 形状 / 第二轮版本下拉组件路径 / AppShell 切会话 handler / agent 列表 hook / 只读 transcript 组件 / DispatchForm 子任务 setter 名 / runControllers 落点 / factory-config 落点）+ 2 项待 ADR（N=20/M=50、dot-matrix 映射）——标注得当，实现期处理。

## ⑤ 承重墙（写清、可执行、lead 可独立复跑）
- **T1-A 冻结释槽**（AC-2/3 命门）：现 orchestrator completed 路径**全程无 destroy/evict**（`orchestrator.ts:156-198` + `dispatch-runner.ts:161-163` 实证）→ 占槽到 `rpc-manager.ts:52` 10min idle。新增 evict 后释槽是真增量。验法 = faux registry/activeCount DI + faux runWorker（留假 inner 模拟 completed 不自销毁），断言每阶段 evict 后 activeSessionCount 归零、下阶段 acquireSlot 立即放行。
- **T1-B re-attach 续聊**（AC-12 命门）：D-R5-06 实证（dispatch worker 会话 idle re-attach 得 doc 工具集）；本轮差异（idle→主动 evict）二者都只 destroy wrapper、不删 jsonl（`evict-agent-sessions.ts:14-16`）→ 链成立推理扎实。不绿则降级「进入完整对话」为只读、AC-12 改判需用户确认，不阻塞其余 MVP。

## 待补清单
1. ✅【已处理·用户】R1 合并入口 → B 一个入口·两 tab。
2. ✅【已处理·lead 读码】R2 doc-session 白名单真值修正。
3. ✅【已处理】C1 离散映射 / C2 currentStageIndex 时机。
4. 【实现期·lead 核】④ 行号校正 + 草稿自标 8 项「待核」+ 2 项待 ADR——均标注得当，无需开工前处理。

**审查员判定**：扣除 R1/R2（均已处理）后，三件套质量高、可作为开工依据。
