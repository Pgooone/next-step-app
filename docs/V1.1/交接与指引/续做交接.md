# Next-Step V1.1 续做交接（新窗口从这里开始）

> 上个窗口在内存受限（本机 **3.4G 无 swap**）下做完了 M1–M7，剩 **M8 + 端到端收尾**。
> 因「多 agent + dev server 并行顶不住」，拆到新窗口、**低并发**完成。先读这份，再读 `开工指引.md`。

## 一、现状（已完成，全部已提交到 v1.1 分支）

| 模块 | 状态 | commit | 验收 |
|---|---|---|---|
| M1 删除确认去 UUID | ✅ | `158c807` | 逻辑门禁 |
| M2 文本文件上传 | ✅ | `69ff09e` | 真浏览器双层 PASS |
| M3 右看/左确认提示 | ✅ | `7485f8d` | 真浏览器双层 PASS |
| M5 会话↔agent 映射(承重墙) | ✅ | `cb7181b` | API 烟测 PASS |
| M6 项目卡片墙首页 | ✅ | `8e233e7` | 真浏览器 7/7 PASS |
| M4 玻璃卡片重写 | ✅ | `38f1534`(+`dfc57e2`) | 真浏览器 8/8 PASS |
| M7 主对话+归属+左栏分组 | ✅ 代码 | `e129e53` | **真浏览器 E2E：见下「待确认」** |

- 崩溃恢复检查点 `b5f30fe`（M4 助手 + M7 分组纯函数）；决策 `df8614c`（D-V1.1-05/06/07）。
- 决策已锁 **D-V1.1-01~09**，见 `docs/设计决策记录.md`。
- `tasks/第一轮-基础迭代/progress.md` 已勾到 M4；M7 待真浏览器过后勾。

> **M7 真浏览器 E2E：部分完成，新窗口须补完**（上个窗口因内存被打断）：
> - ✅ **5.2 标 main 已实测**（独立诊断：发首条主对话后 ~2s `mainSessionId` 正确标到新会话）。
> - ⏳ **5.3（服务端 setOwner 浏览器落地）/ 5.4（三区渲染）/ 防串显 未跑** → 新窗口用 `scripts/m7-e2e-drive.mjs`（种子锁 cwd + 轮询版）补完。5.3 代码在 `agents/[agentId]/session/route.ts:41`、领域 setOwner 有 16 单测；5.4/防串显 是 UI 须真浏览器看。
>
> **M7 E2E 续验四坑（务必照做）**：
> 1. **新空项目 selectedCwd 会 fallback 到全局最近会话 cwd**（SessionSidebar 按 cwd 选、非 projectId）→ 测试环境孤儿会话会让新项目发消息用错 cwd → `/api/agent/new` 400。**必须「种子会话锁 cwd」**：给测试项目预建一个 cwd=项目 root 的会话，`?session=` 恢复它锁定 selectedCwd，再 New+发消息。
> 2. `setMain`/`setOwner` **异步** → 轮询 session-map 期望态，别定长 wait（假阴性）。
> 3. **faux 模型别显式传**（provider:faux→500 Model not found）；新建会话不带 provider/modelId（用内核默认）或用 deepseek 真模型。
> 4. **dev server 涨到 ~1.1GB** → 重 AC 组间重启它（见内存铁律）。
>
> **一个设计点（lead 已判：非 M7 缺陷、本期不修、仅记录）**：SessionSidebar 会话列表按 **cwd** 过滤、非 projectId（M7 的归属 map 已按 projectId 隔离，会话列表沿用 pi-web 既有 cwd 过滤）。单项目 cwd≈project 不出问题；多项目+新空项目时会借用别的 cwd。这是 **pi-web 既有耦合**（`docs/第一轮-基础迭代/后续迭代清单.md` #4），**非 M7 引入**；验防串显时用坑 1 的种子法规避即可。

## 二、剩余任务（新窗口做）

1. **补完 M7 真浏览器 E2E**（5.3/5.4/防串显，复用 `scripts/m7-e2e-drive.mjs`、照上方四坑），过了在 `progress.md` 勾 M7。
2. **M8 · at-agent-transfer**（最后一个模块）：实现 + 单测 + 三门禁 + 真浏览器 E2E + 提交。
3. **端到端验收**（全链路，见第四节）。
4. **文档回写**：`progress.md` 勾 M8、`docs/设计决策记录.md` 记 M8 期新决策（若有）、相关区 README。
5. **`git push origin v1.1`**（阶段性推送）。

## 三、M8 怎么做（指针 + 复用点）

- 规格：`tasks/第一轮-基础迭代/M8-at-agent.md` + `docs/第一轮-基础迭代/详细设计.md`·M8；决策 **D-V1.1-03**（载荷=`<context>`+`<file>` 作首条消息）、**D-V1.1-04**（投递=起新会话 `POST /api/projects/[id]/agents/[agentId]/session`，载荷作 `firstMessage`）。
- 文件：`components/ChatInput.tsx`（@ 触发 + 转交内容勾选）+ 转交逻辑。
- **直接复用（已就绪）**：
  - `lib/chat-file-attach.ts`：`composeMessageWithFiles(message, attachments)`、`buildFileBlock(name, content)`（M8 组装 `<file>` 内联直接用）。
  - 起会话路由 `app/api/projects/[id]/agents/[agentId]/session/route.ts`：**M7 已在此加服务端 `setOwner`** → M8 转交起的新会话**自动归属到目标 agent**、自动进其左栏分组，无需 M8 再标。
  - `handleAgentSessionStarted`（AppShell）：切到新会话 + 接 SSE，M8 投递后复用它。
- 关键点：历史序列化保留角色标注（user/assistant/工具）且目标会话可见；默认带「全主对话历史+附件」、发送时可现场勾选增减；**确认 @agent 与 Dispatch 并存**。
- 防滑坡：M8 是**跨会话异步转交**，非「同窗口实时 agent team」；若要演进成实时须回找用户拍板。

## 四、端到端验收清单（M8 后）

项目墙 → 进项目落主对话 → 建 agent → 给 agent 起会话（会话归到该 agent 分组）→ 主对话 `@agent` 转交（带历史+附件、可勾选）→ 进目标 agent 单独新会话 → 产物按块确认全链路；三门禁全绿 + 真浏览器全绿。

## 五、⚠️ 内存铁律（本机 3.4G 无 swap，已崩过一次 OOM）

**核心：一次只跑一个重活，少开 agent。**
- **不要并行重活**：实现跑门禁(vitest/tsc 峰值~500MB) 与 E2E 的 dev+chromium **绝不同时**。
- **dev server 会涨到 ~1.1GB**（Next dev 编译路由累积）→ E2E 重 AC 组间 **kill next dev 重起** 重置回 ~300MB。
- **E2E**：单 chromium、每用例 `close()`、跑完拆 dev+chromium 腾内存；用 faux 模型（轻、不依赖真凭证）。
- **挂内存看门狗**：`free -m` available 跌破 ~300MB 就减载（Monitor 转换式发事件）。
- **`pkill -f` 别用含 'next dev'/'chromium' 的模式**（会杀到自己的 shell，exit 144）；按端口（`ss`/`fuser -k 30141/tcp`）或 PID 杀。
- **建议编排（低并发）**：lead 直接做 M8 实现 + 1 个独立 verifier 跑 E2E（实现者不自证）；或最多 lead + 1 impl + 1 idle verifier。**别再起 5 个并行 teammate**。
- 崩了不慌：committed 安全、靠 `git status` 看落盘真相、`TeamCreate` 新名（旧名残留用 `-r2`/`-r3`）重建续做。详见项目记忆 `next-step-local-oom-constraint` / `next-step-agent-team-recovery`。

## 六、约定
- **每完成一个 task 就单独 commit**（细粒度便于回退，用户要求）；中文 commit、按 `CLAUDE.md`。
- 门禁：`npm run test` + `node_modules/.bin/tsc --noEmit` + `npm run lint` 全绿；UI 走真浏览器。
- 红线：不改 pi 内核、产物按块确认、纯文件、并发会话≤3、复用 pi-web。
