# V2 · vibe-coding 拆任务启动决策（2026-06-18）

> 承接「受管文档架构反思-AFFiNE对照」轮次 1~5（收敛模型定型）。用户在新窗口拍「开 vibe-coding 拆任务」，本文记拆任务启动时的 3 个**用户拍板**（选项 + 取舍）。
> lead 自主的 3 条实现级取舍记 `../../../设计决策记录.md` D-V2-01~03。

## 决策1 · 本次范围
- **背景**：V2 含核心闭环（提议工具）+ PDF/Word 解析（轮次 4/5 定为"入口必需但真没建"的独立工作线）。本次拆哪些？
- **选项**：A 只拆核心闭环、解析后置 / B 连解析一起拆。
- **推荐**：A（先立承重墙、不引新依赖）。
- **谁拍**：用户。
- **最终**：**A 只拆核心闭环**（create/propose/list + 物化 crd.md + 删 guard + 复用 D3-D5）；PDF/Word 解析作后续独立工作线单独拆。

## 决策2 · 文档形态 + 目录重组
- **背景**：需求/设计已在 QA 轮次 1~5 定型，本次 vibe-coding 文档怎么产、怎么放？
- **选项**：A 复用 QA 源、只产精炼设计/任务卡 / B 重做标准三件套 / C 折中补详细设计。
- **推荐**：A（避免把已拍板结论重抄）。
- **谁拍**：用户。
- **最终**：**B 重做标准三件套**，并**按轮重组目录**——第一轮（M1~M8/bugfix/P0）产物归档、第二轮另起文件夹，检查修复关联引用。

## 决策3 · 目录结构形态
- **背景**：按轮重组的具体结构？
- **选项**：A docs/tasks 各自内部分轮 / B 一轮一个自包含总文件夹 / C 只建第二轮、第一轮不动。
- **推荐**：A（保持 docs/+tasks/ 顶层约定、改动可控）。
- **谁拍**：用户。
- **最终**：**A**。命名「第一轮-基础迭代 / 第二轮-V2提议工具」；QA/ 与 设计决策记录.md 跨轮留 docs 根；约 30 处引用已同步修复（grep 零残留）。

## 成果
- 目录重组 + 引用修复（commit `31f2064`）；CLAUDE.md 文档索引升级两轮结构（git 外）。
- V2 三件套（commit `d343df6`）：`../../../第二轮-V2提议工具/{需求文档,概要设计,详细设计}.md`。
- V2 任务卡 + 监工 prompt（commit `035b818`）：`../../../../../tasks/V1.1/第二轮-V2提议工具/`（V2-0 spike + V2-1~V2-6 + progress + prompt）。
- **下一步**：实现阶段用 **agent team**（监工 prompt 已明确：TeamCreate 可寻址队员、串行）；**⚠️ 前置须 `/login`** 解决本地 401 无凭证（本轮 Explore 勘察 agent 已实测 401 失败）。
