# T4 · 文案中文化 C2/C12 · C7 · C11 · C14（small）

> 设计依据：`前端设计.md` §5 + 台账 D-V1.2-62。纯文案/小逻辑，无产品取舍。

## 改动点
- **C2/C12 · 欢迎短语 + 打字机**（`ChatWindow.tsx`）：
  - `:105` `TYPEWRITER_PHRASES` 整组换 8 条中文：派一次多 Agent 协作 / 写一份需求文档 / 让架构师出技术方案 / 审查这段实现 / 拆解一个新功能 / 为项目排条流水线 / 总结这份文档 / 和某个 Agent 单聊。
  - `:146` `current.slice(0,n±1)` → 按**码点**切（`Array.from(current).slice(...).join("")`，防 emoji/生僻字切半）。
  - `:147` 打字 `55 → 90ms/字`；删 28 / 停 1800 / 光标 530 不变。
- **C7**（`DispatchPanel.tsx:282`）：人数 label 下补灰字 helper「最多 3 个：受并发上限约束，可在 `~/.pi/factory-config.json` 调整。」（勿混「子任务≤3」与「全局并发≤3」两个 3）
- **C11**（`AgentManager.tsx`）：`:1078-1118`「文档型」按钮加「推荐」角标；`:1127-1129` helper 改场景化「文档型（推荐）：写需求/设计/文档等产物，改动按块确认更安全（不写盘、不跑命令）。编码型：直接读写文件、跑命令，适合落地写代码的角色。」
- **C14 · 界面汉化**（单列集中扫，**不引 i18n**）：grep `components/` JSX 文本 + `title`，照 spec §5.4 译表替换（New→新建 / Send→发送 / Export→导出 / System→系统提示 / Branches→分支 / Compact→压缩 / Copy message→复制消息 / Rename·Delete·Close / **Explorer→文件树** / Refresh·Refresh explorer→刷新·刷新文件树 / auto·default→自动·默认 / New session in…→在…新建会话 / Select a project first→请先选择项目）。✅ **订正（A4-EXPLORER）**：`SessionSidebar.tsx:846` 有可见文本 `Explorer`、`:835 textTransform:uppercase` 渲染成大写 **EXPLORER**（用户记得的形态）→ **必须汉化、勿被旧注「不存在」误导跳过**（漏了 AC3 FAIL）。

## AC（真浏览器）
1. 对话空态打字机跑中文短语、**90ms、逐字无半字乱码**。
2. 派发有人数 helper；Agent 模式有「推荐」角标 + 场景化 helper。
3. 顶栏/会话栏/输入框英文标签均已汉化、无明显遗漏（**含文件区 EXPLORER 大写标题已汉化**）。
4. `pageErrors=0`。

## DoD
lint/test/tsc 绿；AC PASS；回写 progress。

## 依赖
独立。可与 T2/T3 并行（不同文件）。
