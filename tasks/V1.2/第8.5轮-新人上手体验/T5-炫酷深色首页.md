# T5 · 炫酷深色首页（shader 背景 · 试点 · medium · 新依赖）

> 设计依据：`前端设计.md` §6 + 台账 D-V1.2-63。**仅首页深色试点 · 工作台不动 · 可回退**。

## 目标
首页（项目墙）改 **WebGL shader 流光背景 + 深色玻璃首页**，字体 **B·优雅衬线**，带试点开关可一键回退；性能/降级护栏齐全。

## 子任务（checklist）
- [ ] `npm i three @types/three`。
- [ ] 新建 `components/shader-animation.tsx`（用户提供组件；改成背景：`position:absolute;inset:0;z-index:0`、`time += 0.018`、`devicePixelRatio` 封顶 2）。
- [ ] **性能/降级护栏**：① `visibilitychange`/离开首页 `cancelAnimationFrame`、回来再起；② `prefers-reduced-motion` → 静态渐变/单帧兜底；③ WebGL 不可用 → 回退纯深色渐变。
- [ ] **字体 B 自托管**：下载 `Instrument Serif` + `Noto Serif SC`（必要子集）woff2 放 `app/fonts`，用 `next/font/local` 引；**禁用 `next/font/google`**（容器 build 取不到 Google Fonts）。品牌/UI 用 `Space Grotesk`（亦自托管）。
  - **获取步骤（开工前 prep·A3-1）**：`--noproxy '*'` 直连 `fonts.gstatic.com` 下载（CLAUDE.md:14 容器可绕代理直连）+ `pyftsubset` 子集化（中文常用字+标题用字，Noto Serif SC 全字重过大）+ **开工前先验 woff2 在磁盘且 `next/font/local` 能编译**（编译期从磁盘读、文件缺即 dev 报错）。
- [ ] ⚠️ **迁移既有 `app/layout.tsx` 的 `Noto_Sans_Mono`（承重·A3-2）**：`layout.tsx:2` 既有 `import { Noto_Sans_Mono } from "next/font/google"`（:7 实例化、:24 className，**全仓唯一 google 字体消费点**）不迁则**做完 build 仍按 ADR D-R1-04 失败** → 一并迁成 `next/font/local`（自托管 mono woff2），记 ADR。
- [ ] 首页（`ProjectHome.tsx` / 项目墙 route）深色玻璃改造：暗角 `radial-gradient` scrim 保可读、品牌/新建项目/项目卡用白字 + `rgba` 半透明 + `backdrop-filter:blur`；标题 Instrument Serif 斜体 + Noto Serif SC。
- [ ] **试点开关**（env/flag，仅首页生效，关掉即回退浅色原样）。
- [ ] 工作台（AppShell 内）**保持浅色不动**。

## AC（双层）
1. （真浏览器 :30141）首页 shader 背景渲染、深色玻璃文字可读、速度 0.018、**字体 B 真机生效**（Instrument Serif + Noto Serif SC）。
2. **离开首页 / 标签隐藏 → RAF 暂停**（实测无后台 GPU 占用，回首页恢复）。
3. `prefers-reduced-motion` 下走静态兜底；强制关 WebGL 回退渐变；工作台仍浅色不受影响。
4. 试点开关关闭 → 首页回浅色原样（可回退）。
5. `pageErrors=0`；lead 亲看截图（含字体 B 真机渲染——headless 取不到中文 webfont，必须真机验）。

## DoD
lint/test/tsc 绿；AC 真浏览器 PASS；ADR（试点开关落点 / 字体自托管方案 / **layout mono 迁移**）记 D-R8.5-06；回写 progress。
> ⚠️ **build oracle 缺口（A3-2）**：本容器 `next build` 因 Google Fonts 不通**跑不到分包**，three.js（WebGL/window 仅客户端，`page.tsx`/`ProjectHome.tsx` 均 `'use client'`）的 SSR/bundle 雷（同 D-R7B-07）**无 build 安全网** → 验收**强制真浏览器 `pageErrors=0` + dev issues overlay/全量 console** 作唯一 oracle（别只筛 error/warn）；three 组件须 `'use client'` 或 `dynamic(ssr:false)` 隔离。

## 依赖 / 风险
独立（新依赖 three + 新视觉方向）。风险：字体自托管子集化、WebGL 兼容/性能——都在护栏内兜底。设计稿基准 `shader-homepage-fonts.html`（字体方向 B）。
