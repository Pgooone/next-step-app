# T6 · 进会话二级菜单 + 底部切换条 + run cancel + 失败态

> 承重：是（依赖 T1-B）｜ 依赖：T1-B, T3, T4 ｜ 对应 AC：AC-8 / AC-9 / AC-11 / AC-12 ｜ 权威：详细设计 §一(#13/#19) + §3.11 + §3.10(失败态/停止按钮)

## 目标
落左键悬浮二级菜单（只读冻结对话 + 产物 + 底部 agent 切换条 +「进入完整对话」解冻续聊）+ run cancel（停止按钮 + API + AbortController 翻转）+ 失败态可见；真浏览器验（含故意失败的流水线 + cancel）。

## 子任务（最小可执行 · checklist）
- [ ] 新增 `components/StageSessionMenu.tsx`（左键点 stage 卡弹出悬浮二级菜单、**非整页跳转**）
  - [ ] 只读 transcript：读 `stage.sessionId` 会话 jsonl（既有会话读取链/组件 **待 lead 核**）；**已完成/已销毁/历史 run 阶段 → 纯只读、不挂 ChatInput 的 send**（不复活会话绕过 acquireSlot，审计#1）
  - [ ] jsonl 缺失 → 降级「记录已不存在」（审计#3）
  - [ ] 产物区：`stage.artifactId` → `onOpenArtifact(stage.artifactId)`（by-id 开 ArtifactPanel、落 artifactId 非 sessionId，审计#3；复用 `AppShell.tsx:447` handleOpenArtifact）
  - [ ] 底部 agent 切换条：**只枚举 `sessionId != null` 的阶段**（running/done/failed），pending 阶段置灰（审计#2）
  - [ ] **「进入完整对话」按钮 → `onOpenSession(stage.sessionId)`**：跳主对话窗 ChatWindow（AppShell 切会话 handler **待 lead 核**），首条跟进消息触发 `POST /api/agent/[id]` → `resolveOrReattachSession`（`session-reattach.ts:97`）→ getOwner 命中(`:115`) → lookupProfile 命中(`:117`) → reattachProfileSession 装回 doc 工具(`:119-125`) → 占 1 槽
- [ ] run cancel — 新增 `app/api/pipeline-runs/[runId]/cancel/route.ts`（POST）：
  - [ ] 1 `findRun(runId)` → {projectId, run}
  - [ ] 2 `run.cancelRequested=true`; `runStore.write(projectId, run)`（持久标记）
  - [ ] 3 `runControllers.get(runId)?.abort()`（翻 signal → runPipeline 循环顶检测 + runWorker 内 abort 在跑的 worker）
  - [ ] `runControllers` 进程级 Map（与起 run 路由同一模块单例，落点 **待 lead 核**：`lib/pi/run-controllers.ts` 或挂 globalThis）
- [ ] 改 `components/PipelineBoard.tsx`：接停止按钮（调 `usePipelineStore.cancelRun` → POST cancel）+ 失败态头完整渲染
  - [ ] 失败态（AC-8）：`run.status==='failed'` 容器头显「已失败·停在第 {currentStageIndex+1} 阶段」+ 失败阶段卡左红条 + 红徽章（红来自 STATUS_META，`DispatchPanel.tsx:38`）
- [ ] 改 `components/PipelineStageCard.tsx`：`onClick` 接 StageSessionMenu（T4 已预留挂载点）

## 关键复用 / 触点（file:line，照搬详细设计，勿改勿造）
- `resolveOrReattachSession`：`session-reattach.ts:97`；getOwner 命中 `:115`；lookupProfile 命中 `:117`；reattachProfileSession 装回 doc 工具 `:119-125`
- evict 只 destroy 不删 jsonl（解冻能重开根因）：`evict-agent-sessions.ts:45-47`(destroy) / `:14-16`(不删 jsonl)
- ADR 强证据（dispatch worker re-attach 得 7 项含 propose_edit）：`设计决策记录.md` D-R5-06
- 产物 by-id 打开：`AppShell.tsx:447`（handleOpenArtifact）
- 状态徽章红（失败态）：`DispatchPanel.tsx:38`（STATUS_META）
- `runWorker` 支持 signal：`dispatch-runner.ts:97`；abort→`reason==="aborted"` `waitForTurnEnd` `dispatch-runner.ts:251-258`；收尾非 completed 主动 abort 会话 `dispatch-runner.ts:161-163`
- 既有会话只读 transcript 读取链/组件（如 session-reader / ChatWindow 只读模式）：**待 lead 核**（§九待核 5）
- AppShell 切会话 handler（onOpenSession）：**待 lead 核**（§九待核 3）
- `runControllers` 落点：**待 lead 核**（§九待核 7）

## 验收标准（AC）
- AC-8：故意造会失败流水线，看板该 run 显失败徽章(红)+左红条+run 级「已失败·停在第N阶段」头，一眼看出哪步炸。
- AC-9：运行中 run 点「停止」/`POST .../cancel` 能中止编排（AbortSignal 生效）、不再被逼重启 dev；停止后会话被释放（runWorker 收尾 abort + 编排器随后 evict）。
- AC-11：左键弹悬浮二级菜单只读看冻结对话+产物；已完成/已销毁/历史 run 会话页纯只读（无 send、不复活会话绕过 acquireSlot）；底部切换条只枚举有 sessionId 阶段、pending 置灰；jsonl 缺失降级「记录已不存在」、产物链接落 artifactId。
- AC-12：二级菜单「进入完整对话」跳主对话窗 ChatWindow，能解冻 re-attach 续聊（占 1 槽、受并发上限）；续聊前 spike（T1-B）已验可行。

## 门禁 / 验收方式
- lint / test / tsc 全绿。
- **承重卡（依赖 T1-B）**：T1-B 不绿则「进入完整对话」降级只读、AC-12 改判须用户确认。
- **真浏览器**（UI 卡必走）：含故意失败的流水线（验 AC-8 失败态）+ cancel（验 AC-9）+ 二级菜单只读守卫（AC-11）+ 进入完整对话续聊（AC-12）；pageErrors=0。

## 降级 / 备注
- **红线**：StageSessionMenu 已完成/历史 run 会话纯只读、**不挂 send**（不复活绕过 acquireSlot）；底部切换条只枚举有 sessionId 阶段。
- 「进入完整对话」= 解冻续聊：evict 只 destroy wrapper 不删 jsonl（`evict-agent-sessions.ts:14-16`），故 jsonl 仍在盘、`resolveOrReattachSession` 能重开（D-R5-06 语义）；**勿改 reattachProfileSession / session-reattach.ts，仅复用**。
- run cancel 是看板唯一 run 级写操作（不取消单阶段/不重跑阶段，守 D-V1.2-33 只读）。
