# T4 · 看板族 UI + AppShell 入口/模态 + dicebear 离线头像 + store

> 承重：否 ｜ 依赖：T2, T3 ｜ 对应 AC：AC-10 / AC-13(部分) ｜ 权威：详细设计 §一(#6/#7/#8/#14/#15/#16/#17/#18/#20/#21/#23) + §3.6/§3.7/§3.8/§3.10 + §3.12(AppShell)

## 目标
落看板族 UI（模态外壳 + 看板 + 阶段卡 + 点阵 + 悬停浮窗 + 蓝图编辑器）+ AppShell 左栏「流水线」入口与模态挂载 + dicebear 离线头像 + Zustand store（2s 轮询）；视觉严格遵循 `看板视觉定稿-kimi-v11.html` 的 Kimi(A) 块、亮/暗双主题；真浏览器 pageErrors=0 且 node:fs 不入 bundle。

## 子任务（最小可执行 · checklist）
- [ ] `npm install @dicebear/core @dicebear/collection`（当前 package.json 与 node_modules 均无，已 Read 实证；注意本环境 npm 镜像）
- [ ] 新增 `lib/pipeline/dot-matrix.ts`（纯算法、零 import）：`DOT_ROWS=3` / `DOT_COLS=12` / `filledCols(progress)=round(clamp01(progress)*DOT_COLS)`
- [ ] 新增 `lib/pipeline/avatar.ts`：`agentAvatarDataUri(seed)` = `createAvatar(notionists,{seed}).toDataUri()`（dicebear、纯 JS、离线）
- [ ] 新增 `lib/stores/usePipelineStore.ts`（Zustand、单一职责、命名 useXxxStore）：blueprints/runs/currentRun + `loadBlueprints`/`saveBlueprint`/`deleteBlueprint`/`startRun`/`loadRuns`/`selectRun`/`pollCurrentRun`/`cancelRun`/`stopPolling`
  - [ ] 轮询复用 DispatchPanel 范式：`POLL_INTERVAL=2000`（`DispatchPanel.tsx:33`）+ setInterval（`:93-95`）+ 终态停轮询（`:91` selectIsActive 守卫）；`status==='running'`(或有 queued 阶段)才轮询 GET `pipeline-runs/[runId]`、终态停
  - [ ] **只 fetch JSON、绝不 value-import pipeline-store/-run-store/-orchestrator；类型用 `import type`**（D-R7B-07）
- [ ] 新增 `components/PipelineModal.tsx`：左栏一个入口模态外壳 + 两 tab（「流水线」=看板/编辑器 ｜「快速派发」=旧表单，旧表单原样并入由 T7 接，本卡先搭外壳与「流水线」tab）
  - [ ] 模态外壳仿 `DispatchPanel.tsx:170-199`（`fixed inset-0` + backdrop 点击关闭 `:182-184`）
  - [ ] Props：`{ projectId, onClose, onOpenArtifact, onOpenSession, onArtifactsChanged?, onSessionsChanged? }`
- [ ] 新增 `components/PipelineBoard.tsx`：run 下拉(多 run 回看) + 容器头 `⑂ 流水线名 · ④/N` + 全局进度条 `▰▰▱…`(格数随 stages 动态、取 `run.currentStageIndex`) + 纵向 stage 卡列表 + 空态 + 失败态头 + 停止按钮
  - [ ] 状态徽章色复用 `DispatchPanel.tsx:38` 的 `STATUS_META`（失败态红即来自此）
  - [ ] 空态（D-V1.2-34）：`runs.length===0` 无当前 run 时显「+ 新建流水线」卡 → 切 PipelineEditor
  - [ ] 失败态头（AC-8 渲染在 T6 重点，本卡可先占位）：`run.status==='failed'` 显「已失败·停在第 {currentStageIndex+1} 阶段」
  - [ ] 排队态：`stage.statusDetail==='queued'` 显「排队中·等会话槽」
  - [ ] 只读 affordance（D-V1.2-33）：不取消/重跑阶段；停止按钮是唯一 run 级写操作（cancel 实现在 T6）
  - [ ] run 下拉「选历史 run 看只读」仿第二轮版本历史范式 → 版本下拉组件路径 **待 lead 核**（详细设计 §九待核 2）
- [ ] 新增 `components/PipelineStageCard.tsx`：两行卡（行1 头像+阶段名+`agentName ?? agentId`+右上两位零填充序号纯文本灰字；行2 `└`+单行截断 subTask+`<StageDotMatrix progress={statusToProgress(stage)} />`+状态徽章+`›`）
  - [ ] 各状态视觉：pending(灰/空点阵)·running(半亮+左 3px emerald 条+呼吸动画+整卡高亮)·done(变灰+✓+满点阵)·failed(左红条+红徽章)
  - [ ] **交互务必分开实现勿合并**：①`onMouseEnter`→StageHoverPreview；②`onClick`→StageSessionMenu（StageSessionMenu 在 T6）
  - [ ] `statusToProgress(stage)` 离散映射：done=1/failed=1(满点阵但红)/running=0.5/pending|queued=0（记 ADR D-R7-*）
- [ ] 新增 `components/StageDotMatrix.tsx`：`DOT_ROWS×DOT_COLS`(3×12=36)块，`col<filledCols(progress)` 给 emerald 否则灰；`import { filledCols, DOT_ROWS, DOT_COLS } from "@/lib/pipeline/dot-matrix"`（守 D-R7B-07）
- [ ] 新增 `components/StageHoverPreview.tsx`：悬停浮窗(~330px 可向下滚动)：任务/范围/上游依赖/产物/验收清单/最近动作/耗时(=finishedAt-startedAt)；**与 StageSessionMenu 分开实现**
- [ ] 新增 `components/PipelineEditor.tsx`：手填 N 阶段（选 agent + subTaskTemplate 多行 + order 上/下移、增删、空态引导、保存调 POST/PUT pipelines、校验在后端 store 前端给即时反馈）
  - [ ] agent 选择复用既有 agent 列表（AgentProfileStore）→ 前端取 agent 列表的现成 hook/接口 **待 lead 核**（详细设计 §九待核 4）
- [ ] 改 `components/AppShell.tsx`：左栏「流水线」入口按钮 + 模态挂载 + 透传
  - [ ] 入口按钮仿 `AppShell.tsx:572`（`onClick:()=>setDispatchPanelOpen(true)` 范式）→ 新增 `setPipelineModalOpen(true)`，按钮在侧栏底排循环 `:570-604` 加一项 `label="Pipeline"`、`disabled:!currentProjectId`
  - [ ] 新增 state `const [pipelineModalOpen, setPipelineModalOpen] = useState(false)`（仿 `:61` dispatchPanelOpen）
  - [ ] 模态挂载仿 `:1117-1127`：`{pipelineModalOpen && currentProjectId && <PipelineModal projectId=... onClose=... onOpenArtifact={handleOpenArtifact}(:447) onOpenSession={...}(切会话 handler 待 lead 核) onArtifactsChanged={()=>setExplorerRefreshKey(k=>k+1)}(:1124 同款) onSessionsChanged={handleDispatchSessionsChanged}(:393) />}`
  - [ ] **零破既有**：复用 handleOpenArtifact(`:447`)/explorerRefreshKey(`:56`)/handleDispatchSessionsChanged(`:393`)，不新造

## 关键复用 / 触点（file:line，照搬详细设计，勿改勿造）
- 模态外壳：`DispatchPanel.tsx:170-199`；backdrop 点击关闭 `:182-184`；快派表单嵌入点 `DispatchForm`(`DispatchPanel.tsx:285`)（T7 接）
- 轮询：`DispatchPanel.tsx:33`(POLL_INTERVAL)/`:93-95`(setInterval)/`:91`(终态守卫)
- 状态徽章色 STATUS_META：`DispatchPanel.tsx:38`
- AppShell 入口按钮范式：`AppShell.tsx:572`；侧栏底排循环 `:570-604`；dispatchPanelOpen state `:61`；模态挂载 `:1117-1127`；handleOpenArtifact `:447`；explorerRefreshKey `:56`/`:1124`；handleDispatchSessionsChanged `:393`
- 纯算法 import 守红线：`@/lib/pipeline/dot-matrix`（D-R7B-07）
- 第二轮版本历史下拉组件路径（run 下拉对照）：**待 lead 核**（§九待核 2）
- AppShell 切会话 handler（onOpenSession）：**待 lead 核**（§九待核 3）
- 前端取 agent 列表 hook/接口：**待 lead 核**（§九待核 4）

## 验收标准（AC）
- AC-10：纵向列表 + Kimi 卡片（亮/暗双主题）；全局进度条格数随 stages 动态；绿点阵 LED 3×12 按列点亮、`filledCols=round(progress*12)`；dicebear notionists 头像本地离线生成（断网可渲染）；progress 由离散 status 三档映射（done=满列/running≈半列/pending=空列）。
- AC-13(部分)：左栏一个「流水线」入口 + 模态（无新路由）；两 tab 外壳（「快速派发」tab 内容与小修在 T7）。

## 门禁 / 验收方式
- lint / test / tsc 全绿。
- **真浏览器 pageErrors=0 + node:fs 不入 bundle**（UI 卡必走真浏览器；lint/test/tsc/build 抓不到 node:fs 污染，须真浏览器兜底，D-R7B-07）；断网验 dicebear 头像可渲染；亮/暗双主题对照定稿 HTML。

## 降级 / 备注
- **红线（命门）**：客户端组件/store 只 `import type` + import 纯算法（dot-matrix/avatar），run/blueprint 经 fetch JSON 取，**绝不 value-import** pipeline-store/-run-store/-orchestrator（含 node:fs）。
- StageSessionMenu（左键二级菜单）+ run cancel + 失败态完整渲染在 T6（依赖 T1-B）；本卡 onClick 先预留挂载点。
- 视觉以 `看板视觉定稿-kimi-v11.html` Kimi(A) 块为准（实现时对照 HTML 抄类名/结构），否决 Swiss(B)。
