# T5 · 并发上限可配（factory-config + concurrency-gate + 根 CLAUDE.md + UI 警示）

> 承重：否 ｜ 依赖：—（无强依赖）｜ 对应 AC：AC-14 ｜ 权威：详细设计 §一(#4/#5/#24) + §3.4 + §3.5 + §六(并发红线变更)
> 🚩 **红线变更**（D-V1.2-41，风险自担）：项目原红线「并发会话 ≤3（硬墙、不改）」→「默认 3、用户可配」。

## 目标
让 `concurrency-gate` 读配置（默认 3、HARD_CAP clamp）、流水线调用点传 Infinity 不限超时、UI 给资源警示、同步根 CLAUDE.md 红线条目；既有快派/会话语义零回归。

## 子任务（最小可执行 · checklist）
- [ ] 新增 `lib/pi/factory-config.ts`：`DEFAULT_MAX=3` / `HARD_CAP=8`(待 lead 确认) / `readMaxConcurrent():number`
  - [ ] 读 `~/.pi/factory-config.json`（落点待 lead 定）；缺省/解析失败 → 返 DEFAULT_MAX（仿 `session-agent-map.ts:27-47` readMap「损坏回退空」范式）；值越界 → clamp `[1, HARD_CAP]`
- [ ] 改 `lib/pi/concurrency-gate.ts`（改动 1·上限可配）：`MAX_CONCURRENT_SESSIONS` 由 `const=3`（`concurrency-gate.ts:16`）→ 读 `readMaxConcurrent()`
  - [ ] **倾向方案**：改 `acquireSlot:39` 一行 `limit = opts?.limit ?? readMaxConcurrent()`（保留 `MAX_CONCURRENT_SESSIONS` 导出名零破既有调用方，且每次读最新配置）
  - [ ] 既有快派/会话语义不变：`acquireSlot()` 不传 limit 自动读配置；前端聊天会话计数源 `activeSessionCount`（`:19-21`）不动
- [ ] concurrency-gate 改动 2（不限超时）= **零改 acquireSlot 本体**：仅流水线编排器调用点传 `{timeoutMs:Infinity}`（`Infinity` 走现有逻辑天然成立、`deadline` 永不触发）；旧快派 `orchestrator.ts:117` 的 `await doAcquireSlot()` 不动（仍 60s）
  - [ ] 注：流水线调用点的 Infinity 已在 T3 主循环落（本卡只保证 acquireSlot 支持 Infinity、无须本体改动）
- [ ] 改 `components/PipelineBoard.tsx`（或调高并发处）：UI 资源警示「单进程更多活会话、资源耗尽风险自担」
- [ ] 改根 `CLAUDE.md`：「并发会话 ≤3（硬墙、不改）」红线条目 →「默认 3、用户可在设置调整（冻结模型让 ≤3 从硬墙变吞吐限流；调高资源风险自担）」

## 关键复用 / 触点（file:line，照搬详细设计，勿改勿造）
- `MAX_CONCURRENT_SESSIONS` const：`concurrency-gate.ts:16`
- `acquireSlot` 默认 limit 用处：`concurrency-gate.ts:39`；`timeoutMs` 默认 60_000：`concurrency-gate.ts:40`；deadline 计算 `:43`；while 轮询 `:44-49`；超时抛点 `:45`
- 聊天会话计数源 `activeSessionCount`：`concurrency-gate.ts:19-21`
- 容错回退范式 readMap：`session-agent-map.ts:27-47`
- 旧快派调用点不动：`orchestrator.ts:117`（`await doAcquireSlot()` 仍 60s）
- factory-config 落点（`~/.pi/factory-config.json` vs 项目级）+ HARD_CAP 值（建议 8）：**待 lead 核**（详细设计 §九待核 8）

## 验收标准（AC）
- AC-14：`concurrency-gate` 读配置（默认 3、有合理上限）；UI 有资源警示；根 `CLAUDE.md`「并发会话 ≤3」红线条目同步更新。

## 门禁 / 验收方式
- lint / test / tsc 全绿；既有快派/会话语义零回归（acquireSlot 不传 limit 默认 3、不传 timeoutMs 默认 60s）。
- 单测覆盖 readMaxConcurrent 缺省/损坏/越界 clamp。

## 降级 / 备注
- **🚩 红线变更须同步根 `CLAUDE.md`**（红线变更三件套之③：读配置 / UI 警示 / 同步根 CLAUDE.md），缺一不可。
- 冻结模型本身不变（≤3 从硬墙变吞吐限流）。
- 待 lead 核：factory-config 落点 + HARD_CAP 值。
