# T8 · 双层验收 + 收尾 + 回写 docs（含残留清理）

> 承重：否 ｜ 依赖：全部（T1~T7）｜ 对应 AC：AC-15（+ 端到端复验 AC-8/9/10/11/12/13）｜ 权威：详细设计 §五任务表 T8 + §六红线 + §七已知限制 + §八测试残留清理

## 目标
全量门禁绿 + 双层验收（逻辑层 verifier + 端到端真浏览器）+ 红线复核 + 回写本文实测落地与 ADR D-R7-* + 清测试产物，保持仓库与盘干净。

## 子任务（最小可执行 · checklist）
- [ ] 全量门禁：`npm run lint` 净 / `npx tsc --noEmit` 零错误 / `npm test`（vitest）全绿
- [ ] 端到端真浏览器复验：AC-8（失败态）/ AC-9（cancel）/ AC-10（看板视觉·亮暗·点阵·dicebear 断网渲染）/ AC-11（进会话只读守卫）/ AC-12（进入完整对话续聊）/ AC-13（合并入口两 tab + F6/F8/F15）；**真浏览器 console pageErrors=0**（客户端 bundle 不被 node:fs 污染）
- [ ] 红线复核：headless worker 不放 propose_edit（无永久悬而未决 PendingChange）；旧 dispatch 与既有受管文档/会话分组功能零回归
- [ ] 回写详细设计「## 任务卡与落地」：commit hash / 双层验收 / 真浏览器截图名
- [ ] 补 lead ADR D-R7-*（实现期定项）：
  - [ ] run 列表「最近 N」值（建议 20）+ runs/*.json 保留上限 M（建议 50）
  - [ ] dot-matrix progress 离散映射（done=1/failed=1/running=0.5/pending=0）
  - [ ] factory-config 落点 + HARD_CAP 值
  - [ ] runControllers 落点
  - [ ] §九「待 lead 核」8 项的实测结论
- [ ] 测试残留清理（详细设计 §八）：删测试项目下 `.pi/factory/**`（pipelines + runs）+ 受管 .md + `.pi/artifacts/managed/*` + 派发会话 jsonl；或整 registry 复位（备份 projects.json）

## 关键复用 / 触点（file:line，照搬详细设计，勿改勿造）
- 红线速查逐条 holds：详细设计 §六
- 已知限制（文档显式记）：详细设计 §七（同名 agent 跨阶段复用 / 改上游不回灌下游 / token 护栏 / 跨轮文档性能 / 崩溃恢复 / F10 引导非强制）
- 测试残留清理清单：详细设计 §八
- headless worker 白名单（复核不放 propose_edit）：`doc-session.ts:44`（6 项）；交互式 7 项 `doc-session.ts:28`
- 跨轮文档性能回归（受管分组列 10+ 项 file panel 不卡）：详细设计 §七

## 验收标准（AC）
- AC-15：`npm run lint` 净 / `npx tsc --noEmit` 零错误 / `npm test` 全绿；真浏览器 console pageErrors=0（客户端 bundle 不被 node:fs 污染）；headless worker 不放 propose_edit（无永久悬而未决 PendingChange）；旧 dispatch 与既有受管文档/会话分组功能零回归。

## 门禁 / 验收方式
- 全量 lint / tsc / vitest 全绿。
- **双层验收**：逻辑层独立 verifier（不认 impl 自跑）+ 端到端真浏览器（AC-8/9/10/11/12/13 + pageErrors=0）。
- 回写 docs 与 ADR、清测试产物后保持仓库与盘干净（沿第二~六轮收尾纪律）。

## 降级 / 备注
- 跨轮文档性能：MVP 不在 runPipeline 同步刷 ArtifactPanel，产物经看板/受管分组按需 by-id 打开（复用第三轮 memo 化基线）；验收回归确认列 10+ 受管文档时 file panel 不卡。
- 已知限制全部文档显式记（详细设计 §七），非本轮 bug。
- 收尾纪律：测试残留清理是收官前必做项，备份 projects.json 再复位 registry。
