# 第七轮 续做交接（T4–T8）

> **新窗口先读这份 + `progress.md`，即可接着做，不必回读旧对话。** 日期：2026-06-27。
> 活跃代码在 `next-step-V1.2/`，分支 `v1.2`。

## 一句话现状
第七轮「流水线与阶段看板」**承重三卡全绿**：T1 承重墙 spike ✅、T2 数据层 ✅、T3 承重编排器 ✅（F16「冻结释槽」机制已真正接进生产 `runPipeline`）。剩 **T4–T8**：UI（T4/T6）+ 并发可配（T5）+ 合并入口（T7）+ 收尾双层验收（T8）。

## 现状表

| 卡 | 状态 | commit | 关键产物 |
|---|---|---|---|
| T1 | ✅ 承重 GO | `f430d95` | 两 spike（探针·待 T8 删）：冻结释槽 + re-attach |
| T2 | ✅ | `23f2460` | pipeline-store / pipeline-run-store / 蓝图 2 路由 / 校验 |
| T3 | ✅ 承重 | `636133e` | runPipeline + run-controllers + 起run/读时对账 2 路由 |
| **T4** | ⬜ 待开工 | — | 看板族 UI + AppShell 入口/模态 + dicebear + usePipelineStore（**UI 卡·真浏览器**） |
| **T5** | ⬜ | — | 并发上限可配（factory-config + concurrency-gate + 根 CLAUDE.md + UI 警示）🚩红线变更 |
| **T6** | ⬜ 承重(依赖 T1-B) | — | 进会话二级菜单 + 底部切换条 + run cancel + 失败态（**真浏览器**） |
| **T7** | ⬜ | — | 合并入口（一个入口·两 tab）+ 旧 DispatchForm 小修 F6/F8/F15 |
| **T8** | ⬜ | — | 双层验收 + 收尾 + 回写 docs + 残留清理（删 T1 探针 + 回填 ADR hash） |

**未 push**：本地 `v1.2` 领先 `origin/v1.2`(`2828e6d`) 共 7 个 commit（第七轮全部），**push 待用户授权**。

## 开工方式（每卡固定三步，沿用本轮）
1. **ultracode 调查**（Workflow）：核验复用触点 file:line + 设计 tricky 逻辑 + 对抗式挖坑 → 产出「照写 impl 规格」。脚本写到 `/tmp/*.js` 跑（**prompt 内别用 markdown 反引号**，会破模板字符串解析→用单引号）。
2. **agent team 执行**：新卡新队员（`Agent` 指定 `model:'opus'`、`name:'ns-impl-tN'`），照规格写、不 commit、跑门禁回报。
3. **lead 双层验收**：亲读 diff + **独立复跑门禁**（不认队员自证）+ UI 卡**走真浏览器** → 回写 progress/ADR → 单独 commit（git 写须 `dangerouslyDisableSandbox` + heredoc）。

> 权威设计 = `docs/V1.2/第七轮-流水线与阶段看板/{需求,概要,详细设计}.md` + 决策 QA `docs/V1.2/QA/第七轮-流水线与阶段看板决策.md`（D-V1.2-28~47）+ lead ADR `docs/V1.2/设计决策记录.md`（D-R7-01~03 已写、04+ 待补）。每卡 spec 由 ultracode 当场重出（不依赖 /tmp 旧文件）。

## T4–T8 逐卡速查（详见详细设计 §五任务表 + §一文件总表）

- **T4 看板族 UI**（依赖 T2/T3，AC-10/13部分）：先 `npm install @dicebear/core @dicebear/collection`（当前未装、本环境 npm 镜像见 `~/.claude/CLAUDE.md`）。新建 `PipelineModal/PipelineBoard/PipelineStageCard/StageDotMatrix/StageHoverPreview/PipelineEditor` + 改 `AppShell`（加「流水线」入口+模态，仿 dispatch）+ `usePipelineStore`（仿 DispatchPanel 2s 轮询、终态停）+ 纯算法 `lib/pipeline/dot-matrix.ts`/`avatar.ts`。**视觉严格照 `看板视觉定稿-kimi-v11.html` 的 Kimi(A) 块**（亮/暗）。**🔴红线 D-R7B-07**：客户端只 `fetch JSON + import type`，**绝不 value-import** pipeline-store/-run-store/-orchestrator（含 node:fs→拖进 client bundle 致全站 500，lint/test/tsc/build 全抓不到、只真浏览器暴露）。验收：真浏览器 pageErrors=0 + dicebear 断网可渲染。
- **T5 并发可配**（无强依赖，AC-14，🚩红线变更 D-V1.2-41）：`concurrency-gate.ts` 的 `MAX_CONCURRENT_SESSIONS` 改读 `factory-config.readMaxConcurrent()`（默认 3、HARD_CAP=8 clamp）；流水线调用点传 Infinity（**T3 已传**）；UI 给资源警示；**同步根 `CLAUDE.md`「并发会话 ≤3（硬墙）」条目 → 「默认 3、可配」**。既有快派/聊天会话语义零回归（`acquireSlot()` 不传 limit 默认读配置）。
- **T6 进会话 + cancel**（依赖 T1-B/T3/T4，承重·AC-8/9/11/12，**真浏览器**）：`StageSessionMenu`（左键悬浮二级菜单：只读冻结对话+产物+底部切换条+「进入完整对话」）+ `cancel/route.ts`（翻 signal：`getRunController(runId)?.abort()` + run.cancelRequested 落盘）+ 失败态看板。**「进入完整对话」复用 T1-B 已验证的 re-attach**（`resolveOrReattachSession`→装回 7 项含 propose_edit，**仅复用勿改** session-reattach/reattachProfileSession/doc-session）。已读/历史 run 纯只读不复活。⚠️ T3 ADR D-R7-03 留口：排队中(acquireSlot 轮询)的阶段 cancel 不中断——若要彻底闭合，可在 T6 给 acquireSlot 加可选 signal（5 行）。
- **T7 合并入口**（依赖 T4，AC-13）：做「一个入口 + 两 tab」外壳（「流水线」/「快速派发」），旧快派表单**原样并入**「快速派发」tab（仍走旧 `runDispatch`）；F6 顺序号①②③+串行提示 / F15 重跑·新建空白两按钮 / F8 未选 agent 占位提示 在 tab 内就地修。**旧 dispatch 零回归**。
- **T8 收尾**（依赖全部，AC-15）：真浏览器 AC-8/9/10/11/12/13 + pageErrors=0；headless worker 不放 propose_edit 复核；**删 T1 两个 spike 探针**（`lib/pi/pipeline-evict-release.spike.test.ts` + `pipeline-reattach.spike.test.ts`，命门已迁 progress.md「T3/T6 承重契约」+ ADR D-R7-01）；**回填 ADR D-R7-01/02/03 的 commit `<待填>` 占位**；清测试产物（测试项目 `.pi/factory/**` + 受管 .md + 派发会话，或 registry 复位备份 projects.json）；push + 独立 ls-remote 实测同步。

## 复用点 / 模板（照搬勿造）
- 领域 store / 编排器：`lib/domain/{dispatch-store,orchestrator}.ts`；轮询 UI：`DispatchPanel.tsx`（POLL_INTERVAL 2000 + 终态停 + STATUS_META 色）；模态：`DispatchPanel.tsx:170-199`；AppShell 入口/挂载：`AppShell.tsx:570-604/1117-1127`。
- API 错误映射：`lib/api/errors.ts` 的 `domainErrorResponse`（按 `code` 鸭子类型 NOT_FOUND→404/INVALID→422）。
- **承重 verify 纪律**（T6 若动释槽/re-attach）：真实函数 + faux Map「三处闭包同一对象」+ faux.destroy 真删 Map + 负对照（见 T3 `pipeline-orchestrator.test.ts` A 部分、T1 spike）。
- 真浏览器：用 `browser-e2e` skill（已 scoped 到 next-step-V1.2）；`source ~/.local/bin/ns-browser-env.sh`；`page.goto` 用 `domcontentloaded`。

## 红线 / 踩坑（务必带走）
- 客户端不 value-import 含 node:fs 的领域层（D-R7B-07，T4 头号坑）；headless worker 不放 propose_edit；evict 只 destroy 不碰 owner map；不改 pi 内核；纯文件无 DB；并发默认 3（T5 起可配、风险自担）。
- subagent 自证不可信 / idle 通知不等于完成 → **进度只认 git 实盘**；lead 独立复跑门禁。
- 后台 Workflow/Agent 完成会自动通知，**别 bash 轮询**（浪费 token，用户 2026-06-27 反馈）。
- 每卡门禁绿即单独 commit（细粒度回退）；git 写 `dangerouslyDisableSandbox` + heredoc，落地只认独立 `git log`。

## 记忆锚点
长期记忆 `next-step-v1.2-r7-pipeline.md`（含 T1/T2/T3 收官状态 + 承重契约 + 假GO/T2五坑/T3头号gap 教训）+ `MEMORY.md` 索引行已更新到 T3。
