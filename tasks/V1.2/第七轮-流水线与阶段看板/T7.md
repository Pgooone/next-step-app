# T7 · 合并入口（一个入口·两 tab）+ 旧 DispatchForm 小修（F6/F8/F15）

> 承重：否 ｜ 依赖：T4 ｜ 对应 AC：AC-13 ｜ 权威：详细设计 §〇(合并入口口径) + §3.10(PipelineModal) + §3.12(DispatchPanel 小修) + §五任务表 T7

## 目标
做「一个入口 + 两 tab 外壳」（「流水线」/「快速派发」），把旧快派表单原样并入「快速派发」tab（仍走旧 `runDispatch`/`POST .../dispatch`），并在 tab 内就地修 F6/F8/F15；旧 dispatch 零回归。

## 子任务（最小可执行 · checklist）
- [ ] 把现有 `DispatchPanel` 的 `DispatchForm`（`DispatchPanel.tsx:285`）内容原样嵌入 PipelineModal 的「快速派发」tab（仍走旧 `runDispatch`，逻辑几乎不动）
- [ ] **F6**（审计 §🟡）：`DispatchForm`（`DispatchPanel.tsx:285`）给已选 agent 标执行顺序号 ①②③ + 一句「将按此顺序串行执行，上游产物自动喂给下游」
- [ ] **F15**（审计 §🟡）：`handleRestart`（`DispatchPanel.tsx:165`）拆两按钮——「重跑本次」（保留预填）/「新建空白派发」（补 `setGoal('')` + `setSelectedIds([])` + `setSubTasks({})`，子任务 state setter 名 **待 lead 核**）
- [ ] **F8**（审计 §🟡）：未勾选 agent 时子任务输入框占位提示「勾选 Agent 后可填子任务」（DispatchForm 子任务输入区，行号 **待 lead 核**）
- [ ] **零新增 import**（守 D-R7B-07，仿第四轮 T5 AppShell 改法）

## 关键复用 / 触点（file:line，照搬详细设计，勿改勿造）
- 旧表单嵌入点 `DispatchForm`：`DispatchPanel.tsx:285`
- F15 `handleRestart`：`DispatchPanel.tsx:165`
- DispatchForm 子任务 state setter 名（F15「新建空白」清空）：**待 lead 核**（详细设计 §九待核 6）
- F8 子任务输入区行号：**待 lead 核**（详细设计 §3.12 标注）
- 模态外壳/两 tab 由 T4 PipelineModal 提供（详细设计 §3.10）

## 验收标准（AC）
- AC-13：左栏一个「流水线」入口 + 模态（无新路由）；模态内两 tab（「流水线」/「快速派发」），旧快派表单原样并入「快速派发」tab；F6 顺序号①②③+串行提示、F15 重跑/新建空白两按钮、F8 未选 agent 子任务占位提示均在「快速派发」tab 内生效。

## 门禁 / 验收方式
- lint / test / tsc 全绿；**旧 dispatch 零回归**（仍走 `runDispatch`/`POST .../dispatch`）。
- **真浏览器**（UI 卡必走）：两 tab 切换、旧快派表单功能、F6/F8/F15 三处小修在 tab 内生效；pageErrors=0。

## 降级 / 备注
- 合并入口已定「一个入口·两 tab」（D-V1.2-39），无开工前待拍板项。
- 旧 `runDispatch` 逻辑几乎不动；F6/F8/F15 三处小修就地在「快速派发」tab 内做（落 DispatchForm）。
- **零新增 import**（守 D-R7B-07）。
