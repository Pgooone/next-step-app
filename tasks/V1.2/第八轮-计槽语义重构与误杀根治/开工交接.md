# 第八轮 开工交接（新窗口读这份 + progress.md 即可接着做）

> 日期 2026-06-28。活跃代码在 `next-step-V1.2/`、分支 `v1.2`。走 cc-multi-agent-dev-flow（spec-first 承重轮）。

## 一句话现状
第八轮「误杀根治」spec 已定稿（**evict-by-sessionId**，commit `44756bc`），**从 T1 hermetic spike 开工**。
（注：曾先做方案 B〔计槽 size→in-flight〕、被 ultracode 设计评审门判 NO-GO〔isStreaming 在 retry 退避漏算〕→ 改选 evict-by-sessionId。方案 B 全分析留在 `方案B设计评审-NO-GO记录.md` 供回溯。）

## 要做什么（最小）
把流水线编排器每阶段的 evict 从「按 `stage.agentId` 一锅端」收窄为「只逐本阶段 `stage.sessionId`」——根治第七轮 T6 的跨 run 误杀。**保留 F16 释槽、不动并发计量语义。**

## 怎么开工（三步）
1. **读**：`progress.md`（任务序）+ `docs/V1.2/第八轮-计槽语义重构与误杀根治/详细设计.md` §一改动面/§二承重墙 + 需求 AC-1~6。为何不走方案 B 看 `方案B设计评审-NO-GO记录.md`。
2. **T1 承重 spike（先做、未 GO 不开 T2）**：hermetic 单测坐实 `evictSession(sidA)` 只逐 sidA、同 agent sidB 仍活、流式先 abort、owner-map 零碰 + 负对照 + 变异检查（退化回 by-agentId→sidB 误删断言红）。**纯应用层、零内核时序依赖、预期高置信 GO。** lead 独立复跑。
3. **T2 实现**：`evict-agent-sessions.ts` 新增 `evictSession`（复用 :44-46 abort-then-destroy 配方、跳过 sessionsForAgent）+ orchestrator `:167/:187` 改传 `stage.sessionId` + 两测试（evict-agent-sessions 追加单测 / orchestrator 承重断言 agentId→sessionId，按 describe 块改全 :98-261/:603-775/:386）。→ **T3 双层验收**（真浏览器 AC-1 误杀根除核心场景 + AC-2 长流水线 + AC-4 cancel/reattach）+ 删探针 + push。

## 关键事实（lead 已 file:line 亲核，照用）
- **一 worker = 一 sessionId = 一槽**：worker 工具集无 spawn（`DISPATCH_DOC_SESSION_TOOLS` 6 项 + coding 7 项无 subagent/task）+ `__piSessions` 写入仅 `rpc-manager.ts:290-291` 两点 → 按 sid 逐出不漏槽、不碰用户复活的同 agent 其他 sid。
- evict 配方在 `evict-agent-sessions.ts:44-46`（isStreaming→abort→destroy）；owner-map 红线注释 `:11-13`。
- orchestrator evict 触点 `:187`（正常）+ `:167`（catch），`stage.sessionId` 在 `:181` 赋值；`doEvict` 注入口在 `:82`（注意 :80 是 doAcquireSlot）。

## 红线（务必带走）
- 只**新增** `evictSession`、**不动** `evictAgentSessions` 本体 / `concurrency-gate` 计量 / owner-map(`bySession`/`removeOwner`，第五轮) / `HARD_CAP` / 内核。
- 第二条 evict-by-agentId（`agents/[agentId]/route.ts:63` 改 mode）**不动**（不在误杀链上）。
- 承重测试改断言后变异检查证非 vacuous；每卡 lead 亲读 diff + 独立复跑门禁 + 单独 commit；UI/端到端走真浏览器（`next-step-V1.2:browser-e2e`、deepseek 可真起会话）。

## git / push
- 本地 `v1.2` 领先 `origin/v1.2`(`0a877da`) **3 commit**（顺手小改 `bd031ca` HARD_CAP→100 + 方案B立项 `fadfe88` + evict-by-sessionId 改选 `44756bc`），**未 push**——T1~T3 做完 + 用户授权一起 push。

## 记忆锚点
长期记忆 `next-step-v1.2-r8-slot-accounting.md`（全上下文 + 评审 NO-GO + 改选）+ MEMORY.md 索引已更新。第七轮已收官 push 在 `0a877da`（见 [[next-step-v1.2-r7-pipeline]]）。
