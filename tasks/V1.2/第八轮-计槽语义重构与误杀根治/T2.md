# T2 · 实现：evictSession + orchestrator 逐出收窄 + 测试

> 承重：是（依赖 T1 GO）｜ 依赖：T1 ｜ 对应 AC：AC-1/2/3/5 ｜ 权威：详细设计 §一

## 目标
新增 `evictSession`（按 sessionId 逐出）+ 把流水线编排器每阶段 evict 从「按 agentId」收窄为「按本阶段 sessionId」+ 配套测试。**保留 F16 释槽机制、不动 concurrency-gate/内核/owner-map/上限。**

## 子任务（最小可执行）
- [ ] **`lib/pi/evict-agent-sessions.ts`**：新增 `evictSession(projectRoot, sessionId, deps?)`——复用 `:44-46` 单会话配方（`getSession(sid)`→`isAlive` 检查→`isStreaming` 则先 `send({type:'abort'})`→`destroy()`），**跳过 `sessionsForAgent` 反查**、直接对单 sid 操作；getSession 经既有 DI 缝。**保留 `evictAgentSessions` 不动**（grep 确认别处引用）。绝不碰 `bySession`/`removeOwner`。
- [ ] **`lib/domain/pipeline-orchestrator.ts`**：`:187` `doEvict(projectRoot, stage.agentId)` → 按 `stage.sessionId` 逐出（:181 已赋值）；catch `:167` 同步改（sid 可能 null→`evictSession` 内 getSession 返 undefined→no-op 安全）；注入口（`deps.evictSession ?? evictSession`）+ 头注 `:9-12` 订正为「按本阶段 sessionId evict」。**保留** `setOwner`(:182)/`acquireSlot`(:127)/cancel signal(:102)。
- [ ] **`lib/pi/evict-agent-sessions.test.ts`（追加，非新建）**：`evictSession` describe——faux registry 放 sidA/sidB 同属 agentX，断言 evictSession(sidA) 后 sidA destroy / sidB.isAlive() 仍 true / 流式先 abort / evict 后 getOwner(cwd,sidA) 仍返 agentX。
- [ ] **`lib/domain/pipeline-orchestrator.test.ts`（四处实质改造，非「只改第二参」，详设 §1.4 评审 CP-1/2/5）**：
  - (a) evictSpy（`:173-179`/`:681-687`）内层真函数 `evictAgentSessions(root,agentId,{sessionsForAgent,getSession})` → `evictSession(root,sessionId,{getSession})`、删 `sessionsForAgent` DI；注入口 `deps.evictAgentSessions`→`deps.evictSession`。
  - (b) 正常路径第二参 agentId→sessionId 的**真断言**在 `:212-215`（四阶段）+ `:738` + `:754`（`evictedAgents).toEqual` 改按 sid）。⚠️ spec 旧引 `:253/:737/:753` 是 `toHaveBeenCalledTimes` 计数行、差 1 行勿漏改下一行；**失败分支 `:386` 起 `:424/:447` 只有 count 断言、不绑 agentId、无需改**。
  - (c) **catch test⑥（`:243-260`）**：catch 时 `stage.sessionId` 恒 null → `evictSession(null)` no-op → `:256 expect(fauxMap.size).toBe(0)` 必 FAIL；改断言为 **size 不回落 / no-op**（catch 漏正崩 worker 自己一槽、有 idle+对账兜底），`:254` 改为断言以 null 被调，保留 failedReason + 后续 pending。
  - (d) **加固误杀覆盖（CP-2①）**：某阶段 agent 预置第二条「用户接管」会话 sidB（`fauxMap` + 同 agentId 名下）+ 断言「N 阶段 evict 后 sidB 仍在 fauxMap」。变异（误写回 by-agentId）→ sidB 被误删 → 断言红（非 vacuous）。

## 验收标准（AC）
- AC-3：每阶段 evict 只 destroy `stage.sessionId`、不逐同 agent 其他会话。
- AC-1 误杀根除：承重测试断言「同 agent 另一 sid 用户接管会话」N 阶段后仍在 Map。
- AC-2/F16：N 阶段串行跑完、活 worker 恒 ≤1（释槽不变）。
- AC-5：`concurrency-gate`/`HARD_CAP` 零改动（计槽语义不变）。

## 门禁 / 纪律
- lint / tsc / vitest 全绿（lead 独立复跑）。承重网变异检查（误写回 by-agentId）必须能让断言红。
- **红线 git diff --stat 收口**：`concurrency-gate.ts` / 内核 / owner-map(`bySession`/`removeOwner`) / `factory-config.ts`(HARD_CAP) / `agents/[agentId]/route.ts` **零改动**；`evict-agent-sessions.ts` 只新增 `evictSession`、不动 `evictAgentSessions` 本体。
