# T2 · 实现：计量改 in-flight（改动1）+ 删每阶段 evict（改动2）+ 测试

> 承重：是（依赖 T1 GO）｜ 依赖：T1 ｜ 对应 AC：AC-1/2/3/5/7 ｜ 权威：详细设计 §一

## 目标
方案 B 两改**成对**落地（缺一即 F16 复发）：计量口径 size→in-flight + 删流水线每阶段 evict；配套 concurrency-gate 单测 + orchestrator 承重 verify 重写。

## 子任务（最小可执行）
- [ ] **改动1** `lib/pi/concurrency-gate.ts`：新增 `inFlightSessionCount()`（`[...__piSessions.values()].filter(w => w.isAlive() && w.inner.isStreaming).length`、gate 内直接读 globalThis 不 import rpc-manager）；`:40` 默认 `activeCount` 改指它；保留 `activeSessionCount` 导出（除非 grep 证零引用）；头注 `:4-8` 更新。
  - [ ] lead 核 `AgentSessionLike`（inner 类型）暴露 `isStreaming` 的类型可达性。
- [ ] **改动2** `lib/domain/pipeline-orchestrator.ts`：删 `:187` 每阶段 `doEvict` + catch 兜底 `:166-170` + `evictAgentSessions` import(:29)/注入口(:46)/`doEvict`(:80) 孤儿；头注 `:9-12` 更新。**保留** `setOwner`(:182) + `acquireSlot`(:127) 顺序 + cancel signal 链。
- [ ] **改动1+2 必须同 commit**（成对铁律；分开提交会留「删 evict 未改计量」的死等中间态）。
- [ ] **`lib/pi/concurrency-gate.test.ts`（新）**：`inFlightSessionCount` 单测（faux globalThis Map + faux wrapper isAlive/isStreaming 真假混合，断言只数 alive&&streaming；空 Map/全 idle/混合边界）。
- [ ] **`lib/domain/pipeline-orchestrator.test.ts`（改）**：承重 verify `:98-227` 重写——删「每阶段 evict/size→0/peak≤1/含 evict 顺序」断言，改证：①N 阶段串行可跑完（completed 留 Map 但不计 in-flight→下阶段放行）②peak≤1 ③run 结束 evict 零调用 ④误杀根除（同 agent 另一 sid「用户接管」会话 N 阶段后仍在 Map 未 destroy）+ 负对照（completed streaming 永不翻 false→第2阶段死等超时）+ 变异检查（数 size→断言①必红）。T6 cancel 块只删「evict 被调」类断言、保 `failedReason==='已取消'`。

## 验收标准（AC）
- AC-3：`acquireSlot` 只数 in-flight；完成态/idle 不计槽。
- AC-2/F16：N 阶段串行跑完（承重 verify 断言①）。
- AC-1 误杀根除：承重 verify 断言④（同 agent 用户接管会话不被销毁）。
- AC-5：`__piSessions.keys()` 集合不变（completed 不主动删 Map）——单测守。
- AC-7：legacy `orchestrator.ts` 自动受益（同享 in-flight 计量）。

## 门禁 / 纪律
- lint / tsc / vitest 全绿（lead 独立复跑、不认 impl 自跑）。
- **承重网非 vacuous**：变异检查（计量误写数 size）必须能让承重断言红。
- **红线 git diff --stat 收口**：`evict-agent-sessions.ts` / owner-map / 内核 / `factory-config.ts`(HARD_CAP) **零改动**。
