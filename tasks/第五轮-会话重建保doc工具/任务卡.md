# 第五轮 · 会话 re-attach 重建受限 doc 工具集 —— 任务卡

> 详细设计：`docs/第五轮-会话重建保doc工具/详细设计.md`。范围=只修 profile 会话 re-attach。串行 T1→T2→T3。

## T1 · `reattachProfileSession` + 单测
**做**：在 `lib/pi/profile-session-wiring.ts` 新增 `reattachProfileSession({sessionId, filePath, projectId, projectRoot, profile})`：`SessionManager.open(filePath)` 重开既有会话 → spread `assembleProfileSessionOptions` + `assembleDocSessionOptions`（`extraSkillDirs` 透传）→ `createAgentSession` → `applyProfileRuntime` 重设 model → `registerInnerSession`。**不发首条 message**。
**先 spike**：确认 opened 会话的持久化 systemPrompt 与 resourceLoader 注入是覆盖还是叠加；若叠加，re-attach 只传 docOptions（tools+customTools）、不重注 systemPrompt。
**AC**：
- ① faux 单测：`reattachProfileSession` 后 `getActiveToolNames()` 恰=7 受限名（read/grep/find/ls + create_artifact/propose_edit/list_artifacts），不含 write/edit/bash。
- ② 重开的会话保留既有 jsonl 历史（messageCount > 0 或能读到首轮内容）。
- ③ systemPrompt 不重复角色注入块（spike 结论落实）。
- ④ lint+test+tsc 绿、零回归。

## T2 · `resolveOrReattachSession` 解析器 + 单测
**做**：新增 `lib/pi/session-reattach.ts`（或等价共享 helper）：活会话→返回；否则 `bySession[sessionId]` 有 agentId→反查 projectId(`ProjectRegistry.list().find(root===cwd)`)+取 profile→`reattachProfileSession`；非 profile/取不到→`startRpcSession`（generic）。
**AC**：
- ① bySession 有 agentId 且 profile 在 → 走 reattach（断言激活集含 propose_edit）。
- ② sessionId=mainSessionId（无 bySession 项）→ 走 generic（不含 doc 工具，符合主对话现状）。
- ③ agentId 在但 profile/项目被删 → 容错落 generic、不抛错。
- ④ root→projectId 命中/未命中纯逻辑测。

## T3 · 两路由接线 + 真机回归
**做**：`POST /api/agent/[id]` 与 `GET /api/agent/[id]/events` 的 not-alive 分支改调 `resolveOrReattachSession`（取代裸 `startRpcSession`）；`/api/agent/new` 不动。
**AC**：
- ① 真机 Phase-A/B 复现脚本（turn1 create→**重启 dev**→turn2 propose）：修后 turn2 **落 PendingChange**（修前 ❌→修后 ✅）。
- ② SSE 重连后（刷新页面/断连重连）会话仍含 doc 工具、propose_edit 可用。
- ③ 真浏览器走一遍 onboarding §三.8（建 doc-agent→create→改→propose→按块确认），跨一次 idle/重启仍闭环。
- ④ 门禁全绿 + 零回归。
