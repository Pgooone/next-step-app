# V2-6 · 端到端复用验证

> 批次 5，依赖 V2-1~V2-5。详见 `../../docs/第二轮-V2提议工具/详细设计.md` · V2-6。

## 目标
提议工具 + D3-D5 UI/API 端到端跑通，确认复用无回归。

## AC
- [ ] 逻辑层：`create_artifact`→真实文件+v1 侧车；`propose_edit`→PendingChange（无新版本/真实文件未变）；走 resolve 路由按块确认→新版本 + 真实文件同步更新。
- [ ] **局部修改正/负用例（coreIssue）**：多段文档只改一段→仅产 1 个 mod 块（其余进 equal 不产块）；传残篇→产大量 del 块（固化"必须整篇"契约）。
- [ ] **并发/外部编辑用例（D-V2-05/06）**：同一 artifact 已有未决→再 propose 被拒；外部改真实 crd.md 后确认→抛 EXTERNAL_MODIFIED 不静默覆盖。
- [ ] 真浏览器：ArtifactPanel diff/版本 + PendingChangeCard 按块 ✓/✗ + 版本下拉/rollback（复用 D4/D5 的 E2E fixture/drive）。
- [ ] 受限工具集双向负对照：write/edit/bash 不可用 + 3 提议工具能调起。
- [ ] D3-D5 既有单测无回归（test 全绿）。
- [ ] 三类门禁全绿（test + tsc + lint）。

## 提示（踩坑记忆）
- **凭证已配**：`~/.pi/agent` 已配 DeepSeek（default deepseek-v4-flash，已验证）→ **本轮可真起 pi agent 验证**、不必全靠 faux（faux 仍作 hermetic 单测）。注意这是 pi 运行时凭证，与 Claude Code team 的 Claude 凭证两层。
- E2E 别 `waitIdle` 死等：轮询期望态、`waitForResponse`；冷编译先看 dev 日志状态码再判是不是实现 bug（`next-step-d5-verified`）。
- 真浏览器用 repo-vendored 的 browser-e2e（非全局 run-e2e，APP_ROOT 会错）。
