# V2-4 · 接线改造

> 批次 3（V2-3 之后），依赖 V2-3。详见 `../../docs/第二轮-V2提议工具/详细设计.md` · V2-4。

## 目标
`lib/pi/profile-session-wiring.ts` 把 guard 接入点（:126-135）换成 `assembleDocSessionOptions`。

## AC
- [ ] wiring 用 `assembleDocSessionOptions` 替 `assembleArtifactGuardOptions`，spread 进 `createAgentSession({...options, ...docOptions, ...createOptionsOverride})`。⚠️ **D-V2-04/major4**：`options` 与 `docOptions` 都含 `tools` 键，**`docOptions` 必须排 `options` 后覆盖 `profile.tools`**（否则 profile.tools 若含 write/edit/bash 会泄漏）；spread 处加注释。
- [ ] 补齐 `projectId`（registry 反查 projectRoot 或入参透传）传给装配（提议工具闭包需要）。
- [ ] 经真实 `startProfileSession` + faux 起会话，工具集为受限集（含 7 名、不含 write/edit/bash）。**泄漏对照测**：profile.tools 含 write/edit/bash 的档案经装配后断言被覆盖掉。
- [ ] create/propose 闭环可跑（faux）。
- [ ] 门禁绿 + 更新 `lib/pi/README.md`。

## 范围 / 登记
- **仅 profile 会话**（同 P0 范围）；dispatch / 主对话 / idle 不在本轮（本就未接 guard）。
- 🔧 文档型 profile vs coding 型 profile 区分 = **登记后续**（本轮 profile 会话 = 文档会话）。
