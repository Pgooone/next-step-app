# V2-3 · 文档会话装配

> 批次 3（先于 V2-4），依赖 V2-2。详见 `../../docs/第二轮-V2提议工具/详细设计.md` · V2-3。

## 目标
新建 `lib/pi/doc-session.ts`：`assembleDocSessionOptions(deps)` 产出**受限工具集**会话选项（替 artifact-guard 装配位）。

## AC
- [ ] `tools` 白名单 = **7 项 `["read","grep","find","ls","create_artifact","propose_edit","list_artifacts"]`**（含全部 customTool 名，**D-V2-04 命门**；漏 3 工具名则 agent 调不到、闭环断）—— 无 write/edit/bash。
- [ ] `customTools` = `buildDocTools(deps)`（3 个提议工具）。
- [ ] 返回形态可直接展开进 `createAgentSession`（同 guard 的 `{ options }` 边界）。
- [ ] 单测：`options.tools` = 含且仅含上述 7 名、不含 write/edit/bash；`customTools` 含且仅含 create_artifact/propose_edit/list_artifacts。
- [ ] 门禁绿。

## 关键
- 比 guard **更简**：guard 要拦 write/edit（保留名 + 重建 operations）；本模块**不要** write/edit/bash，故白名单直接排除、customTools 只加 3 新工具，无需重建任何内核工具 operations。
- 安全依赖 V2-0 spike 已验证的"白名单**含全部 customTool 名** + customTools 提议工具"组合行为（D-V2-04：白名单漏名则 customTool 被内核 `agent-session.js:1831` 过滤）。
- skill 经既有 `assembleProfileSessionOptions` 的 skillNames（profile.skills）启用，本模块不重复处理。
