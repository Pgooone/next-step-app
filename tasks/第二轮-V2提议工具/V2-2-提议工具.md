# V2-2 · 提议工具（核心）

> 批次 2，依赖 V2-1 + V2-0(spike GO)。详见 `../../docs/第二轮-V2提议工具/详细设计.md` · V2-2。

## 目标
新建 `lib/pi/doc-tools.ts`：`buildDocTools(deps)` 用 `defineTool` 返回 3 个提议工具，闭包注入 deps。

## AC
- [ ] `create_artifact({kind,title,content})` → `ArtifactService.createArtifact(projectId,{...,author:sourceActor})` → 返回 `{id, filePath, version:1}`。
- [ ] `propose_edit({id, newContent})`（🔧 取舍1：收 id）→ `old=readCurrentContent(projectId,id)` → `buildReplacePendingChange({artifactId:id,sourceActor,oldContent:old,newContent})` → `pendingStore.save` → 返回 `{changeId, diffBlockCount, note}`；**不写盘**。
- [ ] `list_artifacts({})` → `listArtifacts(projectId)` → `[{id,title,kind,currentVersion}]`。
- [ ] deps（`{projectId, sourceActor, artifactService?, pendingStore?}`）闭包注入；projectId/sourceActor 来自装配期（execute 的 ctx 不带）。
- [ ] faux 单测：create 落 v1 + 真实文件；propose 落 PendingChange（磁盘有 pending JSON、无新版本、真实文件未变）；list 返回含 id 的清单。
- [ ] 门禁绿 + 新增/更新 `lib/pi/README.md`。

## 复用
`buildReplacePendingChange` / `PendingChangeStore`（pending-change-service）；`defineTool`（pi）；V2-1 物化。
**关键**：落的 PendingChange 与既有 resolve 路由 / PendingChangeCard 完全兼容（op=replace），确认流水线零新增。
