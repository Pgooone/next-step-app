# 决策表（Decision Log）

> 记录每一次有「多个选项 + 取舍」的拍板：当时的选项、最终选择、理由（含被否决项的否决理由）。
> **用途**：回溯找 bug 时，快速定位「为什么当初这么设计」，而非靠记忆或重读代码猜。
> **维护**：planner / 实现中冒出的「需 lead 拍板」点，决策后立刻追加一行（任务卡 DoD 的一部分）。改了 planner 建议的，务必记下为什么。

| 编号 | 阶段/卡 | 决策点 | 选项 | ✅ 选择 | 理由 | 关联 |
|---|---|---|---|---|---|---|
| D-01 | spike/D2 | 不 fork 内核下如何拦截 edit/write 写盘 | A) `excludeTools`+customTools　B) `noTools:"builtin"`+customTools　C) subscribe 事后拦截 | **B** | A 实测会把同名替身一起剔除（sdk.js:131-135，组合 A 替身未激活）；C 内核已异步写盘、来不及（rpc-manager.ts:75 fire-and-forget）。B 让内置集为空、同名替身存活。 | a685a91 |
| D-02 | 底座 | next-step-app 怎么起 | A) 复制 pi-web 基座为起点　B) 全新最小 Next.js | **A**（用户确认） | 文档既定路线，直接继承会话/SSE/工具/UI 与 API；后续复用 UI 的 Iter D 零额外搬运。 | f923751 |
| D-03 | A1 | 领域错误与 HTTP 的关系 | A) 领域层直接构造 HTTP 响应　B) 领域抛带 code 的错误、HTTP 映射独立 | **B** | 领域逻辑框架无关、可单测；HTTP 映射集中在 lib/api/errors.ts 供所有领域路由复用。 | d0b002a |
| D-04 | A1 | projects.json 写盘 | A) 直接 writeFileSync　B) 临时文件+rename 原子写 | **B** | 防崩溃/并发写损坏注册表；成本仅一行。 | d0b002a |
| D-05 | A2 | 前端状态层 | A) Zustand 真 store　B) useState 提升+自定义 hook | **A**（改 planner 建议） | 文档/CLAUDE.md 既定 Zustand；B/C/D 还会有 agent/dispatch/artifact 多个 store，Zustand 避免 prop 逐层钻透的返工；zustand 是文档钦定依赖、非投机。 | 44c9345 |
| D-06 | A2 | ProjectSwitcher 与既有 CWD picker | A) 替换 picker　B) 并存（项目在上、picker 保留） | **B** | 替换要删 recent/custom/default 三套交互、违反 surgical；并存让临时目录仍可用。 | 44c9345 |
| D-07 | A2 | cwd 注入点 | A) headerSlot prop + 复用 selectedCwd 受控值　B) 改会话创建逻辑 | **A** | ~3 行 surgical，不碰 /api/agent/new、rpc-manager、内核（红线）。 | 44c9345 |
| D-08 | A2 | 删除二次确认形态 | A) window.confirm　B) 内联确认条 | **B** | 不被浏览器拦截、更可控、UX 一致。 | 44c9345 |
| D-09 | A2 | zustand 版本 | A) 锁 v3（基座传递依赖）　B) 装 v5 直接依赖 | **B** | v5 是当前大版本、build 已过；基座那份 v3 是 @lobehub 传递依赖，二者独立互不影响。 | 44c9345 |
| D-10 | A2 | next-env.d.ts | A) 提交　B) gitignore | **B** | Next 构建产物、每次 build 重生，与已忽略的 .next/ 一致。 | 44c9345 |
| D-11 | 流程 | 是否建区 README | A) 不做　B) 每区薄 README（定位/归属/红线/指针） | **B**（用户提议+认可） | agent 进区即懂、降探查成本；薄+只放稳定信息+并入 DoD 防腐烂。 | d0776e3 |
| D-12 | A3 | doctor 运行方式 | A) 仅独立 npm run doctor　B) doctor + predev 自动阻断 dev | **B** | ①②失败时 predev 非 0 退出会中断 npm run dev，直接满足「阻断启动」AC；③④仅 warning 不挡。 | 1ced9fd |
| D-13 | A3 | doctor 跑 TS 的方式 | A) `node --experimental-strip-types`　B) `tsx` | **B**（改 planner 建议） | strip-types 是 Node **22.6+** 才有，Node 20/21 不支持，会让「检测 Node≥20」自相矛盾（旧 Node 连脚本都跑不起来）；tsx 在 Node 18+ 通吃、单一 TS 源。 | 1ced9fd |
| D-14 | A3 | 凭证检测手段 | A) `ModelRegistry.getAvailable().length>0`　B) authStorage.has 逐 provider | **A** | 与 /api/models:17 一致，统一覆盖 OAuth 登录态与 API Key，贴「有可用模型」语义。 | 1ced9fd |
| D-15 | A3 | 首屏向导形态 | A) 自动弹 ModelsConfig 模态　B) 可关 banner 引导 | **B** | 低打扰、符合「③缺凭证仅 warning 引导」语义；复用 ModelsConfig 不新写设置 UI。 | 1ced9fd |
| D-16 | A3 | doctor 检查逻辑放哪 | A) lib/domain/　B) lib/env/ | **B**（改 planner 建议） | 环境自检非业务领域；lib/domain 只放 Project/Agent/Artifact 业务实体；lib/env 仍在 lib/** 故 vitest 可测。 | 1ced9fd |
| D-17 | A3 | ~/.pi 可写测哪个目录 | A) ~/.pi/agent　B) ~/.pi | **B** | projects.json 固定在 ~/.pi（agentDir 的父级），测父级覆盖最全；PI_CODING_AGENT_DIR 只改 agent 子目录。 | 1ced9fd |
| D-18 | A3 | checkDeps 怎么检测「内核可加载」 | A) `createRequire().resolve(pkg)`　B) `await import(变量)`　C) `await import("字面量")` | **C**（实现期修正，非取舍） | 内核 ESM-only、`exports` 无 `require` 条件 → A 抛 `ERR_PACKAGE_PATH_NOT_EXPORTED`；B 被 webpack 当运行时动态请求、**Next 生产构建下失效**；C 可被 webpack 静态分析，vitest/tsx/Next 三上下文全通。checkDeps 因此改 async。 | 1ced9fd |
| D-19 | B1 | 删除 agent 是否删整个 `.pi/agents/<id>/` 目录 | A) 仅摘注册（仿 A1）　B) rmSync 整个目录 | **B** | 与 A1 刻意不同：项目 root 是用户真实代码目录、误删灾难性故只摘注册；agent 目录是 Next-Step 在 `.pi/` 下生成的内部资产，删档案=删目录才符合直觉、避免孤儿目录。 | 2c7187c |
| D-20 | B1 | agentMdPath/memoryPath 存相对还是绝对 | A) 相对 projectRoot　B) 绝对 | **A** | 与 docs/03 注释一致；项目目录移动/改名后相对路径仍有效，绝对路径会陈旧。消费侧 `join(root,path)` 还原。 | 2c7187c |
| D-21 | B1 | agent.json 与 .md 的分工 | A) agent.json 内联 .md 内容　B) agent.json 仅结构化元数据 + path 指向 | **B** | agent.json 为结构化真相源（list/get 扫它），.md 为正文资产、内容不内联，避免双写不同步。 | 2c7187c |
| D-22 | B1 | create 时是否校验 model/skills/tools 存在 | A) 校验　B) MVP 不校验 | **B** | 校验 model 需调 ModelRegistry、校验 skills 需扫技能目录，B1 引入耦合不划算；更适合 B2 起会话注入时校验/降级。B1 只存白名单字符串。 | 2c7187c |
| D-23 | B1/流程 | API 路由目录是否各带薄 README | A) 每个 API 路由组都加　B) 不加（沿用 projects 先例） | **B** | app/api/projects 本就无 README；API 路由是领域层的薄 HTTP 包装，契约在 docs/04 + 领域区 README，逐路由加 README 碎片化。区 README 约定仅针对 lib/** 等逻辑区。 | 2c7187c |
| D-24 | B2 | system prompt 注入点 | A) 事后改 `agent.state.systemPrompt`　B) 自建 `DefaultResourceLoader` + `appendSystemPromptOverride`　C) `systemPromptOverride` 整体替换 base | **B** | A 被 `_rebuildSystemPrompt` 多处覆盖（agent-session.js:555-556/812-816/1666-1667 每次从 loader 重读），rpc-manager.ts:321 现有先例只在「空工具且此后无 rebuild」窄路径侥幸成立、不可推广；C 会丢内核默认工具说明/guidelines（只想加不想换）。B 是唯一原生且扛 rebuild 的持久源（已核验：`DefaultResourceLoader` 导出于 index.d.ts:15、选项 `appendSystemPromptOverride` 于 resource-loader.d.ts:113、`createAgentSession` 有 `resourceLoader` 于 sdk.d.ts:50）。「头部」取「base 之后、project_context/skills 之前的显著首块」（append 段位置由 system-prompt.js 决定，字面整串最前需 C 替换 base、代价大故不取），用 `<agent_profile>`/`<agent_memory readonly>` 标签包裹强化只读语义。 | d3ad773 |
| D-25 | B2 | model 单 string 解析与「模型不存在」降级 | A) 拆成 provider+modelId 两字段　B) 保持单 string、按首个 `/` 解析 `provider/modelId` | **B** | docs/03 `AgentProfile.model` 是权威单 string，A 违反契约；modelId 可能含 `/` 故按首个 `/` 切分。降级：model 空/格式非法/`registry.find` 落空 → 跳过 set_model 用内核默认 + `diagnostics.modelFallback=true`，**不阻断起会话**（本地单用户工具，档案模型失效不该整体失败；「直接 422」更严格但体验差，否决）。 | d3ad773 |
| D-26 | B2 | skills 注入与「技能不存在」 | A) 校验技能存在否则报错　B) `skillsOverride` 按 name 从已发现集过滤、不存在静默忽略 + 记 `missingSkills` | **B** | 内核技能从目录发现（agentDir/skills、cwd/.pi/skills）按 frontmatter name 建 Map（skills.js:292-334）；profile.skills 是名字数组，正好用 `skillsOverride` 过滤（resource-loader.d.ts:80）。是否安装是 /api/skills/install 的事，B2 只「从已发现集选」；缺失静默忽略 + diagnostics 记录。成本一个 filter，纳入 B2 不拆。 | d3ad773 |
| D-27 | B2 | B2 封装的边界（端点 + 内聚度） | A) 仅返回「注入材料」数据、createAgentSession 调用与 set_model/thinking 全留给 B3　B) 不加端点，但 B2 拥有 `assembleProfileSessionOptions` + `applyProfileRuntime`（含 model 降级），createAgentSession 调用留给调用方 | **B**（改 planner 建议） | 不加用户态端点、不碰 /api/agent/new（契约「复用不动」、新增路由表无此端点、AC 可纯单测闭环）——这点采纳 planner。但**否决** planner「仅返回数据材料」：§5.2「模型不存在」降级是 registry 查找后的边界行为，若只返回 `{provider,modelId}` 数据、由 B3 写 setModel+fallback，则该边界逻辑落在 B3、B2 测不到。故 B2 须拥有 `applyProfileRuntime(session,profile,deps)`（registry 查找 + 降级 + setModel + setThinkingLevel）这一真实可测函数；createAgentSession 调用本身因绑会话生命周期/registry 仍留给调用方（B3 + 单测用 faux）。pure helpers 独立单测。 | d3ad773 |
| D-28 | B2 | profile.tools 为空时与 prompt 清空逻辑的协调 | A) 沿用 rpc-manager.ts:320 空工具清空 systemPrompt　B) 空 tools = 保留档案 prompt、不清空 | **B** | rpc-manager.ts:320-322 在 toolNames 为空时把 systemPrompt 置空，会抹掉档案注入（注入块=档案+记忆，是 prompt 核心价值）。B2 不碰 rpc-manager（归 B3 集成），故在封装注释写明此约定：profile 注入路径下空 tools=「无编码工具但保留档案 prompt」、不走清空分支；tools 收窄用 `setActiveToolsByName`（其 rebuild 从 loader 重读注入、不丢）。 | d3ad773 |
| D-29 | B3 | wiring（按档案接进真实起会话）是否纳入 B3 | A) B3 含 CRUD + 新会话端点 + 起会话入口（端到端接通）　B) B3 仅 CRUD、wiring 拆独立卡 B4 | **B**（用户拍板） | b3-planner 挖出硬事实：`/api/agent/new`→`startRpcSession`→`createAgentSession` 不传也不暴露 `resourceLoader`（rpc-manager.ts:305-310），结构上够不到 B2 注入，且「/api/agent/new 复用不动」红线 → wiring 只能走新端点 `POST /api/projects/[id]/agents/[agentId]/session`。但头号风险 E1（让会话进 rpc-manager 注册表否则前端无 SSE，而 startRpcSession 封死该步、可能被迫触碰 rpc-manager 归避项需批准）是 spike 级未知，且 wiring 本环境无浏览器/凭证无法自动验证、只能 build+人工。故拆：B3 干净可单测闭环，wiring 另起 B4、首步 spike E1（仿 D2 spike 先行）。代价：端到端按档案起会话等 B4。 | 3e5f8d1 |
| D-30 | B3 | 编辑表单 model/skills/tools 的选项来源 | A) 三者皆自由文本　B) 三者皆下拉　C) 有源用下拉、无源用受限输入 | **C**（tools 项改 b3-planner 建议） | model：下拉拉 `/api/models` 的 modelList、选中拼 `provider/modelId`（D-25）存、**允许留空**（→B2 modelFallback 默认）；skills：多选拉 `/api/skills?cwd=<projectRoot>`、存 name[]、无 cwd 禁用+提示；tools：**勾选内置编码工具固定集**（read/bash/edit/write/grep/find/ls，源自 rpc-manager.ts:299 / ToolPanel 预设常量）。b3-planner 原议自由文本（因 get_tools 是会话级、无全局源），lead 改为勾选固定集：内置集是已知稳定常量、勾选避免拼错、UX 更好（MCP/技能附加工具属会话级、profile 配置期不可知，超 MVP）。非法工具名校验仍后置（D-22）。 | 3e5f8d1 |
| D-31 | B3 | 删除档案二次确认形态与文案 | A) window.confirm　B) 内联确认条（仿 D-08 ProjectSwitcher） | **B** | 复刻 ProjectSwitcher 内联条（ProjectSwitcher.tsx:179-208），与 D-08 一致、不被浏览器拦截。**文案与项目删除相反**：项目删除强调「仅移除注册、不删磁盘」，档案删除须强调「删除整个 `.pi/agents/<id>/` 目录」（D-19：删档案=删目录），勿误抄项目措辞。 | 3e5f8d1 |
| D-32 | B3 | AgentManager 拆分粒度与是否复用既有配置组件 | A) 单文件多区　B) 拆 AgentList/AgentForm/DeleteConfirm 多文件 | **A** | 仿 ProjectSwitcher 单文件容纳 list/form/confirm 多状态区；表单仅 6 字段、不到拆文件复杂度（CLAUDE.md「单用途不抽象」）。**不复用** ModelsConfig/SkillsConfig 内部组件（其「全局设置」语义 ≠「选值塞进档案」），只复用其模态挂载范式 + `/api/models`、`/api/skills` 数据源。 | 3e5f8d1 |
| D-33 | B3 | 是否新增 useAgentStore | A) 新增 `useAgentStore`　B) 并入 useProjectStore | **A** | store 单一职责红线 + lib/stores/README 已预告 `useAgentStore`；档案 CRUD 与项目 CRUD 是两个领域，合并违反约定。仿 useProjectStore 的 fetch+refresh+CRUD，但加 projectId 维度（`loadedProjectId`，切项目重拉、不跨项目缓存脏数据）。 | 3e5f8d1 |
| D-34 | 工具/流程 | 浏览器 E2E 固化形态 | A) slash command　B) skill（按 skill-creator 规范手作） | **B**（用户选定） | command 只是 prompt 模板、装不下「无 root 装库 + launch 助手 + 驱动模板」这套实测脚本；skill 能 bundle 脚本 + 描述驱动自动触发（验 UI 时自动想到用）+ progressive disclosure。放仓库 `.claude/skills/browser-e2e/`（与 B4 代码隔离、不污染）；本项目「本地单用户」故 repo==本机、无 root setup 固进仓库不矛盾。skill-creator 作插件存在但不在本会话可调列表，故读其 SKILL.md 方法论手作。全链冒烟通过（setup→dev→建临时项目→AgentManager CRUD→落盘核验→清理，退出码 0）。 | cd9ed49 |
| D-B4-1 | B4 | rpc-manager 让新会话进注册表/SSE | A) startRpcSession 加 options+postCreate　B) 新增 registerInnerSession 提取注册段 | **B** | startRpcSession 封死 createAgentSession options/toolNames 段，A 与档案 tools 冲突；B 仅提取注册段为 ~10 行新函数、零回归，端点自行 createAgentSession 注入档案 options。createAgentSession 原生收 tools:string[]，B2 已用此字段传 profile.tools，绕开旧 toolNames 段零冲突。 | `de9d16b` |
| D-B4-2 | B4 | 新端点 cwd 来源 | A) 请求体传　B) 服务端取项目 root | **B** | cwd 一律 registry.get(id).root（A2 单一真相源），杜绝前端绕开项目边界。 | `de9d16b` |
| D-B4-3 | B4 | 端点是否带首条 message | A) 纯建会话不发　B) 带首条 message 一步建会话+发首条 | **B** | 内核 createAgentSession 未发 prompt 时懒落盘（sessionFile 有值但磁盘无文件），纯建会话后 loadSession 读到 id 错位的幻影空会话；带首条（/api/agent/new 同款已验证路径）触发真落盘。代价：前端行内 input 收首条、禁 window.prompt。 | `de9d16b` |
| D-B4-4 | B4 | idle 重建丢 live 注入 | A) 接受并记录　B) 本卡处理 | **A** | idle 销毁后 SSE 路由重走 startRpcSession 重建无档案注入→model/thinking 回默认（systemPrompt 已落盘仍在）；超 B4 范围，端点留注释、Iter D 再议。 | `de9d16b` |
| D-B4-5 | B4 | 诊断前端提示 | A) 仅 console.warn　B) toast | **A** | 诊断已在返回体，MVP 仅 store console.warn，toast 后置。 | `de9d16b` |
| D-B4-6 | B4 | 编辑档案后注入旧 role（AC④ bug）修法 | A) update 重写 agent.md（renderAgentMd create/update 共用）　B) 不改 | **A** | 根因：update 只写 agent.json 不重写 agent.md，注入读 agent.md 文件→编辑 role 后注入旧 role。抽 renderAgentMd 给 create/update 共用避免模板漂移。重写的触发条件见 D-B4-8。 | `de9d16b` |
| D-B4-7 | B4 | 端点组合逻辑放哪 | A) 写死 route(app/)　B) 抽 lib/pi/profile-session-wiring.ts、route 退薄壳 | **B** | vitest include 仅 lib/**，逻辑留 app/ 的 route 则 faux 测不到 AC②③④；抽 lib/pi 用依赖注入（registerInnerSession/sessionManager/createOptionsOverride）可 faux 集成测。代价多一层 + 3 个仅测试用可选参数（生产省略）。 | `de9d16b` |
| D-B4-8 | B4 | update 重写 agent.md 是否无条件（agent.md 定位） | A) 无条件重写　B) 仅 name/role 实际变更才重写 | **B**（用户拍板） | agent.md 定位为「可手工编辑的资产」：仅 name/role 实际变化才重渲染骨架，model/skills/tools/thinking 变化保留手写内容。⚠️ role 变更仍必须重写（守 AC④，D-B4-6 命脉）；比较用 next.X!==current.X（实际值），「传了 role 但值没变」也不误触发。真浏览器 E2E 两路验证（仅改 model→手编保留；改 role→重渲染骨架）。 | `de9d16b` |
| D-C-1 | Iter C/方向 | worker 产物落盘形态（C 先于 D、Artifact 抽象未建） | A) 轻量普通文件　B) 提前引入 Iter D Artifact 抽象 | **A**（用户拍板） | Orchestrator 把每个 worker 最终 assistant 输出落成 `.pi/artifacts/<dispatchId>/<seq>-<agent>.md` 普通 Markdown，`Assignment.output` 记相对路径；版本/Diff/HITL 全留 Iter D。否决 B：需把 D1（artifact-service+版本表）前置、引入 C→D 耦合与返工，违反「C 先于 D」与 Simplicity First。 | 用户拍板（`157a888`） |
| D-C-2 | Iter C/方向 | C1 多 agent 真实执行的验收口径（本地 auth.json 当前为空、无凭证） | A) faux 单测为主 + 真实端到端留待验项　B) 配真凭证跑真实端到端 | **B**（用户拍板，非 lead 推荐 A） | 用户要求真实多 agent 端到端跑通作为验收门（比 faux 更贴真实价值）。⚠️ 前提：`~/.pi/agent/auth.json` 当前为空 `{}`、无任何 provider 凭证，需用户先在 Models 设置页登录/填 key。落地节奏：实现期仍写 faux 单测+领域单测打底（不依赖凭证、即时抓编排 bug），实现完成后由用户配凭证再走真浏览器+真 LLM 端到端验收（仿 B3/B4）。 | 用户拍板（`157a888`） |
| D-C-3 | Iter C/方向(C2) | DispatchPanel 挂载位置 | A) 模态弹窗（仿 AgentManager）　B) 右侧常驻面板/Tab　C) 独立路由页 /dispatch | **A**（用户拍板） | 对齐现有 AgentManager 模态先例（`agentManagerOpen` 范式）、最 surgical、UI 风格一致；顶栏按钮打开，模态内设目标+选 agent+写子任务+看汇总，轮询/SSE 刷新 assignment 状态。否决 B（改现有右栏布局）/C（与单页壳 AppShell 风格割裂）。 | 用户拍板（`157a888`） |
| D-C1-1 | C1 | 并发≤3 闸门方案（AC⑤） | 超限行为 A) 拒绝该 dispatch　B) 等待空位+超时兜底；粒度 单一全局 gate vs 并发池 | **B 等待 + 单 gate**（lead 拍板，采纳 c1-impl 推荐） | 闸门在 lib/pi/dispatch-runner，计数源=globalThis.__piSessions（含前端聊天会话）。串行派发自身最多占 1 槽，AC⑤ 主要约束 worker+前端会话总和；起每个 worker 前 gate 检查 registry.size<3，超限轮询等待（默认 ~60s 超时→该 worker fail 并提示「活跃会话达上限 3，请关闭部分会话后重试」）。**另设 worker 执行超时**：agent_end 迟不来则 abort+failed（默认 5min 常量），别只做 gate 超时。否决「拒绝」：体验差（前端开 3 会话即不能派发）。单 gate 足够：串行下同时活跃 worker 恒为 1，不需并发池；gate 检查→会话注册有异步 TOCTOU 窗口，串行+单用户可接受（注释记之）。 | `157a888` |
| D-C1-2 | C1/spike | faux 单测如何断言 worker「产物」 | A) 桩 orchestrator、不验真实产物　B) faux 预置响应驱动真实 agent_end | **B**（spike 实证） | 仿 B4 faux 装配，`AgentSessionWrapper.onEvent` 挂 agent_end 监听后再 send prompt，能收到 agent_end(willRetry=false) 且 messages 末条 assistant 含预置文本。⚠️ faux 默认队列空、stream 报「No more faux responses queued」不产文本，须每次 prompt 前 `reg.setResponses([fauxAssistantMessage(...)])`（B2/B4 没踩到因其 fire-and-forget 不断言产物）。故 C1 单测每 worker 起会话前 queue 一条，可真实断言 串行/上游喂下游/落盘/产物文本，无需桩 orchestrator。 | `157a888` |
| D-C2-1 | C2 | 产物链接打开方式 | A) MVP 纯文本展示路径　B) 复用现有 handleOpenFile 在 FileViewer 打开 | **B**（lead 拍板，采纳 c2-impl 提案） | AppShell 已有 `handleOpenFile(filePath,fileName)` 回调（Agent 管理同款用文件 tab/FileViewer）；DispatchPanel 收 onOpenFile 回调（仿 onSessionStarted 接线），Assignment.output 渲染为可点击链接复用之，无需重造查看器。路径基准须对齐：output 相对 projectRoot = FileViewer 经 /api/files 的 cwd 基准。 | `157a888` |
| D-C1-3 | C1 | GET /api/dispatch/[taskId] 如何定位随项目落盘的任务（契约路径无 projectId） | 1) 跨项目扫描 registry 各项目 .pi/dispatch　2) 全局扁平 ~/.pi/dispatch　3) 改契约传 projectId | **1 跨项目扫描**（lead 拍板，采纳 c1-impl 推荐） | docs/04 契约 POST /api/projects/[id]/dispatch（建任务需项目上下文）与 GET /api/dispatch/[taskId]（仅 taskId）非对称，暗示 taskId 是全局唯一句柄、查询不需项目上下文；方案1（DispatchStore.findTask 遍历 registry 各项目 .pi/dispatch 找 <taskId>.json）正是该语义的正确实现而非 workaround，贴合 §03「随项目落盘」、不动契约不破坏 C2、单用户少项目 O(项目数) 无忧。否决 2（违背随项目落盘、与 artifacts 不一致）/3（改契约+C2 返工）。可选优化（非必须）：建任务时进程级 Map 记 taskId→projectId，GET 命中 O(1)、未命中回退扫描。 | `157a888` |
| D-C2-2 | C2 | DispatchTask/Assignment 类型真相源 | A) 前端 lib/stores 本地重复定义　B) import type 复用 lib/domain 导出 | **B**（lead 协调，c2 落实） | 前端 useDispatchStore 用 `import type { DispatchTask } from "@/lib/domain/dispatch-store"` 并 re-export，消除与 domain 的重复定义；`import type` 编译期擦除、不把服务端 node:fs 拖进客户端 bundle（同 B3 useAgentStore import AgentProfile）。否决 A：双份定义易漂移、违反单一真相源。 | `157a888` |
| D-C2-3 | C2 | 产物链接路径基准（/api/files 解析方式） | A) 直接传相对 output　B) joinFilePath(projectRoot, output) 拼绝对 | **B**（c2 实测拍板，纠正 D-C2-1 错误假设） | 实测 /api/files 把 [...path] 当**绝对路径**解析（filePathFromSegments = "/" + joined 再 path.resolve 校验 allowed-roots），**不**按 cwd 拼。Assignment.output 是相对 projectRoot，直接传会变 /.pi/artifacts/... 命中文件系统根 → 404/Access denied。故 DispatchPanel 加 projectRoot prop（AppShell 传 currentRoot，同 AgentManager），点链接时 joinFilePath(projectRoot, output) 拼绝对路径再交 onOpenFile、getFileName 取文件名。⚠️ 本条纠正 D-C2-1 理由中「output = …cwd 基准」的错误表述。真浏览器 E2E 抓到绝对 URL 验证 PASS。 | `157a888` |
| D-C1-4 | C1 | 中文 agentName 产物文件名净化（真实端到端发现的 bug） | A) whitelist `[^\w.\-]+`→`_`（中文全变 `_`）　B) denylist 仅替换非法/控制字符 | **B**（真实 DeepSeek E2E 暴露 A 的 bug，lead 拍板修） | 真实端到端发现：whitelist `\w` 只匹配 ASCII，中文「需求分析师」整体→`_`，产物文件变 `1-_.md` 信息全丢（faux 测 agent 名是 ASCII 没暴露——真实验收价值）。抽 `sanitizeFileName`：仅替换 `/ \ : * ? " < > |` + 控制字符→`_`、去首尾空白与点（防 `..` 越目录 + Windows 尾点）、净化为空兜底 `"agent"`；中文/Unicode 原样保留→`1-需求分析师.md`。+5 单测（纯函数 4 + orchestrator 中文端到端 1）。 | `157a888` |
| D-D1-1 | D1 | 受管 artifact 落盘命名空间 | A) 同根 `.pi/artifacts/<id>` 靠 artifact.json 区分　B) `managed/` 子目录隔离　C) 独立根 `.pi/managed-artifacts/` | **B** | 与 Iter C `<dispatchId>/` 物理隔离、对 Iter C 零回归、listArtifacts 只扫 `managed/`、`findArtifact` 不会误命中派发产物目录；仅需回写一行文档。否决 A（与 `<dispatchId>/` 混在同根、扫描需逐个 stat 区分）/C（独立根多一处约定、与既有 `.pi/artifacts/` 语义割裂）。 | `f9473b1` |
| D-D1-2 | D1 | submitVersion 的 HTTP 路由 | A) `POST .../submit-version`　B) `PUT /api/artifacts/[id]`　C) 复用 rollback 路由 body 区分 | **A** | 与 rollback 路由对称、与 sf-mini 命名一致、不改 GET 契约。否决 B（PUT 语义与「追加新版」不符、易与「整体替换元数据」混淆）/C（一个路由两种语义、入参分支化）。 | `f9473b1` |
| D-D1-3 | D1 | 乐观锁原子写机制 | A) 写前 `assertVersionMatch` + 新版独立文件 + tmp/rename，单用户不加锁　B) proper-lockfile 真互斥 | **A** | 与既有 store `atomicWrite` 同款、单用户无需文件锁、与 D-C1-1 并发哲学一致；「读 version → 写盘」间 TOCTOU 窗口串行单用户可容忍（service 注释记之）。否决 B：引第三方锁库、对单进程单用户过度工程。 | `f9473b1` |
| D-D1-4 | D1 | 是否预埋 realpath→artifactId 索引 | A) 不预埋，仅 `extra` 留口子　B) D1 就建全局索引 | **A** | realpath 形态/相对基准取决于 D2 拦截层细节，现在定死=赌 D2 接口、违反「未决设计不预实现」；索引连同 D2 一起做。否决 B：提前引入未验证假设、易返工。 | `f9473b1` |
| D-D1-5 | D1 | createArtifact 入口纳入 D1 | A) 纳入：service 函数 + `POST /api/projects/[id]/artifacts`　B) 留到 D2 | **A** | 「读写」含写、无创建途径则 GET/submit/rollback 无数据可操作、单测无 fixture、D2 拦截强依赖；契约表补一行。否决 B：D1 自身无法闭环自测。 | `f9473b1` |
| D-D1-6 | D1 | 路由测试边界 | A) 遵循 D-23 + 既有零路由测试先例，行为全用 service 领域单测覆盖　B) 扩 vitest include 到 `app/**`　C) 抽 handler 到 `lib/api` 再测 | **A**（本条修正 lead 原方案清单里「写路由集成测试」的建议） | 路由是 service 薄壳、契约真相源在 service、破例制造不一致、不动测试配置不给后续卡留负担。所有「会经由路由暴露的语义」（乐观锁 409 带/不带 ifMatch、422 INVALID、404 NOT_FOUND、rollback 复制/双计数/原子写）在 `artifact-service.test.ts` 穷尽覆盖；路由层只做 `Number(If-Match)` 解析（非数字→INVALID/422）+ 透传 + domainErrorResponse，靠与 dispatch 路由同构 + build(TS strict) 保证、不单测。否决 B（跨卡基础设施变更、超 D1 范围）/C（抽象层违反「不另起炉灶」）。 | `f9473b1` |
| D-D1-7 | D1 | If-Match helper 的依赖边界 | A) `import ArtifactError`，引入 lib/api→domain 依赖　B) 抛带 `code` 的普通错误，鸭子类型与 errors.ts 一致、不依赖 domain | **B**（lead 复核源码后纠正 d1-impl 初版） | `errors.ts` 刻意用鸭子类型（`"code" in error` + 字符串状态表）映射、README 明示「与 HTTP 解耦」且零 domain 依赖；d1-impl 初版 if-match.ts `import ArtifactError` 是首次打破该解耦。改抛 `Object.assign(new Error(...), { code:"INVALID" })`，`domainErrorResponse` 照样映射 422、行为零变化。顺带把 NaN 守卫 `Number.isFinite` 收紧为 `Number.isInteger`（版本号是整数，`"1.5"` 不该当合法 If-Match）。 | `f9473b1` |
| D-D2-1 | D2 | noTools:builtin 清空全部内置工具，真实会话仍需 read/bash/grep/find/ls，如何「保留其余 + 替换 write/edit」 | A) noTools:"builtin"+customTools[拦截 write/edit + 内核工厂重建 read/bash/grep/find/ls]　B) tools 白名单+customTools　C) 保留内置+createWriteToolDefinition/createEditToolDefinition 注入 operations 分流 | **C（impl 实测后选定；A 为已验证保底未采用）** | 阶段0 实测（spike/d2-intercept/stage0-toolset.ts，9/12 PASS）A 得全 7 工具且 write/edit 是替身；3 个 FAIL 是 B 的预期证伪（allowlist 把不在名单的 customTools write/edit 一并挡掉）→ B 排除。**回填：impl 续测候选 C（stage0-candidate-c.ts 8/8 + APP 内 faux 端到端 4 PASS）确认可行 → 采用 C**：零工具漂移（write/edit 保留内核 name/schema/label）、edit 语义归内核、details.diff/patch 内核自动生成、注入 readFile 顺带解契约 gap。operations 是 cwd 级单函数对所有路径生效→注入 readFile/writeFile 内部自分流（受管拦截/非受管委托真 fs）。⚠️ 内核 ToolDefinition 泛型逆变方差致收进数组必报错，单点 GuardToolDef=ToolDefinition&lt;any,any&gt;+eslint-disable 限一行（内核 ToolDef 同款、未从包根导出故不深 import）=内核 API 客观限制非偷懒。A 重建用内核现成工厂（createReadOnlyToolDefinitions 不含 bash 须 createBashToolDefinition 单补）、内核自身 createCodingToolDefinitions 同款组合、漂移可控；类型须 ToolDefinition&lt;any,any&gt;[] 规避泛型逆变方差（内核同款）。C 更省代码+零工具漂移但 readFile 注入未验。核心捕获逻辑（resolveManagedTarget+切块+落盘）与装配解耦、先实现+单测、装配最后定，A/C 之选不致返工。非受管放行：拦截工具委托内核内置实例 execute、免复刻 edit apply/diff 语义。 | 待回填（D2） |
| D-D2-2 | D2 | 替身拿到写盘路径如何判定受管+解出 artifactId；是否建持久化索引 | A) 运行时计算（resolve+扫 registry，不建索引）　B) 建持久化 realpath→artifactId 索引 | **A** | 单用户项目少、同 findArtifact/findTask 扫描哲学、与 D-D1-4（故意不预埋、留 D2 运行时建）方向一致；持久化索引=提前优化+一致性负担。新增 lib/pi/artifact-intercept.ts: resolveManagedTarget(absPath, registry) 返回 {projectId,artifactId} 或 null；execute 内 path.resolve(ctx.cwd, params.path) 归一（处理相对/..），命中 &lt;root&gt;/.pi/artifacts/managed/&lt;id&gt;/ 前缀 + existsSync(artifact.json) 确认防误判。放 lib/pi（工具拦截层、import 内核类型；与 artifact-service 纯领域分层）。 | 待回填（D2） |
| D-D2-3 | D2 | PendingChange 落盘位置 | A) managed/&lt;id&gt;/pending/&lt;pendingId&gt;.json　B) 全局/独立目录 | **A** | D1 布局兼容（pending/ 与 artifact.json、versions/ 平级；findArtifact 只看 artifact.json 不误扫）、随 artifact 就近落盘、删 artifact 连带清理；沿用 atomicWrite。 | 待回填（D2） |
| D-D2-4 | D2 | 替身如何知道哪个 agent 发起编辑（sourceActor） | A) 从 execute ctx 取　B) 工厂闭包注入 | **B** | 实测 execute 第 5 参 ctx=ExtensionContext（types.d.ts:335）无任何 agent 身份（仅 ui/mode/cwd/sessionManager 无 agentId/model 等）；故工厂 makeInterceptTool({sourceActor,...}) 闭包捕获。D2（方案甲）faux 测模拟传入；真实从 profile.name 取留接线卡。 | 待回填（D2） |
| D-D2-5 | D2 | DiffBlock 切块算法来源 + 切块/PendingChange 逻辑放哪模块 | 切块 A) 手写极简行级 diff　B) 加 diff 库依赖　C) 深 import 内核 edit-diff；模块 X) 新建 pending-change-service.ts　Y) 并入 artifact-service | **手写 A + 新建 X**（手写若行级对齐过重易错，允许 impl 评估改 B 并记） | sf-mini 后端切块算法未归档（archive 仅前端 InlineHighlightView/DiffBlockView + 数据形状）只能自研；手写行级（write→旧 vs 新内容行级对齐聚 add/del/mod；edit→按 oldText/newText 直接映射块）符合「不引入投机依赖+最小代码」。否决 C：内核 edit-diff 不从包根导出、深 import dist/core 触红线「只封装不 fork」。否决 Y：artifact-service 已 307 行职责是版本表，切块/HITL 独立关注点、新模块单一职责（store 红线）、切块纯函数独立可单测。**回填：走 C 装配后 operations.writeFile 只拿到内核 apply 后 finalContent、拿不到原始 edits，故 edit 也按 op=replace+computeReplaceDiffBlocks（与 write 统一）；diffBlocks 质量不降反升（oldContent 经内核 fuzzy 对齐）；computeEditDiffBlocks/buildPatchPendingChange 保留+单测、C 路径暂不走，接线卡改 A 路线或需 op=patch 时现成可用。** | 待回填（D2） |
| D-D2-6 | D2 | D2 是否接进真实业务会话（profile-session-wiring/dispatch-runner/orchestrator） | A) 仅拦截层+注入封装+faux 端到端验证、不改业务端点　B) 同时接进三个业务会话端点 | **A 方案甲**（lead 拍板） | 对齐 D1 先例（只做 service+路由+单测不接业务）；D3/D4 面板+按块确认 UI 未就绪、现接业务=「写不进盘又无处确认」半通电黑洞；当前无 agent 编辑 managed artifact 业务流（artifact 仅经 HTTP API 改）、不接端点不破不变量 2 实际场景；小步低回归。已知 gap（留接线卡）：agent 读 artifact 当前内容的「文件接口」——内容存 versions/&lt;n&gt;.json 非裸文件、agent 内置 read 读不到，须接线时解决（物化工作副本 or 拦截 read）。 | 待回填（D2） |

> 标「待回填」的行在对应任务卡落地提交后补 commit 哈希。
> 标「改 planner 建议」的行是 lead 否决了方案设计员的推荐——回溯时重点看这些。
