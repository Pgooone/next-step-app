# T3 · 真浏览器验收 + 机制层零回归

> 批次 3（收口），依赖 T1+T2。详见 `../../docs/第四轮-删除受管文档/详细设计.md` §七。

## 目标
真浏览器验全部「必走」项 + 复跑机制层 test 证零回归。**不写新代码**（除非揪出 bug）。

## AC（真浏览器·必走，pageErrors 须空）
- [ ] ArtifactPanel 头部有删除按钮；受管分组行 hover 出垃圾桶。
- [ ] 点删除 → 两步二次确认、**不立即删**；取消 → 文档仍在。
- [ ] 确认 → DELETE 调用 → 受管分组该项消失（数量 -1）；侧车目录 + 物化 .md 双双不存在（API/fs 验）。
- [ ] 删的是当前打开项 → 右栏回 FileViewer/空（selectedArtifactId 清）。
- [ ] **彻底删（A1）**：删后该 .md **不在普通树出现**（区别于降级会重现）。
- [ ] 删带 pending 的文档 → 中栏确认卡消失、pending 目录清。
- [ ] 两入口删行为一致；入口②删非当前打开项 → 分组更新、右栏不误清他项。
- [ ] （可选）并发：version 已变后删 → 409 toast、不误删。

## AC（零回归 + 门禁）
- [ ] 复跑 `artifact-service.test.ts`（含新 deleteArtifact 用例）/ `pending-change-service.test.ts` / `doc-tools.test.ts` 全绿。
- [ ] `npm run lint && npm run test` 通过；tsc 0。

## 注意
真浏览器走 **repo-vendored browser-e2e**（非全局 run-e2e）；**fixture 项目根放允许根 `~/pi-cwd-<date>/` 下**（否则文件 API 403、普通树空，见 `next-step-browser-e2e` 记忆）；断言前**轮询等树就绪**（冷编译慢）。本机 3.4G 无 swap，E2E 重启 dev 控内存、串行。fixture 可复用 V3 的 `scripts/v3-e2e-fixture.mts` 思路造受管文档（含带 pending 的）。
