# T4 · 端到端真浏览器验收 + 机制层零回归

> 批次 4（收口），依赖 T1+T2+T3。详见 `../../docs/第三轮-受管文档入口合并/详细设计.md` · §七验收清单。

## 目标
真浏览器验全部「必走」项 + 复跑机制层 test 证零回归。**不写新代码**（除非揪出 bug）。

## AC（真浏览器·必走，pageErrors 须空）
- [ ] 含受管 artifact 的项目：左栏 Explorer 顶部出现「受管文档」分组，列出全部 listArtifacts 条目（title + kind·v），数量与 API 返回一致。
- [ ] 点分组一条 → 右栏切 ArtifactPanel（title/版本下拉/按块确认可用）、`selectedArtifactId` 被 set；不经 filePath 反查即成功。
- [ ] 物化到项目根的 crd.md **只在受管分组、不在普通树重复**；多次刷新无闪现残留（验 `artifacts!==null` gate）。
- [ ] 左栏底部无「Artifacts」按钮；无任何入口能弹出 ArtifactPicker。
- [ ] 文件树根级看不到 `.pi`（无该行、不可展开裸 JSON）；同树 crd.md 仍正常显示可点 → `.pi` 隐藏未误伤项目根 md。
- [ ] 派发面板点开某产物（`.pi/artifacts/<dispatchId>/` 下）仍能在 FileViewer 正常打开 → type=read 不受影响。
- [ ] 受管分组可折叠；无受管 / projectId 空时整组不显示、不报错、不发多余请求、普通树照常。
- [ ] 点普通文件 → 右栏走 FileViewer；受管 .md 不能从普通树再用 FileViewer 打开（不在普通树出现）→ **双入口已堵**。
- [ ] 纯浏览受管文档（无 pending）：右栏 ArtifactPanel 正常、无「N 处待确认」、中栏无 pending-change-card；预置 pending fixture 再点开 → 中栏出现确认卡、resolve 全块后消失。

## AC（零回归 + 门禁）
- [ ] 复跑 `artifact-service.test.ts` / `pending-change-service.test.ts` / `doc-tools.test.ts` 全绿（侧车与三写方法未改）。
- [ ] `npm run lint && npm run test` 通过。

## 注意
真浏览器走 **repo-vendored browser-e2e**（非全局 run-e2e）；本机 3.4G 无 swap，E2E 重启 dev 控内存、串行（见记忆 `next-step-browser-e2e` / `next-step-local-oom-constraint`）。本轮无 agent 流式场景、OOM 风险较低。
