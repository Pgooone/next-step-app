# T3 · 接线 + 删 Artifacts 按钮/ArtifactPicker

> 批次 3，依赖 T2（FileExplorer 已具 onOpenArtifact 入参）。详见 `../../docs/第三轮-受管文档入口合并/详细设计.md` · §五 T3 + ADR D-V3-08。

## 目标
把受管分组点击接到 `open(id)`；删左下 Artifacts 按钮 + ArtifactPicker 弹窗及 orphan。

## AC
- [ ] `SessionSidebar.tsx`：自取 `currentRoot`（已有 `currentProjectId`:372），FileExplorer 挂载处(:865-873)透传 `projectId/projectRoot/onOpenArtifact`（**自取、不经 AppShell 冗余传**，D-V3-08）。
- [ ] `AppShell.tsx`：新增 `handleOpenArtifact = useCallback((id)=>{ useArtifactStore.getState().open(id); setRightPanelOpen(true); },[])`；给 `<SessionSidebar>` 传 `onOpenArtifact`（紧挨 :409 `onOpenFile`）。
- [ ] 删 orphan：底部按钮排 `label:'Artifacts'` 项(:491-503)、ArtifactPicker 挂载块(:1011-1017)、state `artifactPickerOpen`(:62)、`handlePickArtifact`(:354-358)、import ArtifactPicker(:15)。
- [ ] 删 `components/ArtifactPicker.tsx` 整文件（删前再 grep 确认除 AppShell 无引用）。
- [ ] **不碰**：`useArtifactStore` import、`setRightPanelOpen`、`handleOpenFile`（DispatchPanel :1008 复用）。
- [ ] grep 无 `artifactPickerOpen`/`setArtifactPickerOpen`/`handlePickArtifact`/`ArtifactPicker` 活引用残留；门禁绿（无未用变量 / 缺失 import）。
- [ ] 更新 `components/README.md`（Artifacts 按钮 + ArtifactPicker 弹窗已删、入口并入 FileExplorer）。

## 注意
T3 完成后链路 A 端到端可走、旧入口彻底移除。**只清「本轮改动产生的 orphan」，不碰无关 dead code**。
